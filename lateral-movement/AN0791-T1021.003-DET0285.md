### 1. Technique Breakdown: T1021.003

* What it is: Remote Services: Distributed COM. Adversaries use Distributed Component Object Model (DCOM) interfaces (such as MMC20.Application, ShellWindows, or ShellBrowserWindow COM objects) to execute arbitrary commands remotely on targets. When accessed, the system DCOM service host (svchost.exe launching DcomLaunch) spawns the corresponding DCOM server host process (like mmc.exe, explorer.exe, or dllhost.exe), which in turn executes the attacker's command by spawning a shell interpreter (cmd.exe or powershell.exe). This distinctive parent-child-grandchild pattern is highly indicative of DCOM abuse.
* Log Source Requirements: Sysmon process creation logs (Event ID 1) capturing process command line and parent/grandparent relationships.
* Coverage Check:
  * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 logs the parent process of a newly spawned shell. If the parent is mmc.exe or explorer.exe and its parent is svchost.exe, this chain represents DCOM execution.
  * NIDS (Suricata + Zeek): YES. Network logs capture RPC/DCOM bind requests targeting remote endpoints, but host logs are required to confirm successful command launch.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 13 (registry_set)
* Log Sources Missing / Unused:
  - None.
* Conclusion: Distributed COM remote executions and configuration changes can be detected using process creation and registry logs.

---

### 2. Custom Sigma Rule

The detection logic is split into process creation monitoring and DCOM registry configuration monitoring.

Rule 1 of 2: DCOM Server Spawning Suspicious Shell (Process Creation)
```yaml
title: Lateral Movement - DCOM Server Spawning Suspicious Shell (AN0791)
id: 69555c7e-4226-4d1f-9082-4530593ed5c1
status: experimental
description: Detects command-line interpreters (cmd.exe, powershell.exe) spawned by DCOM server hosts (mmc.exe, explorer.exe, dllhost.exe) that are launched under the system DCOM service, indicating DCOM-based remote code execution.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
detection:
    selection_parent:
        ParentImage|endswith:
            - '\mmc.exe'
            - '\explorer.exe'
            - '\dllhost.exe'
    selection_child:
        - Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
        - OriginalFileName:
            - 'Cmd.Exe'
            - 'PowerShell.EXE'
            - 'wscript.exe'
            - 'cscript.exe'
            - 'mshta.exe'
    filter_user:
        ParentCommandLine|contains:
            - ' /factory' # COM server flag
            - '-embedding'
    filter_installer:
        ParentImage|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: selection_parent and selection_child and filter_user and not filter_installer
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Legitimate administration scripts triggered via COM objects by automated software (e.g., local backup agent launching command triggers).
level: high
tags:
    - attack.lateral-movement
    - attack.t1021.003
    - analytic.an0791
    - detection.det0285
```

Rule 2 of 2: DCOM Configuration Modification (Registry Set)
```yaml
title: DCOM Configuration Modification (AN0791-B)
id: 1a1e8121-f66e-4921-81a0-a245ed3aa91a
status: experimental
description: Detects registry modifications targeting DCOM system configurations or AppID registrations, which may indicate lateral movement preparation or COM object hijacking.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: registry_set
    product: windows
detection:
    selection_keys:
        TargetObject|contains:
            - 'HKLM\SOFTWARE\Classes\AppID\'
            - 'HKLM\SOFTWARE\Microsoft\Ole\'
    selection_values:
        TargetObject|endswith:
            - 'EnableDCOM'
            - 'LocalService'
            - 'LaunchPermission'
            - 'AccessPermission'
    condition: selection_keys or selection_values
fields:
    - Image
    - TargetObject
    - EventType
    - User
falsepositives:
    - Legitimate administrative software updates or configuration changes via MMC or GPO.
level: medium
tags:
    - attack.lateral-movement
    - attack.t1021.003
    - analytic.an0791
    - detection.det0285
```

### OCSF Normalized

Rule 1 of 2: DCOM Server Spawning Suspicious Shell (Process Activity)
```xml
title: Lateral Movement - DCOM Server Spawning Suspicious Shell (OCSF-Normalized)
id: 69555c7e-4226-4d1f-9082-4530593ed5c1
status: experimental
description: Detects DCOM servers executing command interpreters, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_parent:
        actor.process.file.path|endswith:
            - '\mmc.exe'
            - '\explorer.exe'
            - '\dllhost.exe'
    selection_child:
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
    selection_dcom:
        actor.process.cmd_line|contains:
            - ' /factory'
            - '-embedding'
    filter_installer:
        unmapped.parent_image|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: selection_parent and selection_child and selection_dcom and not filter_installer
fields:
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Authorized administrative systems triggering local COM triggers.
level: high
tags:
    - attack.lateral-movement
    - attack.t1021.003
```

Rule 2 of 2: DCOM Configuration Modification (Registry Activity)
```xml
title: DCOM Configuration Modification (OCSF-Normalized) - Rule 2 of 2
id: 1a1e8121-f66e-4921-81a0-a245ed3aa91a
status: experimental
description: Detects registry modifications targeting DCOM system configurations or AppID registrations, mapped to OCSF registry_activity.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: system
    product: ocsf
    class: registry_activity
detection:
    selection_keys:
        registry.key|contains:
            - 'HKLM\SOFTWARE\Classes\AppID\'
            - 'HKLM\SOFTWARE\Microsoft\Ole\'
    selection_values:
        registry.key|endswith:
            - 'EnableDCOM'
            - 'LocalService'
            - 'LaunchPermission'
            - 'AccessPermission'
    condition: selection_keys or selection_values
fields:
    - registry.key
    - process.file.path
    - actor.user.name
falsepositives:
    - Authorized administrative configuration updates.
level: medium
tags:
    - attack.lateral-movement
    - attack.t1021.003
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Explorer.exe running in user context (Zebra spawning)
  * The Issue: Users naturally open command prompts from inside Explorer (explorer.exe) by double-clicking batch files or typing cmd in the folder address bar. This matches the parent-child rule and generates massive false alarms.
  * The Fix: Differentiate by looking for the /factory or -embedding flags in the parent command line, which denote COM initialization rather than standard desktop user-shell launch.

* Problem 2: Use of non-standard COM classes
  * The Issue: Attackers can use custom-registered COM applications that are not standard (e.g. custom business application wrappers) to spawn shells, bypassing checks for mmc.exe or explorer.exe.
  * The Fix: Monitor the registry entries for new COM class registrations (HKLM\Software\Classes\CLSID\) that define executable paths.

* Problem 3: Grandparent Process resolution
  * The Issue: Standard process logs only report the direct parent image. If the intermediate process is ephemeral and terminates quickly, identifying the grandparent process (svchost.exe) requires event correlation.
  * The Fix: Correlate process creation events (Sysmon Event ID 1) utilizing the parent process GUID to trace execution chains back to the root initiator.
