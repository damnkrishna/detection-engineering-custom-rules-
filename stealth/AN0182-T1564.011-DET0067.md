### 1. Technique Breakdown: T1564.011

* What it is: Ignore Process Interrupts. Adversaries configure scripts or processes to suppress error handling, ignore interrupt signals, and continue executing silently regardless of failures. On Windows, this is most commonly observed in PowerShell scripts using `-ErrorAction SilentlyContinue` or `$ErrorActionPreference = 'SilentlyContinue'`, which prevents error messages from appearing in console output or event logs. This technique allows adversaries to run reconnaissance or exploitation commands silently even when some steps fail, reducing detection likelihood from error-based monitoring.
* Log Source Requirements: PowerShell Script Block Logs (Event ID 4104), and Sysmon process creation logs (Event ID 1).
* Coverage Check:
    * HIDS (Sysmon + Wazuh): YES. PowerShell Script Block Logging (Event ID 4104) captures the content of PowerShell scripts including error suppression parameters. Sysmon Event ID 1 captures PowerShell and Cmd launches with error action suppression or redirection parameters.
    * NIDS (Suricata + Zeek): NO. Error suppression is a scripting-level construct.
* Log Sources Covered:
    - Sysmon Event ID 1 (process_creation)
    - PowerShell Event ID 4104 (ps_script)
* Log Sources Missing / Unused:
    - None.

---

### 2. Custom Sigma Rule

The detection logic is split into three rules targeting PowerShell script block text, PowerShell process execution command-line flags, and Command Prompt (cmd.exe) error stream redirection parameters separately.

Rule 1 of 3: PowerShell Script Block Error Suppression (Script Block)
```yaml
title: PowerShell Silent Error Suppression for Evasion (AN0182-A)
id: 9a3f90f1-ab32-4acf-8fbf-4d9f22336a17
status: experimental
description: Detects PowerShell execution with error action preference set to SilentlyContinue or Ignore in script block logs, used by adversaries to suppress error output and execute commands without alerting users or detection tools.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: ps_script
    product: windows
    # Relies on PowerShell Script Block Logging (Event ID 4104)
detection:
    selection_silence:
        ScriptBlockText|contains:
            - '-ErrorAction SilentlyContinue'
            - '-EA SilentlyContinue'
            - '$ErrorActionPreference = ''SilentlyContinue'''
            - '$ErrorActionPreference = "SilentlyContinue"'
            - '-ErrorAction Ignore'
            - '$ErrorActionPreference = ''Ignore'''
            - 'SilentlyContinue'
    selection_combined_with_execution:
        ScriptBlockText|contains:
            - 'Invoke-Expression'
            - 'IEX'
            - 'DownloadString'
            - 'WebClient'
            - 'Start-Process'
            - 'Invoke-Command'
    condition: selection_silence and selection_combined_with_execution
fields:
    - ScriptBlockText
falsepositives:
    - Legitimate administrative automation scripts that use SilentlyContinue to gracefully handle expected failures.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1564.011
    - analytic.an0182
    - detection.det0067
```

Rule 2 of 3: PowerShell CommandLine Error Suppression (Process Creation)
```yaml
title: PowerShell Error Suppression Command-Line Option (AN0182-B)
id: d86fdf17-ec32-4e2b-9ca9-adfc0157cf89
status: experimental
description: Detects powershell.exe execution where the command-line parameters specify silencing or ignoring error actions, which is common in adversarial scripting trying to avoid user alerts.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_powershell:
        - Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
        - OriginalFileName:
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_flags:
        CommandLine|contains:
            - '-ErrorAction SilentlyContinue'
            - '-EA SilentlyContinue'
            - '-ErrorAction Ignore'
            - '-EA Ignore'
    condition: selection_powershell and selection_flags
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - User
falsepositives:
    - Administrative automation tasks running PowerShell commands with error ignoring options.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1564.011
    - analytic.an0182
    - detection.det0067
```

Rule 3 of 3: Cmd Error Stream Suppression (Process Creation)
```yaml
title: Cmd Error Stream Suppression (AN0182-C)
id: e94fdf27-dd43-4eab-89cd-bd3f5a7fa84e
status: experimental
description: Detects command prompt (cmd.exe) processes redirecting stderr to null (2>nul or >nul 2>&1) combined with network download or scripting commands, which indicates attempts to execute tools silently.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_cmd:
        - Image|endswith: '\cmd.exe'
        - OriginalFileName: 'Cmd.Exe'
    selection_redirection:
        CommandLine|contains:
            - '2>nul'
            - '>nul 2>&1'
            - '2>&1 >nul'
    selection_execution:
        CommandLine|contains:
            - 'powershell'
            - 'pwsh'
            - 'curl'
            - 'wget'
            - 'certutil'
            - 'bitsadmin'
            - 'reg '
            - 'sc '
            - 'wscript'
            - 'cscript'
            - 'mshta'
            - 'rundll32'
    condition: selection_cmd and selection_redirection and selection_execution
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - Automated system backup or installation scripts using standard error output redirection.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1564.011
    - analytic.an0182
    - detection.det0067
```

### OCSF Normalized

Rule 1 of 3: PowerShell Script Block Error Suppression (Script Block)
```xml
title: PowerShell Silent Error Suppression for Evasion (OCSF-Normalized) - Rule 1 of 3
id: 9a3f90f1-ab32-4acf-8fbf-4d9f22336a17
status: experimental
description: Detects PowerShell execution with error action preference set to SilentlyContinue in script block logs, mapped to OCSF process_activity (representing PowerShell code execution).
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_silence:
        process.cmd_line|contains:
            - '-ErrorAction SilentlyContinue'
            - '-EA SilentlyContinue'
            - 'SilentlyContinue'
            - '-ErrorAction Ignore'
    selection_combined_with_execution:
        process.cmd_line|contains:
            - 'Invoke-Expression'
            - 'IEX'
            - 'DownloadString'
            - 'WebClient'
            - 'Start-Process'
            - 'Invoke-Command'
    condition: selection_silence and selection_combined_with_execution
fields:
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Legitimate administration scripts.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1564.011
```

Rule 2 of 3: PowerShell CommandLine Error Suppression (Process Activity)
```xml
title: PowerShell Error Suppression Command-Line Option (OCSF-Normalized) - Rule 2 of 3
id: d86fdf17-ec32-4e2b-9ca9-adfc0157cf89
status: experimental
description: Detects powershell.exe execution where the command-line parameters specify silencing or ignoring error actions, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_powershell:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
    selection_flags:
        process.cmd_line|contains:
            - '-ErrorAction SilentlyContinue'
            - '-EA SilentlyContinue'
            - '-ErrorAction Ignore'
            - '-EA Ignore'
    condition: selection_powershell and selection_flags
fields:
    - process.file.path
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Legitimate administration task runs.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1564.011
```

Rule 3 of 3: Cmd Error Stream Suppression (Process Activity)
```xml
title: Cmd Error Stream Suppression (OCSF-Normalized) - Rule 3 of 3
id: e94fdf27-dd43-4eab-89cd-bd3f5a7fa84e
status: experimental
description: Detects command prompt (cmd.exe) processes redirecting stderr to null, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_cmd:
        process.file.path|endswith: '\cmd.exe'
    selection_redirection:
        process.cmd_line|contains:
            - '2>nul'
            - '>nul 2>&1'
            - '2>&1 >nul'
    selection_execution:
        process.cmd_line|contains:
            - 'powershell'
            - 'pwsh'
            - 'curl'
            - 'wget'
            - 'certutil'
            - 'bitsadmin'
            - 'reg '
            - 'sc '
            - 'wscript'
            - 'cscript'
            - 'mshta'
            - 'rundll32'
    condition: selection_cmd and selection_redirection and selection_execution
fields:
    - process.file.path
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Administrative update routines or backup tasks.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1564.011
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Ubiquity of SilentlyContinue in Legitimate Scripts
  * The Issue: -ErrorAction SilentlyContinue is an extremely common PowerShell idiom in legitimate scripts (configuration management, CI/CD pipelines, software deployment tools). Alerting on it in isolation produces massive noise.
  * The Fix: Require co-occurrence with execution primitives (IEX, WebClient.DownloadString, Invoke-Command) as this combination specifically indicates an attempt to silently execute remote or encoded content. The rule already implements this correlation — tune further by adding parent process context.

* Problem 2: Encoded Command Bypassing Script Block Logging
  * The Issue: Adversaries may base64-encode their entire payload (using powershell.exe -EncodedCommand) and only set $ErrorActionPreference within the encoded payload. The Script Block log captures the decoded script content, but only if Script Block Logging is enabled.
  * The Fix: Enforce PowerShell Script Block Logging and Transcription Logging via Group Policy. Complement with Sysmon Event ID 1 detection for -EncodedCommand or -enc combined with suspicious parent processes.

* Problem 3: Non-PowerShell Error Suppression (cmd.exe, Python)
  * The Issue: Error suppression also occurs in cmd.exe scripts (using 2>nul or >nul 2>&1) and Python/Bash scripts (in WSL). These don't produce PowerShell Script Block events.
  * The Fix: Add a companion rule detecting cmd.exe with command lines containing 2>nul or >nul 2>&1 combined with network commands (certutil, curl, bitsadmin) or other suspicious utilities.
