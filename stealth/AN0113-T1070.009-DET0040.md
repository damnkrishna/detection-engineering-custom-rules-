### 1. Technique Breakdown: T1070.009

* What it is: Clear Persistence. Adversaries remove artifacts they previously planted for persistence — such as scheduled tasks, registry run keys, services, or startup folder binaries — to cover their tracks and hinder forensic investigation after completing their objectives. On Windows, removal commands include `sc.exe delete <service>`, `schtasks.exe /delete /tn <task>`, `reg.exe delete <key>`, or PowerShell equivalents (`Remove-Item`, `Unregister-ScheduledTask`). The detection pattern focuses on deletion of known persistence mechanisms rather than creation.
* Log Source Requirements: Host process creation and command line logs (Sysmon Event ID 1 / Windows Security Event ID 4688), Sysmon registry deletion logs (Event ID 12/13), and Windows Security Event ID 4699 (scheduled task deleted).
* Coverage Check:
    * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 captures `sc.exe delete`, `schtasks.exe /delete`, and `reg.exe delete` commands. Event ID 12/13 captures registry value/key deletions. Windows Security Event ID 4699 logs scheduled task deletion events.
    * NIDS (Suricata + Zeek): NO. Persistence artifact removal is a host-local operation.
* Log Sources Covered:
    - Sysmon Event ID 1 (process_creation)
    - Sysmon Event ID 12/13 (registry_set)
    - Windows Security Event ID 4699 (service: security)
* Log Sources Missing / Unused:
    - None.

---

### 2. Custom Sigma Rule

The detection logic is split into three rules to target process execution deletions, registry value deletions, and Windows Security Task deletion events separately.

Rule 1 of 3: Persistence Artifact Removal Commands (Process Creation)
```yaml
title: Persistence Artifact Removal for Anti-Forensics (AN0113-A)
id: caaa5221-d01d-464d-84f0-059649a56d14
status: experimental
description: Detects adversary activity removing persistence artifacts such as scheduled tasks, services, or registry run keys using sc delete, schtasks /delete, or reg delete — indicative of anti-forensic cleanup.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_sc_delete:
        - Image|endswith: '\sc.exe'
        - OriginalFileName: 'sc.exe'
    selection_sc_args:
        CommandLine|contains: ' delete '
    selection_schtasks_delete:
        - Image|endswith: '\schtasks.exe'
        - OriginalFileName: 'schtasks.exe'
    selection_schtasks_args:
        CommandLine|contains: ' /delete '
    selection_reg_delete:
        - Image|endswith: '\reg.exe'
        - OriginalFileName: 'reg.exe'
    selection_reg_args:
        CommandLine|contains:
            - ' delete '
            - '\Run'
            - '\RunOnce'
            - '\Services'
    selection_powershell:
        - Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
        - OriginalFileName:
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_powershell_args:
        CommandLine|contains:
            - 'Unregister-ScheduledTask'
            - 'Remove-Item.*Run'
            - 'sc.exe delete'
    filter_installer:
        ParentImage|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: ((selection_sc_delete and selection_sc_args) or (selection_schtasks_delete and selection_schtasks_args) or (selection_reg_delete and selection_reg_args) or (selection_powershell and selection_powershell_args)) and not filter_installer
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Software uninstallers that clean up scheduled tasks or services as part of legitimate removal operations.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1070.009
    - analytic.an0113
    - detection.det0040
```

Rule 2 of 3: Registry Persistence Key Deletion (Registry Set)
```yaml
title: Registry Persistence Key Deletion (AN0113-B)
id: d83fdf14-ec32-4d2b-9ca9-adfc0157cf86
status: experimental
description: Detects deletion of registry values under standard persistence paths (Run, RunOnce, Services) which indicates cleanup of malicious persistence mechanisms.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: registry_set
    product: windows
    # Relies on Sysmon Event ID 12/13
detection:
    selection_path:
        TargetObject|contains:
            - '\SOFTWARE\Microsoft\Windows\CurrentVersion\Run\'
            - '\SOFTWARE\Microsoft\Windows\CurrentVersion\RunOnce\'
            - '\System\CurrentControlSet\Services\'
    selection_delete:
        EventType: 'DeleteValue'
    filter_system:
        Image|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: selection_path and selection_delete and not filter_system
fields:
    - Image
    - TargetObject
    - EventType
    - User
falsepositives:
    - Legitimate application uninstallers cleaning up their startup registry keys or services.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1070.009
    - analytic.an0113
    - detection.det0040
```

Rule 3 of 3: Scheduled Task Deletion via Security Logs (Security Logs)
```yaml
title: Scheduled Task Deletion via Security Logs (AN0113-C)
id: e93fdf25-dd43-4eab-89cd-bd3f5a7fa84d
status: experimental
description: Detects the deletion of scheduled tasks by monitoring Windows Security Event ID 4699, which is common during post-exploit cleanup activities.
author: Krishna Gupta
date: 2026-06-08
logsource:
    service: security
    product: windows
    # Relies on Windows Security Event ID 4699
detection:
    selection_eid:
        EventID: 4699
    filter_system:
        SubjectUserName|endswith: '$'
    condition: selection_eid and not filter_system
fields:
    - EventID
    - TaskName
    - SubjectUserName
    - SubjectUserSid
falsepositives:
    - Legitimate software uninstallation deleting its scheduled tasks.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1070.009
    - analytic.an0113
    - detection.det0040
```

### OCSF Normalized

Rule 1 of 3: Persistence Artifact Removal Commands (Process Activity)
```xml
title: Persistence Artifact Removal for Anti-Forensics (OCSF-Normalized) - Rule 1 of 3
id: caaa5221-d01d-464d-84f0-059649a56d14
status: experimental
description: Detects deletion of persistence artifacts (tasks, services, registry keys) via sc, schtasks, or reg commands, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_sc:
        process.file.path|endswith: '\sc.exe'
    selection_sc_args:
        process.cmd_line|contains: ' delete '
    selection_schtasks:
        process.file.path|endswith: '\schtasks.exe'
    selection_schtasks_args:
        process.cmd_line|contains: ' /delete '
    selection_reg:
        process.file.path|endswith: '\reg.exe'
    selection_reg_action:
        process.cmd_line|contains: ' delete '
    selection_reg_targets:
        process.cmd_line|contains:
            - '\Run'
            - '\RunOnce'
            - '\Services'
    selection_powershell:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
    selection_powershell_action:
        process.cmd_line|contains:
            - 'Unregister-ScheduledTask'
            - 'Remove-Item'
    selection_powershell_targets:
        process.cmd_line|contains:
            - '\Run'
            - '\RunOnce'
    filter_installer:
        unmapped.parent_image|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: >
        ((selection_sc and selection_sc_args) or 
        (selection_schtasks and selection_schtasks_args) or 
        (selection_reg and selection_reg_action and selection_reg_targets) or 
        (selection_powershell and selection_powershell_action and selection_powershell_targets)) and not filter_installer
fields:
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Legitimate software uninstallers cleaning up services and tasks.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1070.009
```

Rule 2 of 3: Registry Persistence Key Deletion (Registry Activity)
```xml
title: Registry Persistence Key Deletion (OCSF-Normalized) - Rule 2 of 3
id: d83fdf14-ec32-4d2b-9ca9-adfc0157cf86
status: experimental
description: Detects registry value deletions under persistence paths, mapped to OCSF registry_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: registry_activity
detection:
    selection_path:
        registry.key|contains:
            - '\SOFTWARE\Microsoft\Windows\CurrentVersion\Run\'
            - '\SOFTWARE\Microsoft\Windows\CurrentVersion\RunOnce\'
            - '\System\CurrentControlSet\Services\'
    selection_delete:
        unmapped.registry_event_type: 'DeleteValue'
    filter_system:
        process.file.path|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: selection_path and selection_delete and not filter_system
fields:
    - registry.key
    - registry.value.name
    - process.file.path
    - actor.user.name
falsepositives:
    - Software uninstallers cleaning up startup items.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1070.009
```

Rule 3 of 3: Scheduled Task Deletion via Security Logs (System Activity)
```xml
title: Scheduled Task Deletion via Security Logs (OCSF-Normalized) - Rule 3 of 3
id: e93fdf25-dd43-4eab-89cd-bd3f5a7fa84d
status: experimental
description: Detects scheduled task deletion events in Windows Security log, mapped to OCSF system_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: system_activity
detection:
    selection_eid:
        unmapped.event_id: 4699
    filter_system:
        actor.user.name|endswith: '$'
    condition: selection_eid and not filter_system
fields:
    - unmapped.event_id
    - unmapped.task_name
    - actor.user.name
falsepositives:
    - Standard administrative tasks removal.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1070.009
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: High Volume from Software Uninstallers
  * The Issue: Nearly every software uninstaller deletes its scheduled maintenance tasks and services as part of normal removal. schtasks /delete and sc delete are extremely common, making blanket monitoring very noisy.
  * The Fix: Correlate deletion events with the preceding creation events. Flag cases where a persistence artifact (service, task, or run key) was created within the same session or within a short lookback window (e.g. 24 hours) and is then deleted — this sequence is more characteristic of malware cleanup than legitimate uninstallation.

* Problem 2: Registry Persistence Removal via Direct API
  * The Issue: Sophisticated adversaries may remove registry run keys directly using Windows Registry API calls (RegDeleteKey, RegDeleteValue) from a compiled binary, bypassing reg.exe entirely.
  * The Fix: Enable Sysmon Registry Monitoring (Event IDs 12, 13, 14) to detect direct registry key/value deletions under HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Run and related persistence paths, regardless of the process that performed them.

* Problem 3: Fileless Persistence Removal (WMI Subscriptions)
  * The Issue: Adversaries using WMI event subscriptions for persistence remove them using WMI queries (Get-WMIObject -Query "SELECT..." | Remove-WmiObject), which may not involve sc.exe or reg.exe.
  * The Fix: Monitor WMI Activity Operational Event ID 5861 for deletion of WMI event filters and consumers, and correlate with Sysmon Event ID 19/20/21 (WMI subscription events).
