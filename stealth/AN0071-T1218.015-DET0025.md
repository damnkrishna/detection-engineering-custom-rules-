### 1. Technique Breakdown: T1218.015

* What it is: Electron Applications. Adversaries abuse trusted Electron-based desktop applications (such as Microsoft Teams, Slack, VS Code, Discord, or Chrome) as proxy execution vehicles for malicious payloads. Because these apps are signed by trusted vendors and widely installed, security tools often allow them. Attackers exploit Electron's Node.js runtime via command-line flags like `--gpu-launcher`, `--renderer-startup-dialog`, or `--inspect` to spawn arbitrary child processes or achieve code execution within the app's sandbox. Alternatively, modifying `.asar` archive files (the bundled app resource format) allows persistent injection into the Electron app itself.
* Log Source Requirements: Sysmon process creation logs (Event ID 1) capturing Electron app parent processes spawning unusual children, image load events (Event ID 7) for node.dll/electron.dll loads, and file write events (Event ID 11) for .asar modifications.
* Coverage Check:
    * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 captures the Electron parent process with command-line flags, and child processes spawned. Event ID 7 captures DLLs loaded, and Event ID 11 logs file writes.
    * NIDS (Suricata + Zeek): NO. Electron app abuse is host-local. The proxy execution produces no network anomalies at first.
* Log Sources Covered:
    - Sysmon Event ID 1 (process_creation)
    - Sysmon Event ID 7 (image_load)
    - Sysmon Event ID 11 (file_event)
* Log Sources Missing / Unused:
    - None.

---

### 2. Custom Sigma Rule

The detection logic is split into three rules to target process creation, DLL library loads, and resource archive modifications separately.

Rule 1 of 3: Electron Application Abuse (Process Creation)
```yaml
title: Electron Application Abuse for Proxy Execution (AN0071-A)
id: 3abb5f5e-0f17-410b-831e-15db7fa84d26
status: experimental
description: Detects abuse of trusted Electron applications (Teams, Slack, VS Code, Discord) via malicious command-line flags (--gpu-launcher, --inspect) or spawning of unexpected child shell processes.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_electron_flags:
        Image|contains:
            - '\Microsoft\Teams\current\Teams.exe'
            - '\Slack\slack.exe'
            - '\Microsoft VS Code\Code.exe'
            - '\Discord\Discord.exe'
        CommandLine|contains:
            - '--gpu-launcher'
            - '--renderer-startup-dialog'
            - '--inspect='
            - '--inspect-brk='
            - '--no-sandbox'
    selection_electron_child:
        ParentImage|contains:
            - '\Microsoft\Teams\current\Teams.exe'
            - '\Slack\slack.exe'
            - '\Microsoft VS Code\Code.exe'
            - '\Discord\Discord.exe'
        Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\rundll32.exe'
    filter_installer:
        ParentImage|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: (selection_electron_flags or selection_electron_child) and not filter_installer
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Developer workflows using VS Code with --inspect for debugging Node.js apps, or Electron app updates that spawn installer child processes.
level: high
tags:
    - attack.defense-evasion
    - attack.t1218.015
    - analytic.an0071
    - detection.det0025
```

Rule 2 of 3: Suspicious Electron Library Load (Image Load)
```yaml
title: Suspicious Electron Library Load (AN0071-B)
id: c71fdf12-5b92-4110-8d25-983dfa7fa84d
status: experimental
description: Detects the loading of node.dll or electron.dll by unexpected processes or from non-standard directories, which may indicate payload execution proxying.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: image_load
    product: windows
    # Relies on Sysmon Event ID 7
detection:
    selection_dll:
        ImageLoaded|endswith:
            - '\node.dll'
            - '\electron.dll'
    filter_legitimate:
        Image|endswith:
            - '\chrome.exe'
            - '\msedge.exe'
            - '\slack.exe'
            - '\Teams.exe'
            - '\Code.exe'
            - '\discord.exe'
            - '\explorer.exe'
    condition: selection_dll and not filter_legitimate
fields:
    - Image
    - ImageLoaded
    - Signed
    - SignatureStatus
falsepositives:
    - Other legitimate node/electron-based apps installed in the environment that are not in the filter list.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1218.015
    - analytic.an0071
    - detection.det0025
```

Rule 3 of 3: Electron ASAR File Modification (File Event)
```yaml
title: Electron ASAR File Modification (AN0071-C)
id: d82fdf23-6c8a-4bbf-92d6-05dfa8e97a3f
status: experimental
description: Detects modifications or writes to Electron application resource archives (.asar files) by non-application updater processes, indicating potential backdoor persistence.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: file_event
    product: windows
    # Relies on Sysmon Event ID 11
detection:
    selection_path:
        TargetFilename|contains:
            - '\Microsoft\Teams\'
            - '\Slack\'
            - '\Microsoft VS Code\'
            - '\Discord\'
    selection_file:
        TargetFilename|endswith: '\.asar'
    filter_updater:
        Image|endswith:
            - '\Teams.exe'
            - '\slack.exe'
            - '\Code.exe'
            - '\Discord.exe'
            - '\Update.exe'
            - '\Squirrel.exe'
    condition: selection_path and selection_file and not filter_updater
fields:
    - Image
    - TargetFilename
    - User
falsepositives:
    - Manual application updates or administrative modifications by system management tools.
level: high
tags:
    - attack.defense-evasion
    - attack.t1218.015
    - analytic.an0071
    - detection.det0025
```

### OCSF Normalized

Rule 1 of 3: Electron Application Abuse (Process Activity)
```xml
title: Electron Application Abuse for Proxy Execution (OCSF-Normalized) - Rule 1 of 3
id: 3abb5f5e-0f17-410b-831e-15db7fa84d26
status: experimental
description: Detects Electron apps launched with abuse flags or spawning shell child processes, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_electron_flags:
        process.file.path|contains:
            - '\Microsoft\Teams\current\Teams.exe'
            - '\Slack\slack.exe'
            - '\Microsoft VS Code\Code.exe'
            - '\Discord\Discord.exe'
        process.cmd_line|contains:
            - '--gpu-launcher'
            - '--inspect='
            - '--inspect-brk='
            - '--no-sandbox'
    selection_electron_child:
        unmapped.parent_image|contains:
            - '\Microsoft\Teams\current\Teams.exe'
            - '\Slack\slack.exe'
            - '\Microsoft VS Code\Code.exe'
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\mshta.exe'
            - '\rundll32.exe'
    filter_installer:
        unmapped.parent_image|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: (selection_electron_flags or selection_electron_child) and not filter_installer
fields:
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Developer VS Code debugging with --inspect, or Electron app updates spawning installer processes.
level: high
tags:
    - attack.defense-evasion
    - attack.t1218.015
    - analytic.an0071
    - detection.det0025
```

Rule 2 of 3: Suspicious Electron Library Load (Library Activity)
```xml
title: Suspicious Electron Library Load (OCSF-Normalized) - Rule 2 of 3
id: c71fdf12-5b92-4110-8d25-983dfa7fa84d
status: experimental
description: Detects the loading of node.dll or electron.dll by unexpected processes, mapped to OCSF library_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: library_activity
detection:
        selection_dll:
            library.file.path|endswith:
                - '\node.dll'
                - '\electron.dll'
        filter_legitimate:
            process.file.path|endswith:
                - '\chrome.exe'
                - '\msedge.exe'
                - '\slack.exe'
                - '\Teams.exe'
                - '\Code.exe'
                - '\discord.exe'
                - '\explorer.exe'
        condition: selection_dll and not filter_legitimate
fields:
    - process.file.path
    - library.file.path
    - library.file.signature.status
falsepositives:
    - Legitimate third-party applications bundling Node.js/Electron dlls.
level: medium
tags:
    - attack.defense-evasion
    - attack.t1218.015
    - analytic.an0071
    - detection.det0025
```

Rule 3 of 3: Electron ASAR File Modification (File Activity)
```xml
title: Electron ASAR File Modification (OCSF-Normalized) - Rule 3 of 3
id: d82fdf23-6c8a-4bbf-92d6-05dfa8e97a3f
status: experimental
description: Detects writes/modifications to .asar files by non-updater processes, mapped to OCSF file_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: file_activity
detection:
    selection_path:
        file.path|contains:
            - '\Microsoft\Teams\'
            - '\Slack\'
            - '\Microsoft VS Code\'
            - '\Discord\'
    selection_file:
        file.path|endswith: '\.asar'
    filter_updater:
        process.file.path|endswith:
            - '\Teams.exe'
            - '\slack.exe'
            - '\Code.exe'
            - '\Discord.exe'
            - '\Update.exe'
            - '\Squirrel.exe'
    condition: selection_path and selection_file and not filter_updater
fields:
    - file.path
    - process.file.path
    - actor.user.name
    - unmapped.file_operation
falsepositives:
    - Manual application updates or administrative modifications by system management tools.
level: high
tags:
    - attack.defense-evasion
    - attack.t1218.015
    - analytic.an0071
    - detection.det0025
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: VS Code Developer Debugging Noise
  * The Issue: VS Code legitimately uses --inspect flags for Node.js debugging, and developers spawn terminal shells (cmd.exe, powershell.exe) from VS Code's integrated terminal constantly. Blanket alerting generates overwhelming developer noise.
  * The Fix: Scope alerts to non-developer workstations or specific departments (e.g. servers, executive laptops). For VS Code, restrict alerts to --inspect combined with remote IP addresses rather than localhost, or focus on child processes with suspicious arguments rather than plain shell spawning.

* Problem 2: .asar File Modification Detection
  * The Issue: If an adversary modifies the .asar resource archive of an Electron app to embed a backdoor, the app itself runs normally without any unusual command-line flags or child processes. No process creation anomaly is triggered on launch.
  * The Fix: Monitor file write events (Sysmon Event ID 11) targeting .asar files in installed application directories. Alert on any modification of .asar files by processes other than the application's own updater.

* Problem 3: Custom or Unknown Electron Applications
  * The Issue: This rule targets specific well-known Electron apps (Teams, Slack, VS Code). Adversaries may bundle their own Electron-based malware disguised as legitimate apps, or exploit less-common Electron apps not in the allowlist.
  * The Fix: Extend detection to any executable that loads electron.exe or bundles node.exe alongside Squirrel.exe (Electron's auto-updater) by monitoring image load events for node.dll or electron.dll in non-standard paths.
