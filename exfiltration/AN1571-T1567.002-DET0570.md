### 1. Technique Breakdown: T1567.002

* What it is: Exfiltration Over Web Service: Exfiltration to Cloud Storage. Adversaries exfiltrate data by uploading it directly to public cloud storage providers (such as Mega, Dropbox, OneDrive, Azure Blob, or AWS S3) using command-line synchronization tools (like `rclone.exe`, `megacmd.exe`) or administrative CLI tools (`aws.exe`, `az.exe`). Because cloud storage domains are frequently accessed for legitimate business, these connections are rarely blocked at the perimeter. Host-based process creation and command line parameters are critical to spot these unauthorized tools.
* Log Source Requirements: Sysmon process creation logs (Event ID 1) capturing cloud sync clients and CLI commands.
* Coverage Check:
    * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 records the execution of cloud clients (like `rclone.exe`) along with sync/copy arguments. Wazuh parses and alerts.
    * NIDS (Suricata + Zeek): YES. Network logs track large data transfers to cloud service provider domains, but host logs are needed to verify the specific application context.

---

### 2. Custom Sigma Rule

```yaml
title: Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571)
id: ecdfcd2a-36d2-446d-9fdd-3432e813de47
status: experimental
description: Detects the execution of command-line cloud synchronization tools (like rclone, megacmd) or cloud provider CLI platforms (AWS, Azure) transferring files, indicating potential exfiltration.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_utilities_image:
        Image|endswith:
            - '\rclone.exe'
            - '\mega-cmd.exe'
            - '\mega.exe'
            - '\MegaSync.exe'
            - '\aws.exe'
            - '\az.exe'
            - '\gsutil.exe'
    selection_utilities_original:
        OriginalFileName:
            - 'rclone.exe'
            - 'mega-cmd.exe'
            - 'mega.exe'
            - 'MegaSync.exe'
            - 'aws.exe'
            - 'az.exe'
            - 'gsutil.py'
    selection_sync_commands:
        CommandLine|contains:
            - ' sync '
            - ' copy '
            - ' upload '
            - ' s3 cp '
            - ' blob upload '
    condition: (selection_utilities_image or selection_utilities_original) and selection_sync_commands
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - IT administrators using rclone or cloud CLIs to manage system backups or sync server repositories to enterprise cloud storage.
level: high
tags:
    - attack.exfiltration
    - attack.t1567.002
    - analytic.an1571
    - detection.det0570
```

---

### 3. OCSF Normalized Rule

```yaml
title: 'Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571) (OCSF-Normalized)'
id: 'ecdfcd2a-36d2-446d-9fdd-3432e813de47'
status: experimental
description: 'OCSF Normalized rule for Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571)'
author: Krishna Gupta
logsource:
    product: ocsf
    class: process_activity
detection:
    selection_utilities_image:
        process.file.path|endswith:
            - '\rclone.exe'
            - '\mega-cmd.exe'
            - '\mega.exe'
            - '\MegaSync.exe'
            - '\aws.exe'
            - '\az.exe'
            - '\gsutil.exe'
    selection_utilities_original:
        process.file.name:
            - 'rclone.exe'
            - 'mega-cmd.exe'
            - 'mega.exe'
            - 'MegaSync.exe'
            - 'aws.exe'
            - 'az.exe'
            - 'gsutil.py'
    selection_sync_commands:
        process.cmd_line|contains:
            - ' sync '
            - ' copy '
            - ' upload '
            - ' s3 cp '
            - ' blob upload '
    condition: (selection_utilities_image or selection_utilities_original) and selection_sync_commands
fields:
    - time
level: high
tags:
    - attack.exfiltration
    - attack.t1567.002
    - analytic.an1571
    - detection.det0570
```

---

### 4. Blind Spots & Tuning (The "Problems")

- Authorized IT administration activities.
- Tune with allowlists for known trusted execution paths.

---

### 5. Test Cases (Engine Pipeline Verification)

#### True Positives
* True Positive 1A: Standard Execution (Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571))
  * The Log: Activity simulating malicious behavior.
  * Expected Outcome: MUST trigger an alert.

* True Positive 1B: Malformed JSON Robustness Test (Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571))
  * The Log: Simulating 1A but intentionally omitting the `unmapped` object entirely.
  * Expected Outcome: Engine parser should degrade gracefully. MUST trigger an alert.

* True Positive 1C: Schema Type Validation Failure (Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571))
  * The Log: Exact match of 1A, but `activity_id` is passed as a string `"1"` instead of an integer `1`.
  * Expected Outcome: Tests if the engine's JSON parser coerces the type gracefully. MUST trigger an alert.

* True Positive 1D: Exact Duplicate Event (Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571))
  * The Log: Exact duplicate of 1A with the exact same timestamp.
  * Expected Outcome: Tests deduplication window.

#### Benign / True Negatives
* Benign 1: Allowed Activity (Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571))
  * The Log: Activity matching legitimate patterns.
  * Expected Outcome: Matches exclusion filters. MUST NOT trigger an alert.

---

### 6. Raw Telemetry Dataset (OCSF JSON)

#### Benign Telemetry
```jsonl
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Windows\\System32\\cmd.exe", "name": "cmd.exe"}, "cmd_line": "cmd.exe"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"generic": "benign_test"}, "metadata": {"test_case": "TN-01", "description": "Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571) (TN-01)", "expected_alert": false, "rule_uid": "ecdfcd2a-36d2-446d-9fdd-3432e813de47"}, "activity_id": 1, "time": 1718000010}
```

#### True Positive Telemetry
```jsonl
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\rclone.exe", "name": "rclone.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c  sync "}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"dummy": "data"}, "metadata": {"test_case": "TP-01A", "description": "Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571) (TP-01A)", "expected_alert": true, "rule_uid": "ecdfcd2a-36d2-446d-9fdd-3432e813de47"}, "activity_id": 1, "time": 1718000100}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\rclone.exe", "name": "rclone.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c  sync "}, "actor": {"user": {"name": "TargetUser"}}, "metadata": {"test_case": "TP-01B", "description": "Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571) (TP-01B)", "expected_alert": true, "rule_uid": "ecdfcd2a-36d2-446d-9fdd-3432e813de47"}, "activity_id": 1, "time": 1718000101}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\rclone.exe", "name": "rclone.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c  sync "}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"dummy": "data"}, "metadata": {"test_case": "TP-01C", "description": "Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571) (TP-01C)", "expected_alert": true, "rule_uid": "ecdfcd2a-36d2-446d-9fdd-3432e813de47"}, "activity_id": "1", "time": 1718000102}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\rclone.exe", "name": "rclone.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c  sync "}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"dummy": "data"}, "metadata": {"test_case": "TP-01D", "description": "Exfiltration - Command-Line Cloud Storage Sync Execution (AN1571) (TP-01D)", "expected_alert": true, "rule_uid": "ecdfcd2a-36d2-446d-9fdd-3432e813de47"}, "activity_id": 1, "time": 1718000100}
```
