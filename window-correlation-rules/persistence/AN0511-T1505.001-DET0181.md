# AN0511 - Server Software Component: SQL Stored Procedures (T1505.001)

## Technique Breakdown
Adversaries may abuse SQL Server stored procedures (like `xp_cmdshell` or `sp_OACreate`) to establish persistence or execute arbitrary OS commands. By modifying or creating malicious stored procedures, attackers maintain access even if OS-level backdoors are removed. Detection involves monitoring `sqlservr.exe` for child process creations or command-line executions containing these specific stored procedure invocations.

## Custom Sigma Rule
```yaml
title: 'AN0511 - Server Software Component: SQL Stored Procedures'
id: 'AN0511'
status: experimental
description: 'Detects process execution originating from SQL Server involving dangerous stored procedures (xp_cmdshell, sp_OACreate), typically used for code execution or persistence.'
author: Krishna Gupta
logsource:
  product: windows
  category: process_creation
detection:
  selection_parent:
    ParentImage|endswith: '\sqlservr.exe'
  selection_cmd:
    CommandLine|contains:
      - 'xp_cmdshell'
      - 'sp_OACreate'
      - 'sp_OAMethod'
  condition: selection_parent and selection_cmd
fields:
  - EventID
  - Image
  - CommandLine
  - ParentImage
level: critical
tags:
  - attack.persistence
  - attack.execution
  - attack.t1505.001
  - analytic.an0511
```

### OCSF Normalized Rule
```yaml
title: 'AN0511 - Server Software Component: SQL Stored Procedures (OCSF)'
id: 'AN0511'
status: experimental
description: 'Detects process execution originating from SQL Server involving dangerous stored procedures (xp_cmdshell, sp_OACreate), typically used for code execution or persistence.'
author: Krishna Gupta
logsource:
  product: windows
  category: process_creation
detection:
  selection_signature:
    signature_name|contains:
      - 'SQL Stored Procedures'
      - 'DET0181'
  condition: selection_signature
fields:
  - event_id
  - signature_name
  - unmapped.parent_image
  - unmapped.parent_cmd_line
  - actor.user.name
  - unmapped.granted_access
level: critical
tags:
  - attack.persistence
  - attack.execution
  - attack.t1505.001
  - analytic.an0511
```

## Blind Spots & Tuning
- Legacy applications or specific database administrative scripts that legitimately require `xp_cmdshell` for OS-level integration.
- Tuning is required to baseline normal `xp_cmdshell` usage vs unexpected commands like encoded PowerShell or unknown binary execution.
