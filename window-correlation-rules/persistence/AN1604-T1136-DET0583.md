# AN1604 - Create Account (T1136)

## Technique Breakdown
Adversaries may generically create accounts (local, domain, or cloud) to ensure redundant access to a network. In a Windows environment, this is most broadly detected by observing command-line tools like `net.exe` or `net1.exe` being used with the `/add` parameter, irrespective of whether it's local or domain targeted.

## Custom Sigma Rule
```yaml
title: 'AN1604 - Generic Create Account'
id: 'AN1604'
status: experimental
description: 'Detects the usage of net.exe to add a new user account, which may indicate an adversary establishing persistence.'
author: Krishna Gupta
logsource:
  product: windows
  category: process_creation
detection:
  selection_image:
    Image|endswith:
      - '\net.exe'
      - '\net1.exe'
  selection_cmd:
    CommandLine|contains|all:
      - ' user '
      - ' /add'
  condition: selection_image and selection_cmd
fields:
  - EventID
  - Image
  - CommandLine
  - ParentImage
level: high
tags:
  - attack.persistence
  - attack.t1136
  - analytic.an1604
```

### OCSF Normalized Rule
```yaml
title: 'AN1604 - Generic Create Account (OCSF)'
id: 'AN1604'
status: experimental
description: 'Detects the usage of net.exe to add a new user account, which may indicate an adversary establishing persistence.'
author: Krishna Gupta
logsource:
  product: windows
  category: process_creation
detection:
  selection_signature:
    signature_name|contains:
      - 'Create Account'
      - 'DET0583'
  condition: selection_signature
fields:
  - event_id
  - signature_name
  - unmapped.parent_image
  - unmapped.parent_cmd_line
  - actor.user.name
  - unmapped.granted_access
level: high
tags:
  - attack.persistence
  - attack.t1136
  - analytic.an1604
```

## Blind Spots & Tuning
- IT Helpdesk personnel manually running `net user /add` during troubleshooting or endpoint setup.
- Scripts (like endpoint provisioning routines) that use `net user`. Tune out known script paths or administrative users.
