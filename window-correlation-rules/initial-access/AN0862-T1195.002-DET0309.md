### 1. Technique Breakdown: T1195.002

* What it is: Supply Chain Compromise: Compromise Software Supply Chain. Adversaries compromise legitimate software installers or update mechanisms (e.g., utility software updates, driver installers) to deliver malware. When the compromised installer runs, it executes post-installation scripts or helper binaries that spawn command shells or scripting tools to download and install implants, often executing out of temporary directories.
* Log Source Requirements: Sysmon process creation logs (Event ID 1) and Sysmon file creation logs (Event ID 11).
* Coverage Check:
  * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 tracks system installers (such as `msiexec.exe`) or update processes (e.g., `update.exe`, `setup.exe`) spawning child commands. Sysmon Event ID 11 captures file writes by installers to system or startup folders.
  * NIDS (Suricata + Zeek): PARTIAL. Network tools can flag update downloads originating from compromised hosts or non-SSL HTTP channels, but cannot see what processes the updater launches on the system.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 11 (file_event)
* Log Sources Missing / Unused:
  - None.
* Conclusion: Software supply chain installer compromise can be detected using process creation and file creation logs.

---

### 2. Custom Sigma Rule

The detection logic is split into monitoring processes spawned by software installers and monitoring installers writing files to sensitive folders.

Rule 1 of 2: Software Supply Chain - Installer Spawning Suspicious Child Process (Process Creation)
```yaml
title: Software Supply Chain - Installer Spawning Suspicious Child Process (AN0862-A)
id: 7ec5a329-4f56-419b-9462-c473d17a812a
status: experimental
description: Detects software installer or updater processes (like msiexec, setup, install, update) spawning command-line interpreters or networking tools, which could indicate a compromised software supply chain.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
detection:
    selection_parent:
        ParentImage|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
            - '\install.exe'
            - '\update.exe'
            - '\updater.exe'
    selection_child:
        - Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\curl.exe'
            - '\certutil.exe'
            - '\bitsadmin.exe'
        - OriginalFileName:
            - 'Cmd.Exe'
            - 'PowerShell.EXE'
            - 'wscript.exe'
            - 'cscript.exe'
            - 'mshta.exe'
            - 'curl.exe'
            - 'certutil.exe'
            - 'bitsadmin.exe'
    condition: selection_parent and selection_child
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Legitimate software installers running configuration shell commands, register DLL actions, or post-install cleanup batch files.
level: medium
tags:
    - attack.initial-access
    - attack.t1195.002
    - analytic.an0862
    - detection.det0309
```

Rule 2 of 2: Software Supply Chain - Installer Writing Suspicious File (File Event)
```yaml
title: Software Supply Chain - Installer Writing Suspicious File (AN0862-B)
id: a39db5a7-d1df-40fd-bedd-01a27a176f4d
status: experimental
description: Detects software installers or updater processes writing scripts, shortcuts, or executables into system folders or startup paths.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: file_event
    product: windows
detection:
    selection_parent:
        Image|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
            - '\install.exe'
            - '\update.exe'
            - '\updater.exe'
    selection_paths:
        TargetFilename|contains:
            - '\Start Menu\Programs\Startup\'
            - '\Windows\System32\'
            - '\Windows\SysWOW64\'
    selection_extensions:
        TargetFilename|endswith:
            - '.bat'
            - '.ps1'
            - '.lnk'
            - '.vbs'
            - '.exe'
    condition: selection_parent and selection_paths and selection_extensions
fields:
    - Image
    - TargetFilename
    - User
falsepositives:
    - Legitimate installer writing binaries or start shortcuts during authorized application setup.
level: medium
tags:
    - attack.initial-access
    - attack.t1195.002
    - analytic.an0862
    - detection.det0309
```

### OCSF Normalized

Rule 1 of 2: Software Supply Chain - Installer Spawning Suspicious Child Process (Process Activity)
```xml
title: Software Supply Chain - Installer Spawning Suspicious Child Process (OCSF-Normalized) - Rule 1 of 2
id: 7ec5a329-4f56-419b-9462-c473d17a812a
status: experimental
description: Detects installer or updater processes spawning command interpreters, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_parent:
        actor.process.file.path|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
            - '\install.exe'
            - '\update.exe'
    selection_child:
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\curl.exe'
    condition: selection_parent and selection_child
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Legitimate software setup wrappers executing command tasks.
level: medium
tags:
    - attack.initial-access
    - attack.t1195.002
```

Rule 2 of 2: Software Supply Chain - Installer Writing Suspicious File (File Activity)
```xml
title: Software Supply Chain - Installer Writing Suspicious File (OCSF-Normalized) - Rule 2 of 2
id: a39db5a7-d1df-40fd-bedd-01a27a176f4d
status: experimental
description: Detects software installers or updater processes writing scripts, shortcuts, or executables into system folders or startup paths, mapped to OCSF file_activity.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: system
    product: ocsf
    class: file_activity
detection:
    selection_parent:
        actor.process.file.path|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
            - '\install.exe'
            - '\update.exe'
    selection_paths:
        file.path|contains:
            - '\Start Menu\Programs\Startup\'
            - '\Windows\System32\'
    selection_extensions:
        file.path|endswith:
            - '.bat'
            - '.ps1'
            - '.lnk'
            - '.exe'
    condition: selection_parent and selection_paths and selection_extensions
fields:
    - actor.process.file.path
    - file.path
    - actor.user.name
falsepositives:
    - Legitimate software setup wrappers copying components to target system folders.
level: medium
tags:
    - attack.initial-access
    - attack.t1195.002
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Complex Legitimate Application Installers (Noise)
  * The Issue: Large software suites (e.g. Adobe, Microsoft Office, SQL Server) run highly complex multi-stage install scripts that spawn cmd prompts to configure services, create directories, and add registry values.
  * The Fix: Implement specific exclusions for known, signed installer binaries of trusted enterprise packages, or build path exclusions for official installation directories.

* Problem 2: Execution in user Temp folder context
  * The Issue: Updaters often unpack themselves to `C:\Users\<user>\AppData\Local\Temp\` and run auxiliary setup files. If these sub-installers are unsigned or named randomly, simple binary name checks won't work.
  * The Fix: Add a core check that flags any unsigned process executing from Temp paths that attempts network connections or modifies boot settings.

* Problem 3: Direct API Execution without Shell Spawning
  * The Issue: If the compromised installer downloads malware using direct Winsock or WinINet APIs inside the installer process itself, no child process will be created, making parent-child rules ineffective.
  * The Fix: Correlate installer network activity (Sysmon Event ID 3) targeting non-vendor domains with driver configuration writes (Event ID 11).

