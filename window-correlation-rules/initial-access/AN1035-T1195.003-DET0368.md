### **1. Technique Breakdown: T1195.003**

* **What it is:** **Supply Chain Compromise: Compromise Hardware Supply Chain**. Adversaries insert malicious physical modifications or subverted firmware (such as tampered UEFI/BIOS, customized network cards, or compromised baseboard management controllers) during manufacturing or distribution. Detecting this post-delivery requires monitoring host BIOS flashing utilities, TPM validation errors, or Secure Boot policy changes.
* **Log Source Requirements:** Windows System Event logs (Kernel-Boot and TPM events) or Sysmon process creation logs (Event ID 1) auditing firmware tools.
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 tracks BIOS flashing tool executions. Windows System log tracks TPM driver alerts (Event ID 2, 15) and Secure Boot configuration modifications (Event ID 81, 153).
    * **NIDS (Suricata + Zeek): NO.** Network monitoring has no visibility into hardware firmware modifications or boot configuration data.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** and **Windows System Log (Kernel-Boot)** to detect execution of firmware flashing tools or boot integrity configuration modifications.

```yaml
title: Hardware Supply Chain - Firmware Tampering and Boot Integrity Modification (AN1035)
id: 726cb1ab-0135-4b44-8dcb-5bd5254fb757
status: experimental
description: Detects the execution of known firmware/BIOS flashing tools or Windows Kernel-Boot events indicating changes to Secure Boot policies or TPM validation failures.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: windows
detection:
    selection_process:
        Image|endswith:
            - '\flashrom.exe'
            - '\fwupd.exe'
            - '\afuwingu.exe' # AMI Firmware Update Utility
            - '\HpbiosUpdate.exe'
            - '\DellBiosProvider.exe'
            - '\WinFlash.exe'
    selection_events:
        EventID:
            - 81  # Secure Boot configuration change
            - 153 # Secure Boot policy modification
            - 2   # TPM driver initialization failure
        Provider_Name: 'Microsoft-Windows-Kernel-Boot'
    condition: selection_process or selection_events
fields:
    - EventID
    - Image
    - Provider_Name
falsepositives:
    - Authorized IT firmware upgrades or hardware maintenance tasks by technicians.
level: high
tags:
    - attack.initial-access
    - attack.t1195.003
    - analytics.an1035
    - detection.det0368
```

### OCSF Normalized
```xml
title: Hardware Supply Chain - Firmware Tampering and Boot Integrity Modification (OCSF-Normalized)
id: 726cb1ab-0135-4b44-8dcb-5bd5254fb757
status: experimental
description: Detects firmware flashing tools or boot configuration modification events, mapped to OCSF system_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: system_activity
detection:
    # Based on OCSF mapping, event IDs or BIOS tool execution are mapped to system_activity
    selection_events:
        activity_name:
            - 'Secure Boot Change'
            - 'TPM Error'
            - 'Firmware Update'
        win_resource.name|contains:
            - 'flashrom'
            - 'Kernel-Boot'
            - 'TPM'
    condition: selection_events
fields:
    - activity_name
    - win_resource.name
falsepositives:
    - Authorized system patches or BIOS modifications.
level: high
tags:
    - attack.initial-access
    - attack.t1195.003
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Routine OEM updates (Lenovo/HP/Dell automated updates)**
    * *The Issue:* Vendor support software (like Lenovo Vantage or Dell SupportAssist) routinely checks and applies BIOS/firmware updates silently in the background, triggering alerts.
    * *The Fix:* Whitelist flashing utility executions when spawned by legitimate, signed vendor service wrappers (checking signature status and vendor parent path).

* **Problem 2: Device driver loading of flash utilities**
    * *The Issue:* Flash utilities often load kernel-mode drivers dynamically (e.g. `winring0.sys`) to talk to BIOS hardware. If the flashing tool executable itself is disguised, the direct process name check fails.
    * *The Fix:* Monitor the registry write events for new driver registrations (`HKLM\System\CurrentControlSet\Services`) corresponding to known low-level access drivers (like WinRing0).

* **Problem 3: Off-line Firmware Flashing (ChIP programmer)**
    * *The Issue:* If the adversary compromises the hardware supply chain by physically removing the BIOS chip and flashing it using a hardware programmer (like a CH341A) before shipment, no OS-level events will record the write.
    * *The Fix:* Rely on remote attestation services (such as Windows Defender System Guard and TPM-based measurements) to validate the system boot sequence against a known good PCR baseline on boot.
