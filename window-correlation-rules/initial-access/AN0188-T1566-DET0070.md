### 1. Technique Breakdown: T1566

* What it is: Phishing. Adversaries send malicious attachments or links through email (e.g., via Outlook) or messaging tools to gain initial access to target environments. When the user opens the attachment, it spawns script interpreters (like PowerShell, cmd, or mshta) or launching programs to run external code.
* Log Source Requirements: Sysmon process creation logs (Event ID 1) capturing parent-child process execution patterns, and Sysmon file creation logs (Event ID 11).
* Coverage Check:
  * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 captures email clients spawning scripting tools or office productivity suites. Sysmon Event ID 11 captures files written by mail clients.
  * NIDS (Suricata + Zeek): YES. Network logs can flag SMTP/IMAP attachments with suspicious extensions or connections to known phishing domains.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 11 (file_event)
* Log Sources Missing / Unused:
  - None.
* Conclusion: Phishing activity can be detected using process creation and file creation logs.

---

### 2. Custom Sigma Rule

The detection logic is split into monitoring process spawns from mail clients and monitoring mail clients writing suspicious file types.

Rule 1 of 2: Phishing - Suspicious Child Process Spawned by Mail Client (Process Creation)
```yaml
title: Phishing - Suspicious Child Process Spawned by Mail Client (AN0188-A)
id: 485fdf8f-9d6c-4b43-aeb2-a0083bb94ad6
status: experimental
description: Detects suspicious child processes spawned by Microsoft Outlook or other common email clients, which indicates potential user execution of a malicious phishing attachment.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
detection:
    selection_parent:
        ParentImage|endswith:
            - '\outlook.exe'
            - '\thunderbird.exe'
            - '\foxmail.exe'
    selection_child:
        - Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\rundll32.exe'
            - '\regsvr32.exe'
            - '\hh.exe'
            - '\bitsadmin.exe'
            - '\curl.exe'
        - OriginalFileName:
            - 'Cmd.Exe'
            - 'PowerShell.EXE'
            - 'wscript.exe'
            - 'cscript.exe'
            - 'mshta.exe'
            - 'rundll32.exe'
            - 'regsvr32.exe'
            - 'bitsadmin.exe'
            - 'curl.exe'
    condition: selection_parent and selection_child
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Office extensions or administrative tools executing command scripts for mail configuration or updates.
level: high
tags:
    - attack.initial-access
    - attack.t1566
    - analytic.an0188
    - detection.det0070
```

Rule 2 of 2: Phishing - Suspicious File Attachment Written by Mail Client (File Event)
```yaml
title: Phishing - Suspicious File Attachment Written by Mail Client (AN0188-B)
id: aca5ffc0-565e-4e02-a9ce-0b8794671e68
status: experimental
description: Detects email client software writing executable, script, or container file formats to disk, which suggests a user downloading a potentially malicious phishing attachment.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: file_event
    product: windows
detection:
    selection_parent:
        Image|endswith:
            - '\outlook.exe'
            - '\thunderbird.exe'
            - '\foxmail.exe'
    selection_file:
        TargetFilename|endswith:
            - '.exe'
            - '.lnk'
            - '.vbs'
            - '.hta'
            - '.iso'
            - '.img'
            - '.bat'
            - '.ps1'
            - '.scr'
            - '.zip'
    condition: selection_parent and selection_file
fields:
    - Image
    - TargetFilename
    - User
falsepositives:
    - Legitimate user downloading file attachments required for business purposes.
level: medium
tags:
    - attack.initial-access
    - attack.t1566
    - analytic.an0188
    - detection.det0070
```

### OCSF Normalized

Rule 1 of 2: Phishing - Suspicious Child Process Spawned by Mail Client (Process Activity)
```xml
title: Phishing - Suspicious Child Process Spawned by Mail Client (OCSF-Normalized) - Rule 1 of 2
id: 485fdf8f-9d6c-4b43-aeb2-a0083bb94ad6
status: experimental
description: Detects suspicious child processes spawned by email clients, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_parent:
        actor.process.file.path|endswith:
            - '\outlook.exe'
            - '\thunderbird.exe'
            - '\foxmail.exe'
    selection_child:
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\rundll32.exe'
    condition: selection_parent and selection_child
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Mail setup utilities or developer tooling integration.
level: high
tags:
    - attack.initial-access
    - attack.t1566
```

Rule 2 of 2: Phishing - Suspicious File Attachment Written by Mail Client (File Activity)
```xml
title: Phishing - Suspicious File Attachment Written by Mail Client (OCSF-Normalized) - Rule 2 of 2
id: aca5ffc0-565e-4e02-a9ce-0b8794671e68
status: experimental
description: Detects email client software writing executable, script, or container file formats to disk, mapped to OCSF file_activity.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: system
    product: ocsf
    class: file_activity
detection:
    selection_parent:
        actor.process.file.path|endswith:
            - '\outlook.exe'
            - '\thunderbird.exe'
            - '\foxmail.exe'
    selection_file:
        file.path|endswith:
            - '.exe'
            - '.lnk'
            - '.vbs'
            - '.hta'
            - '.iso'
            - '.img'
            - '.bat'
            - '.ps1'
            - '.scr'
            - '.zip'
    condition: selection_parent and selection_file
fields:
    - actor.process.file.path
    - file.path
    - actor.user.name
falsepositives:
    - User downloading business-related zip files or installers via mail client.
level: medium
tags:
    - attack.initial-access
    - attack.t1566
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Phishing via Hyperlinks (Indirect Execution)
  * The Issue: If a user clicks a phishing link that triggers a browser download, the browser downloads the payload. When the user launches the payload, the parent process is `explorer.exe` or `browser.exe`, not `outlook.exe`.
  * The Fix: Correlate browser file writes to the downloads path with process executions in a tight time frame, and monitor browser child processes.

* Problem 2: Office Document Intermediate (Phishing attachments that spawn word first)
  * The Issue: Phishing attachments are often document templates (`.docx`, `.xlsm`). In this case, `outlook.exe` spawns `winword.exe`, which then spawns `powershell.exe`. The parent of the malicious shell is Word, not Outlook, bypassing a direct parent-child check.
  * The Fix: Add a rule checking for office application parent images (`winword.exe`, `excel.exe`, `powerpnt.exe`) spawning shell/scripting executables.

* Problem 3: Standard Mail Integrations / Add-Ins
  * The Issue: Legitimate mail encryption or calendar add-ins may spawn cmd scripts to perform key decryption or sync configurations, generating false positives.
  * The Fix: Create specific exclusions based on command-line argument signatures or code signature validation for known business add-ins.

