### 1. Technique Breakdown: T1495

* What it is: Firmware Corruption. Adversaries modify or corrupt system firmware (such as UEFI, BIOS, or hard drive controller firmware) to make the target system completely inoperable and brick the hardware. In Windows environments, firmware or boot sequence tampering is preceded by command-line execution of tools like bcdedit.exe to modify the Boot Configuration Data (BCD) store, disabling boot recovery menus (recoveryenabled No) or configuring the OS to ignore boot failures (bootstatuspolicy ignoreallfailures), preventing recovery.
* Log Source Requirements: Host process creation and command line logs (Sysmon Event ID 1 / Windows Security Event ID 4688) tracking execution, and PowerShell script block logs (Sysmon Event ID 4104).
* Coverage Check:
  * HIDS (Sysmon + Windows Defender): YES. Sysmon Event ID 1 captures bcdedit.exe executions with full command parameters. Sysmon Event ID 4104 (Script Block) captures scripts modifying boot loader properties.
  * Windows Security Logs: YES. Standard Security Logs track process execution (Event ID 4688) containing the command line arguments.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 4104 (ps_script)
* Log Sources Missing / Unused:
  - None.
* Conclusion: Boot configuration tampering can be detected using process creation and PowerShell script block logs.

---

### 2. Custom Sigma Rule

The detection logic is split into command-line execution monitoring and script block content monitoring.

Rule 1 of 2: Boot Configuration Tampering via CLI (Process Creation)
```yaml
title: Boot Configuration Tampering via CLI (AN0474-A)
id: faad8c3b-e2ea-49f3-ba30-8d52b2a73e47
status: experimental
description: Detects command-line modifications using bcdedit.exe to disable boot recovery options, disable safe boot configurations, or set anomalous boot configurations, which is typical of destructive ransomware and system disruption.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
detection:
    selection_bcdedit:
        - Image|endswith: '\bcdedit.exe'
        - OriginalFileName: 'bcdedit.exe'
    selection_bcdedit_cmds:
        CommandLine|contains:
            - '/set'
            - '/deletevalue'
    selection_bcdedit_targets:
        CommandLine|contains:
            - 'recoveryenabled'
            - 'bootstatuspolicy'
            - 'ignoreallfailures'
            - 'safeboot'
    filter_installer:
        ParentImage|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: selection_bcdedit and selection_bcdedit_cmds and selection_bcdedit_targets and not filter_installer
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - IT administrators configuring boot options during OS installation or system maintenance.
level: high
tags:
    - attack.impact
    - attack.t1495
    - analytic.an0474
    - detection.det0167
```

Rule 2 of 2: Boot Configuration Tampering via PowerShell Script Block (Script Block)
```yaml
title: Boot Configuration Tampering via PowerShell Script Block (AN0474-B)
id: 752e7dd6-3fcd-4a68-a2e1-90de8d261518
status: experimental
description: Detects PowerShell script block logs containing commands attempting to modify Windows Boot Configuration Data (BCD).
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: ps_script
    product: windows
detection:
    selection_keywords:
        ScriptBlockText|contains:
            - 'bcdedit'
            - 'recoveryenabled'
            - 'bootstatuspolicy'
            - 'ignoreallfailures'
    condition: selection_keywords
fields:
    - ScriptBlockText
falsepositives:
    - Legitimate administrative scripts or maintenance tools configuring boot settings.
level: high
tags:
    - attack.impact
    - attack.t1495
    - analytic.an0474
    - detection.det0167
```

### OCSF Normalized

Rule 1 of 2: Boot Configuration Tampering via CLI (Process Activity)
```xml
title: Boot Configuration Tampering via CLI (OCSF-Normalized) - Rule 1 of 2
id: faad8c3b-e2ea-49f3-ba30-8d52b2a73e47
status: experimental
description: Detects command-line modifications to Boot Configuration Data, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_bcdedit:
        process.file.path|endswith: '\bcdedit.exe'
    selection_bcdedit_cmds:
        process.cmd_line|contains:
            - '/set'
            - '/deletevalue'
    selection_bcdedit_targets:
        process.cmd_line|contains:
            - 'recoveryenabled'
            - 'bootstatuspolicy'
            - 'ignoreallfailures'
            - 'safeboot'
    filter_installer:
        unmapped.parent_image|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: selection_bcdedit and selection_bcdedit_cmds and selection_bcdedit_targets and not filter_installer
fields:
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - IT administrators configuring boot options.
level: high
tags:
    - attack.impact
    - attack.t1495
```

Rule 2 of 2: Boot Configuration Tampering via PowerShell Script Block (Process Activity)
```xml
title: Boot Configuration Tampering via PowerShell Script Block (OCSF-Normalized) - Rule 2 of 2
id: 752e7dd6-3fcd-4a68-a2e1-90de8d261518
status: experimental
description: Detects PowerShell script block logs containing commands attempting to modify Windows Boot Configuration Data, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_keywords:
        process.cmd_line|contains:
            - 'bcdedit'
            - 'recoveryenabled'
            - 'bootstatuspolicy'
            - 'ignoreallfailures'
    condition: selection_keywords
fields:
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Legitimate administrative configuration changes.
level: high
tags:
    - attack.impact
    - attack.t1495
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Direct Registry/API BCD Modifications
  * The Issue: The Boot Configuration Data store is mapped to a registry hive under HKLM\BCD. Attackers can write directly to these registry paths without launching bcdedit.exe, bypassing process creation checks.
  * The Fix: Enable registry auditing on HKLM\BCD and monitor registry write events targeting BCD registry paths.

* Problem 2: Vendor Firmware Utilities
  * The Issue: Attackers can use vendor-specific BIOS/UEFI flashing utilities to directly corrupt the firmware without altering BCD settings.
  * The Fix: Audit the installation and execution of known vendor firmware flashing utilities.

* Problem 3: Standard IT OS Deployment and Patching
  * The Issue: OS deployment scripts modify boot loader settings during upgrades, triggering alerts.
  * The Fix: Whitelist SCCM or standard corporate domain deployment service accounts.
