### **1. Technique Breakdown: T1021.005**

* **What it is:** **Remote Services: VNC**. Adversaries install Virtual Network Computing (VNC) software on target systems to establish interactive GUI remote control sessions. This allows them to interact with the target machine directly, bypass standard administrative shell logs, and easily move laterally across the network. Detection focuses on identifying VNC server processes launching from non-standard folders or running as unauthorized services.
* **Log Source Requirements:** Sysmon process creation logs (Event ID 1) and network connection logs (Event ID 3) capturing VNC client/server activity.
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 tracks VNC process execution, and Event ID 3 logs network connections targeting standard VNC ports (5900-5909). Wazuh generates alerts based on these logs.
    * **NIDS (Suricata + Zeek): YES.** Network filters identify unencrypted VNC protocols in transit and alert on port 5900 traffic.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** and **Event ID 3 (Network Connection)** to detect the execution of VNC server applications or network socket bindings on VNC-related ports (5900-5909).

```yaml
title: Lateral Movement - Unauthorized VNC Server Execution (AN0504)
id: 8604b200-ef75-45c2-b108-0b1d94245730
status: experimental
description: Detects execution of VNC server binaries (such as TightVNC, UltraVNC, RealVNC) or socket connections targeting standard VNC ports (5900-5909), suggesting unauthorized GUI-based remote access.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1/Event ID 3 collected via Wazuh
detection:
    selection_process:
        - Image|endswith:
            - '\winvnc.exe'
            - '\tightvnc.exe'
            - '\ultravnc.exe'
            - '\vncserver.exe'
        - OriginalFileName:
            - 'winvnc.exe'
            - 'tightvnc.exe'
            - 'vncserver.exe'
    selection_network:
        EventID: 3
        DestinationPort:
            - 5900
            - 5901
            - 5902
            - 5903
            - 5904
            - 5905
    condition: selection_process or selection_network
fields:
    - DestinationPort
    - EventID
    - Image
    - OriginalFileName
falsepositives:
    - Legitimate remote support or server administration using authorized VNC software packages.
level: medium
tags:
    - attack.lateral-movement
    - attack.t1021.005
    - analytics.an0504
    - detection.det0178
```

### OCSF Normalized
```xml
title: Lateral Movement - Unauthorized VNC Server Execution (OCSF-Normalized)
id: 8604b200-ef75-45c2-b108-0b1d94245730
status: experimental
description: Detects VNC server process execution or network connection events, mapped to OCSF process_activity and network_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    # Based on OCSF mapping, process path is process.file.path and destination port is activity.dst_endpoint.port
    selection_process:
        process.file.path|endswith:
            - '\winvnc.exe'
            - '\tightvnc.exe'
            - '\vncserver.exe'
    condition: selection_process
fields:
    - process.file.path
falsepositives:
    - Authorized remote management systems.
level: medium
tags:
    - attack.lateral-movement
    - attack.t1021.005
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Non-standard Port Mappings**
    * *The Issue:* Attackers can configure VNC services to listen on common business ports (like port 80, 443, or 8080) to bypass firewall filters and static port detection rules.
    * *The Fix:* Focus detection on the VNC server process name (`winvnc.exe`) and command line parameters, rather than relying solely on destination port numbers.

* **Problem 2: Execution via Portable VNC Binaries**
    * *The Issue:* Attackers can copy portable, single-file VNC binaries to local writeable directories (like Temp or Downloads) and rename them to avoid file name checks (e.g. renaming `winvnc.exe` to `taskhost.exe`).
    * *The Fix:* Enable PE file metadata monitoring (checking `OriginalFileName` header signatures) and monitor network sockets initiated by renamed processes executing from temp directories.

* **Problem 3: Standard corporate helpdesk usage**
    * *The Issue:* Desktop support teams may use VNC to manage end-user workstations, triggering a large volume of alerts.
    * *The Fix:* Restrict VNC client access to authorized administrator subnets and GPO whitelist policies, and alert on connections originating from standard user segments.
