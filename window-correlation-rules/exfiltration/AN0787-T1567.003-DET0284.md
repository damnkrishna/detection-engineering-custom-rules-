### **1. Technique Breakdown: T1567.003**

* **What it is:** **Exfiltration to Code Repository: Exfiltration to Text Storage Sites**. Adversaries exfiltrate sensitive files or credentials by uploading their content as plain text snippets to public paste bins (such as Pastebin, Hastebin, or Ghostbin) where text can be hosted anonymously. Malware scripts read the target files (e.g., configurations, database dumps) and send HTTP POST/PUT requests transmitting the data. Because paste bins are popular and use standard port 443 HTTPS connections, they successfully bypass network inspection.
* **Log Source Requirements:** Endpoint network connection logs (Sysmon Event ID 3) or DNS queries (Sysmon Event ID 22), combined with requesting process metadata.
* **Coverage Check:**
* **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 3 logs outbound connection hostnames and the initiating application path. Wazuh aggregates and evaluates these connections.
* **NIDS (Suricata + Zeek): YES.** Zeek `ssl.log` and `http.log` record SSL/TLS SNI negotiations and HTTP methods targeting paste sites.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 3 (Network Connection)** to detect command-line interpreters, scripting hosts, or file transfer tools initiating network connections to known public paste and text storage domains.

```yaml
title: Outbound Data Exfiltration to Paste Sites via CLI (AN0787)
id: aaee7d8e-1a7e-4867-932a-9462b39a6970
status: experimental
description: Detects command-line interpreters, scripting hosts, or file transfer tools initiating network connections or DNS queries to known paste sites (e.g., Pastebin, Hastebin), which may indicate data exfiltration.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: network_connection
    product: windows
    # Relies on Sysmon Event ID 3 collected via Wazuh
detection:
    selection_process:
        Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\curl.exe'
    selection_domains:
        DestinationHostname|contains:
            - 'pastebin.com'
            - 'hastebin.com'
            - 'ghostbin.co'
            - 'paste.ee'
            - 'controlc.com'
    condition: selection_process and selection_domains
fields:
    - DestinationHostname
    - Image
falsepositives:
    - Highly unlikely in standard enterprise environments. Some administration scripts may query paste sites for update configs, but this is a discouraged practice.
level: high
tags:
    - attack.exfiltration
    - attack.t1567.003
    - analytics.an0787
    - detection.det0284
```

### OCSF Normalized
```xml
title: Outbound Data Exfiltration to Paste Sites via CLI (OCSF-Normalized)
id: aaee7d8e-1a7e-4867-932a-9462b39a6970
status: experimental
description: Detects command-line interpreters or scripting hosts initiating network connections to public paste sites, mapped to OCSF network_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: network_activity
detection:
    # Based on OCSF mapping, process is under process.file.path and destination is activity.http_request.host
    selection_process:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\curl.exe'
    selection_domains:
        activity.http_request.host|contains:
            - 'pastebin.com'
            - 'hastebin.com'
            - 'ghostbin.co'
            - 'paste.ee'
            - 'controlc.com'
    condition: selection_process and selection_domains
fields:
    - activity.http_request.host
    - process.file.path
falsepositives:
    - Highly unlikely in standard enterprise environments.
level: high
tags:
    - attack.exfiltration
    - attack.t1567.003
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Use of In-House/Corporate Text Sites**
* *The Issue:* If the organization hosts its own internal paste utility (e.g. an internal GitLab paste bin or wiki page), attackers can target it for exfiltration, which avoids public paste domain matches.
* *The Fix:* Implement file access monitoring (Sysmon Event ID 11 / Event ID 1) to alert if sensitive configuration folders are read by non-standard processes prior to any internal upload.


* **Problem 2: API Request Obfuscation**
* *The Issue:* Attackers can use Base64 encoding or encryption to wrap payloads within standard administrative parameters, causing SIEM boundary inspections to miss the data copy.
* *The Fix:* Enable SSL/TLS decryption on the proxy gateway to inspect the raw content length and frequency of HTTP POST requests targeting text storage APIs.


* **Problem 3: Corporate Policy Violations**
* *The Issue:* Developers occasionally paste debugging snippets containing corporate credentials or APIs to Pastebin to share with vendor support, generating policy alerts that are not malicious exfiltration.
* *The Fix:* Standardize enterprise developer tooling, and use security awareness training to discourage posting proprietary code to public text bins.
