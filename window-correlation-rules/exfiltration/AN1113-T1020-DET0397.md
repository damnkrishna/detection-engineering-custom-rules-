### **1. Technique Breakdown: T1020**

* **What it is:** **Automated Exfiltration**. Adversaries automate the search, collection, packaging, and transmission of sensitive files using scheduled scripts or custom implants. Once deployed, these scripts execute at set intervals (using scheduled tasks or system daemons), compress targeted file formats (using LOLBins like `tar.exe` or `makecab.exe`), and transmit them to external infrastructure. Detecting automated exfiltration relies on flagging scripting utilities or scheduled tasks spawning archiving tools.
* **Log Source Requirements:** Endpoint logs capturing process creation and parent-child execution context (Sysmon Event ID 1 / Windows Security Event ID 4688).
* **Coverage Check:**
* **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 captures parent process paths, target binaries, and command-line parameters. Wazuh aggregates these activities to detect command-line archiving behavior.
* **NIDS (Suricata + Zeek): YES (Partial).** Network tools can identify periodic beaconing traffic patterns to external destinations, but host-level context is needed to identify data collection and archiving operations.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect command-line archiving tools (`7z.exe`, `rar.exe`, `tar.exe`, `makecab.exe`) being spawned by task schedulers or scripting environments.

```yaml
title: Automated Archiving and Staging via CLI (AN1113)
id: 74bde7cd-79fb-44ae-bd48-51829210d551
status: experimental
description: Detects command-line archiving utilities being spawned by command shells or task schedulers, which may indicate automated data harvesting and staging for exfiltration.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_parent:
        ParentImage|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\taskeng.exe'
            - '\svchost.exe'
    selection_archive:
        - Image|endswith:
            - '\7z.exe'
            - '\7za.exe'
            - '\rar.exe'
            - '\zip.exe'
            - '\tar.exe'
            - '\makecab.exe'
        - OriginalFileName:
            - '7z.exe'
            - '7za.exe'
            - 'rar.exe'
            - 'zip.exe'
            - 'tar.exe'
            - 'makecab.exe'
    condition: selection_parent and selection_archive
falsepositives:
    - Legitimate administrative backup scripts, database backups, or software build tools performing automated file archiving.
level: medium
tags:
    - attack.exfiltration
    - attack.t1020
    - analytics.an1113
    - detection.det0397
```

### OCSF Normalized
```xml
title: Automated Archiving and Staging via CLI (OCSF-Normalized)
id: 74bde7cd-79fb-44ae-bd48-51829210d551
status: experimental
description: Detects command-line archiving utilities being spawned by command shells or task schedulers, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    # Based on OCSF mapping, process is under process.file.path and parent is process.parent_process.file.path
    selection_parent:
        process.parent_process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\taskeng.exe'
            - '\svchost.exe'
    selection_archive:
        process.file.path|endswith:
            - '\7z.exe'
            - '\7za.exe'
            - '\rar.exe'
            - '\zip.exe'
            - '\tar.exe'
            - '\makecab.exe'
    condition: selection_parent and selection_archive
falsepositives:
    - Legitimate administrative backup scripts, database backups, or software build tools performing automated file archiving.
level: medium
tags:
    - attack.exfiltration
    - attack.t1020
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: In-Memory Compression via Native APIs**
* *The Issue:* Attackers can use PowerShell script blocks that call native .NET compression classes (e.g., `[System.IO.Compression.ZipFile]::CreateFromDirectory`) or custom DLLs, which compresses files in-memory or directly to disk without invoking third-party binaries like `7z.exe` or `tar.exe`.
* *The Fix:* Enable PowerShell Script Block Logging (Event ID 4104) and monitor scripts containing references to `Compression` namespaces.


* **Problem 2: Renaming Archiving Utilities**
* *The Issue:* Attackers can copy `7z.exe` or `rar.exe` to user directories and rename them (e.g., `log.exe`) to bypass simple file path filters.
* *The Fix:* Check the `OriginalFileName` and `Publisher` metadata fields in process execution logs, which remain unchanged despite file renaming.


* **Problem 3: Database Backups and Standard Admin Noise**
* *The Issue:* Server configurations routinely run scheduled tasks to archive databases or system logs, generating significant log noise.
* *The Fix:* Baseline standard backup tasks and whitelist specific administrator backup directories (like `D:\Backup\`), or whitelist specific database process parentage (like SQL Server agents).
