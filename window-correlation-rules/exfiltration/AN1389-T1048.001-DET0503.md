### **1. Technique Breakdown: T1048.001**

* **What it is:** **Exfiltration Over Alternative Protocol: Exfiltration Over Symmetric Encrypted Non-C2 Protocol**. Adversaries encrypt stolen files locally using symmetric cryptography (like AES-256) and upload the data via an alternative network protocol or a public file hosting API that is distinct from their primary C2 channel. Because the traffic is encrypted, corporate data loss prevention (DLP) filters cannot read the content or identify sensitive keywords. Detections monitor scripting hosts loading cryptographic DLLs (`bcryptprimitives.dll`, `cryptsp.dll`) to prepare for secure transfers.
* **Log Source Requirements:** Endpoint image load logs (Sysmon Event ID 7 / Windows Security Event ID 5056) capturing cryptographic DLL loading.
* **Coverage Check:**
* **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 7 captures DLL load events alongside the calling process name, which allows detection of command-line scripting utilities importing cryptographic functions.
* **NIDS (Suricata + Zeek): NO.** Local cryptographic processing occurs entirely within the endpoint user-space. Network logs can capture the outbound file upload, but cannot confirm if the payload was encrypted locally using native DLL APIs.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 7 (Image Loaded)** to detect command-line interpreters or scripting hosts loading Windows cryptographic DLLs, which indicates symmetric encryption preparations.

```yaml
title: Cryptographic DLL Loaded by Scripting Host (AN1389)
id: 1a59e4fb-05c9-49c1-98ca-bae5c8832b27
status: experimental
description: Detects command-line interpreters or scripting hosts loading Windows cryptographic libraries (e.g., bcryptprimitives.dll, cryptsp.dll), which may indicate symmetric encryption of data prior to exfiltration.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: image_load
    product: windows
    # Relies on Sysmon Event ID 7 collected via Wazuh
detection:
    selection_process:
        Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\cmd.exe'
    selection_dll:
        ImageLoaded|endswith:
            - '\bcrypt.dll'
            - '\bcryptprimitives.dll'
            - '\cryptsp.dll'
            - '\ncrypt.dll'
    condition: selection_process and selection_dll
falsepositives:
    - Administrative scripts executing complex hashing routines (e.g. comparing file hashes using Get-FileHash) or managing secure password fields.
level: medium
tags:
    - attack.exfiltration
    - attack.t1048.001
    - analytics.an1389
    - detection.det0503
```

### OCSF Normalized
```xml
title: Cryptographic DLL Loaded by Scripting Host (OCSF-Normalized)
id: 1a59e4fb-05c9-49c1-98ca-bae5c8832b27
status: experimental
description: Detects command-line interpreters or scripting hosts loading Windows cryptographic libraries, mapped to OCSF library_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: library_activity
detection:
    # Based on OCSF mapping, process is under process.file.path and loaded DLL is library.file.path
    selection_process:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\cmd.exe'
    selection_dll:
        library.file.path|endswith:
            - '\bcrypt.dll'
            - '\bcryptprimitives.dll'
            - '\cryptsp.dll'
            - '\ncrypt.dll'
    condition: selection_process and selection_dll
falsepositives:
    - Administrative scripts executing complex hashing routines (e.g. comparing file hashes using Get-FileHash) or managing secure password fields.
level: medium
tags:
    - attack.exfiltration
    - attack.t1048.001
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: In-Memory / Pure Scripting Cryptography Implementation**
* *The Issue:* Attackers can execute symmetric encryption routines written entirely in pure PowerShell/Python code without loading Windows APIs or DLLs (like `bcrypt.dll`), which evades DLL loading filters.
* *The Fix:* Implement PowerShell Script Block Logging (Event ID 4104) and monitor script strings containing crypto classes like `System.Security.Cryptography.RijndaelManaged` or `System.Security.Cryptography.Aes`.


* **Problem 2: Performance Overhead of Image Load Auditing**
* *The Issue:* Logging DLL loads globally consumes significant system resources and floods the SIEM with high log volumes.
* *The Fix:* Scope Sysmon configuration to only audit DLL loads for high-risk scripting processes (like `powershell.exe` or `wscript.exe`) rather than globally.


* **Problem 3: Standard Administrative File Hashing**
* *The Issue:* Legitimate system administrators regularly run commands like `Get-FileHash` to verify installer file integrity, which loads cryptographic libraries and triggers false alerts.
* *The Fix:* Whitelist approved administrative systems, filter out known IT management software, and correlate DLL loads with subsequent outbound socket connections to unusual IP addresses.
