### **1. Technique Breakdown: T1052.001**

* **What it is:** **Exfiltration Over Physical Medium: Exfiltration over USB**. Adversaries exfiltrate data by writing files directly to USB mass storage devices. To accelerate data collection and minimize interactive steps, they often execute automated command-line copy tools (like `robocopy.exe`, `xcopy.exe`) or custom scripts to scan and copy targeted files directly to plugged-in USB drives. Detections monitor file writes onto removable drive paths by these specific copy utilities.
* **Log Source Requirements:** Endpoint file creation logs (Sysmon Event ID 11 / Windows Security Event ID 4663) capturing file paths, writing processes, and usernames.
* **Coverage Check:**
* **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 11 logs file creation paths (e.g. `E:\Staged\sensitive.zip`) and the writing process (e.g. `robocopy.exe`). Wazuh parses these logs to trigger alerts.
* **NIDS (Suricata + Zeek): NO.** USB storage exfiltration occurs entirely locally and offline, generating no network traffic.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 11 (File Create)** to detect files being written to removable media drive letters by standard command-line copy tools (`robocopy.exe`, `xcopy.exe`, `cmd.exe`, or `powershell.exe`).

```yaml
title: File Copy to USB Storage via CLI (AN0616)
id: 44e824ce-8c28-4fb8-ba3b-b05838ce6d7d
status: experimental
description: Detects command line execution of file copy utilities (xcopy, robocopy) or scripting engines targeting USB/removable storage drive letters, which may indicate automated data harvesting to USB media.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: file_event
    product: windows
    # Relies on Sysmon Event ID 11 collected via Wazuh
detection:
    selection_process:
        Image|endswith:
            - '\xcopy.exe'
            - '\robocopy.exe'
            - '\powershell.exe'
            - '\cmd.exe'
    selection_paths:
        TargetFilename|startswith:
            - 'D:\'
            - 'E:\'
            - 'F:\'
            - 'G:\'
            - 'H:\'
            - 'I:\'
            - 'J:\'
    condition: selection_process and selection_paths
fields:
    - Image
    - TargetFilename
falsepositives:
    - Legitimate IT administration or users manually backing up files using command line copy tools to external media.
level: medium
tags:
    - attack.exfiltration
    - attack.t1052.001
    - analytics.an0616
    - detection.det0220
```

### OCSF Normalized
```xml
title: File Copy to USB Storage via CLI (OCSF-Normalized)
id: 44e824ce-8c28-4fb8-ba3b-b05838ce6d7d
status: experimental
description: Detects files copied or written to USB mass storage directories by command line tools, mapped to OCSF file_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: file_activity
detection:
    # Based on OCSF mapping, target file path is file.path and creating process is process.file.path
    selection_process:
        process.file.path|endswith:
            - '\xcopy.exe'
            - '\robocopy.exe'
            - '\powershell.exe'
            - '\cmd.exe'
    selection_paths:
        file.path|startswith:
            - 'D:\'
            - 'E:\'
            - 'F:\'
            - 'G:\'
            - 'H:\'
            - 'I:\'
            - 'J:\'
    condition: selection_process and selection_paths
fields:
    - file.path
    - process.file.path
falsepositives:
    - Legitimate IT administration or users manually backing up files using command line copy tools to external media.
level: medium
tags:
    - attack.exfiltration
    - attack.t1052.001
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Internal Partition Drive Letter Matches**
* *The Issue:* Hardcoded drive letter lists (D:, E:, etc.) can trigger false alerts if endpoints contain multiple local partitions, DVD/CD-ROM drives, or permanent network mounts.
* *The Fix:* Complement this rule with device arrival events (Event ID 7045 or Windows Driver Framework logs) to verify that a USB storage driver was recently loaded, or check Event ID 4663 device type metadata.


* **Problem 2: Explorer GUI Drag-and-Drop**
* *The Issue:* If the attacker uses the standard Windows Explorer graphical user interface (GUI) to drag and drop files onto the USB drive, the writing process is `explorer.exe` rather than command line tools, bypassing command-utility filters.
* *The Fix:* Implement file copy auditing on sensitive files (Event ID 4663) and alert on high-volume file reads and subsequent writes by `explorer.exe` targeting removable storage devices.


* **Problem 3: Administrative Staging Scripts**
* *The Issue:* IT support scripts might run copy tasks during endpoint migration or hardware refresh sessions.
* *The Fix:* Restrict this alert to non-administrative users, and whitelist approved IT migration tool paths.
