### 1. Technique Breakdown: T1685.001

* What it is: Disable or Modify Windows Event Log. Adversaries impair Windows event logging to hide their actions, prevent forensic analysis, and bypass security alerts. They do this by clearing log channels using native tools like `wevtutil.exe`, disabling specific event channels in the registry, modifying system audit policies via `auditpol.exe` to stop recording high-risk activities, or forcibly stopping/disabling the EventLog service.
* Log Source Requirements: Endpoint logs capturing process creation and command line parameters (specifically target administrative tools used to stop services, adjust audit policies, or configure log channels).
* Coverage Check:
* HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 (Process Creation) logs the execution of utilities like `wevtutil.exe`, `auditpol.exe`, `sc.exe`, or PowerShell commands targeting event services.
* NIDS (Suricata + Zeek): NO. Impairing local system logging is an endpoint-internal operation. It generates no network signatures.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 13 (registry_set)
* Log Sources Missing / Unused:
  - None.

---

### 2. Custom Sigma Rule

The detection logic is split into process creation monitoring and registry modification monitoring of system event log services.

Rule 1 of 2: Disabling or Tampering with Windows Event Logging (Process Creation)
```yaml
title: Disabling or Tampering with Windows Event Logging (AN0535)
id: f38a1b2c-4a3b-4c2d-8e9f-5b6d7e8a9f0b
status: experimental
description: Detects command-line operations that attempt to disable, clear, or modify Windows Event Log services, logging channels, or system audit policies.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_wevtutil:
        - Image|endswith: '\wevtutil.exe'
        - OriginalFileName: 'wevtutil.exe'
    selection_wevtutil_args:
        CommandLine|contains:
            - ' cl '      # Clear log
            - ' clear-log '
            - '/e:false'  # Disable log
            - '/enabled:false'
    selection_auditpol:
        - Image|endswith: '\auditpol.exe'
        - OriginalFileName: 'Auditpol.exe'
    selection_auditpol_args:
        CommandLine|contains:
            - '/clear'
            - '/remove'
            - '/success:disable'
            - '/failure:disable'
    selection_service_control:
        - Image|endswith:
            - '\sc.exe'
            - '\net.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
        - OriginalFileName:
            - 'sc.exe'
            - 'net.exe'
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_service_args:
        CommandLine|contains|all:
            - 'eventlog'
        CommandLine|contains:
            - 'stop'
            - 'disable'
    condition: (selection_wevtutil and selection_wevtutil_args) or (selection_auditpol and selection_auditpol_args) or (selection_service_control and selection_service_args)
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Legitimate administrative maintenance, diagnostic troubleshooting, or script-based log rotation configured by enterprise engineers.
level: high
tags:
    - attack.defense-evasion
    - attack.t1685.001
    - analytic.an0535
    - detection.det0187
```

Rule 2 of 2: Windows Event Logging Service Registry Modification (Registry Set)
```yaml
title: Windows Event Logging Service Registry Modification (AN0535-B)
id: a08f1960-b533-4a04-bf9b-c64acce0d7a8
status: experimental
description: Detects registry modifications under Windows EventLog configurations, indicating attempts to disable or redirect logging.
author: Krishna Gupta
date: 2026-06-17
logsource:
    category: registry_set
    product: windows
detection:
    selection:
        TargetObject|contains:
            - '\System\CurrentControlSet\Services\EventLog\'
    condition: selection
fields:
    - TargetObject
    - Details
    - Image
    - User
falsepositives:
    - Administrative configuration of custom event log paths or settings.
level: high
tags:
    - attack.defense-evasion
    - attack.t1685.001
    - analytic.an0535
    - detection.det0187
```

### OCSF Normalized

Rule 1 of 2: Disabling or Tampering with Windows Event Logging (Process Activity)
```xml
title: Disabling or Tampering with Windows Event Logging (AN0535) (OCSF-Normalized)
id: f38a1b2c-4a3b-4c2d-8e9f-5b6d7e8a9f0b
status: experimental
description: Detects command-line operations that attempt to disable, clear, or modify Windows Event Log services, logging channels, or system audit policies.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_wevtutil:
        - process.file.path|endswith: '\wevtutil.exe'
        - process.file.path: 'wevtutil.exe'
    selection_wevtutil_args:
        process.cmd_line|contains:
            - ' cl '      # Clear log
            - ' clear-log '
            - '/e:false'  # Disable log
            - '/enabled:false'
    selection_auditpol:
        - process.file.path|endswith: '\auditpol.exe'
        - process.file.path: 'Auditpol.exe'
    selection_auditpol_args:
        process.cmd_line|contains:
            - '/clear'
            - '/remove'
            - '/success:disable'
            - '/failure:disable'
    selection_service_control:
        - process.file.path|endswith:
            - '\sc.exe'
            - '\net.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
        - process.file.path:
            - 'sc.exe'
            - 'net.exe'
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_service_args:
        process.cmd_line|contains|all:
            - 'eventlog'
        process.cmd_line|contains:
            - 'stop'
            - 'disable'
    condition: (selection_wevtutil and selection_wevtutil_args) or (selection_auditpol and selection_auditpol_args) or (selection_service_control and selection_service_args)
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Standard administrative tasks or baselined activity.
level: high
tags:
    - attack.defense-evasion
    - attack.t1685.001
```

Rule 2 of 2: Windows Event Logging Service Registry Modification (Registry Activity)
```xml
title: Windows Event Logging Service Registry Modification (OCSF-Normalized) - Rule 2 of 2
id: a08f1960-b533-4a04-bf9b-c64acce0d7a8
status: experimental
description: Detects registry modifications under Windows EventLog configuration keys, mapped to OCSF registry_activity.
author: Krishna Gupta
date: 2026-06-17
logsource:
    category: system
    product: ocsf
    class: registry_activity
detection:
    selection:
        registry.key|contains:
            - '\System\CurrentControlSet\Services\EventLog\'
    condition: selection
fields:
    - registry.key
    - process.file.path
    - actor.user.name
falsepositives:
    - Standard event log size or pathway tuning.
level: high
tags:
    - attack.defense-evasion
    - attack.t1685.001
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Command Line Obfuscation
  * The Issue: Adversaries can obfuscate command-line arguments using environment variables, caret escapes (^), or base64 encoding, evading simple string matching.
  * The Fix: Implement PowerShell Script Block Logging and utilize regex or script-decoding scripts in the SIEM to check arguments.

* Problem 2: Administrative Activity Noise
  * The Issue: IT administrators legitimately run command-line tools or installers that trigger these rules during system maintenance.
  * The Fix: Establish a baseline of legitimate admin tools, whitelisting specific administrative account names or management servers.

* Problem 3: Masquerading via Renamed Binaries
  * The Issue: Attackers rename native Windows utilities (such as net.exe or cmd.exe) to custom filenames to bypass path-based filters.
  * The Fix: Incorporate Masquerade-resistant criteria, matching on OriginalFileName rather than the static Image path in process creation logs.
