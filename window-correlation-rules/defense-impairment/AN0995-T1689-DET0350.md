### **1. Technique Breakdown: T1689**

* **What it is:** **Downgrade Attack**. Adversaries downgrade or force the use of legacy versions of applications or system features (like PowerShell v2) that lack modern security monitoring, logging (like Script Block Logging), or protections (like AMSI).
* **Log Source Requirements:** Process creation logs (Sysmon Event ID 1 / Windows Security Event ID 4688) and PowerShell operational logs (Event ID 400).
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 captures command-line arguments specifying older versions (e.g., `powershell.exe -version 2`).
    * **NIDS (Suricata + Zeek): NO.** Executing downgraded tools is a host-level activity.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect the execution of PowerShell with flags explicitly requesting an older version, typically Version 2, which bypasses modern logging.

```yaml
title: Downgrade Attack - PowerShell v2 Execution (AN0995)
id: fbbbb76c-3a8c-4a1e-8488-8120ec2c5d14
status: experimental
description: Detects the execution of PowerShell explicitly requesting version 2 or legacy binaries, bypassing modern security logging.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_ps:
        Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
    selection_args:
        CommandLine|contains|all:
            - '-version'
            - '2'
    condition: selection_ps and selection_args
fields:
    - CommandLine
    - Image
falsepositives:
    - Legacy enterprise scripts that explicitly require PowerShell v2 for compatibility.
level: high
tags:
    - attack.defense_evasion
    - attack.t1689
    - analytics.an0995
    - detection.det0350
```

### OCSF Normalized
```xml
title: Downgrade Attack - PowerShell v2 Execution (OCSF-Normalized)
id: fbbbb76c-3a8c-4a1e-8488-8120ec2c5d14
status: experimental
description: Detects the execution of PowerShell explicitly requesting version 2, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    # Based on OCSF mapping, process path is process.file.path and cmd line is process.cmd_line
    selection_ps:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
    selection_args:
        process.cmd_line|contains|all:
            - '-version'
            - '2'
    condition: selection_ps and selection_args
fields:
    - process.cmd_line
    - process.file.path
falsepositives:
    - Legacy enterprise scripts that explicitly require PowerShell v2 for compatibility.
level: high
tags:
    - attack.defense_evasion
    - attack.t1689
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Alternate Command Line Syntaxes**
    * *The Issue:* The `-Version` flag can be abbreviated (e.g., `-v 2` or `-ver 2`).
    * *The Fix:* Expand the Sigma `CommandLine` strings to include common abbreviations or use regex matching.

* **Problem 2: Hardcoded Legacy Binaries**
    * *The Issue:* Attackers might copy the actual older binary (e.g., `powershell.exe` from an old Windows 7 ISO) instead of using the version flag.
    * *The Fix:* Monitor the `FileVersion` or `OriginalFileName` fields in Sysmon Event ID 1, and alert on deprecated version numbers of PowerShell running on modern OS versions.

* **Problem 3: Valid Legacy Dependencies**
    * *The Issue:* Some legacy applications explicitly invoke PowerShell v2.
    * *The Fix:* Baseline legacy environments and whitelist specific parent processes or script paths known to require v2.
