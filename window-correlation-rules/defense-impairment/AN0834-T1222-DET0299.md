### 1. Technique Breakdown: T1222

* What it is: File and Directory Permissions Modification. Adversaries modify file and directory permissions (DACLs) and attributes to bypass access controls, escalate privileges, or evade detection. They use built-in Windows utilities like `icacls`, `cacls`, `takeown`, and `attrib` to grant themselves full control over system directories, deny access to defenders/security tools, remove audit trails, or hide malicious binaries under system and hidden attributes.
* Log Source Requirements: Endpoint logs capturing process creation and command-line execution (specifically Windows CLI utilities modifying DACLs or attributes).
* Coverage Check:
* HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 (Process Creation) captures when utilities like `icacls.exe`, `cacls.exe`, `takeown.exe`, or `attrib.exe` are spawned, including their command-line arguments.
* NIDS (Suricata + Zeek): NO. DACL and attribute modifications are local system/operating system tasks. They generate no network traffic.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 4104 (ps_script)
* Log Sources Missing / Unused:
  - None.

---

### 2. Custom Sigma Rule

The detection logic is split into process creation monitoring of DACL command utilities and PowerShell script block logging of ACL modifications.

Rule 1 of 2: Suspicious File and Directory Permissions Modification (Process Creation)
```yaml
title: Suspicious File and Directory Permissions Modification (AN0834)
id: c46f7e8a-2c1b-4d92-8c7a-5f6d7e8a9b0d
status: experimental
description: Detects command-line permissions modifications using built-in Windows utilities (icacls, cacls, takeown, attrib) targeting directory ownership, granting excessive rights, denying access, or hiding files.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_takeown:
        - Image|endswith: '\takeown.exe'
        - OriginalFileName: 'takeown.exe'
    selection_takeown_cmd:
        CommandLine|contains|all:
            - ' /f '
            - ' /r'
    selection_icacls:
        - Image|endswith:
            - '\icacls.exe'
            - '\cacls.exe'
        - OriginalFileName:
            - 'iCACLS.EXE'
            - 'cacls.exe'
    selection_icacls_cmd:
        CommandLine|contains:
            - 'Everyone:F'
            - 'Everyone:FA'
            - ' /deny '
            - ' /reset'
    selection_attrib:
        - Image|endswith: '\attrib.exe'
        - OriginalFileName: 'attrib.exe'
    selection_attrib_cmd:
        CommandLine|contains|all:
            - ' +h'
            - ' +s'
    condition: (selection_takeown and selection_takeown_cmd) or (selection_icacls and selection_icacls_cmd) or (selection_attrib and selection_attrib_cmd)
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Administrative scripts, software installation, or updates modifying permissions or attributes on legacy applications.
    - System setup utilities configuring system files.
level: high
tags:
    - attack.defense-evasion
    - attack.privilege-escalation
    - attack.t1222.001
    - analytic.an0834
    - detection.det0299
```

Rule 2 of 2: Permissions Modification via Script Block (PowerShell Script Block)
```yaml
title: Permissions Modification via Script Block (AN0834-B)
id: 1b9c493c-9dfb-4624-bb63-83e476b3c64b
status: experimental
description: Detects executing PowerShell script blocks containing cmdlets that alter file or directory permissions (DACLs).
author: Krishna Gupta
date: 2026-06-17
logsource:
    category: ps_script
    product: windows
detection:
    selection:
        ScriptBlockText|contains:
            - 'Set-Acl'
            - 'Grant-Acl'
            - 'Revoke-Acl'
    condition: selection
fields:
    - ScriptBlockText
    - User
falsepositives:
    - Administrative scripts managing ACL policies.
level: high
tags:
    - attack.defense-evasion
    - attack.privilege-escalation
    - attack.t1222.001
    - analytic.an0834
    - detection.det0299
```

### OCSF Normalized

Rule 1 of 2: Suspicious File and Directory Permissions Modification (Process Activity)
```xml
title: Suspicious File and Directory Permissions Modification (AN0834) (OCSF-Normalized)
id: c46f7e8a-2c1b-4d92-8c7a-5f6d7e8a9b0d
status: experimental
description: Detects command-line permissions modifications using built-in Windows utilities (icacls, cacls, takeown, attrib) targeting directory ownership, granting excessive rights, denying access, or hiding files.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_takeown:
        - process.file.path|endswith: '\takeown.exe'
        - process.file.path: 'takeown.exe'
    selection_takeown_cmd:
        process.cmd_line|contains|all:
            - ' /f '
            - ' /r'
    selection_icacls:
        - process.file.path|endswith:
            - '\icacls.exe'
            - '\cacls.exe'
        - process.file.path:
            - 'iCACLS.EXE'
            - 'cacls.exe'
    selection_icacls_cmd:
        process.cmd_line|contains:
            - 'Everyone:F'
            - 'Everyone:FA'
            - ' /deny '
            - ' /reset'
    selection_attrib:
        - process.file.path|endswith: '\attrib.exe'
        - process.file.path: 'attrib.exe'
    selection_attrib_cmd:
        process.cmd_line|contains|all:
            - ' +h'
            - ' +s'
    condition: (selection_takeown and selection_takeown_cmd) or (selection_icacls and selection_icacls_cmd) or (selection_attrib and selection_attrib_cmd)
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Standard administrative tasks or baselined activity.
level: high
tags:
    - attack.defense-evasion
    - attack.t1222.001
    - analytic.an0834
    - detection.det0299
```

Rule 2 of 2: Permissions Modification via Script Block (Process Activity)
```xml
title: Permissions Modification via Script Block (OCSF-Normalized) - Rule 2 of 2
id: 1b9c493c-9dfb-4624-bb63-83e476b3c64b
status: experimental
description: Detects executing PowerShell script blocks containing cmdlets that alter file or directory permissions (DACLs), mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-17
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection:
        process.cmd_line|contains:
            - 'Set-Acl'
            - 'Grant-Acl'
            - 'Revoke-Acl'
    condition: selection
fields:
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Administrative automation scripts.
level: high
tags:
    - attack.defense-evasion
    - attack.t1222.001
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Command Line Obfuscation
  * The Issue: Adversaries can obfuscate command-line arguments using environment variables, caret escapes (^), or base64 encoding, evading simple string matching.
  * The Fix: Implement PowerShell Script Block Logging and utilize regex or script-decoding scripts in the SIEM to check arguments.

* Problem 2: Administrative Activity Noise
  * The Issue: IT administrators legitimately run command-line tools or installers that trigger these rules during system maintenance.
  * The Fix: Establish a baseline of legitimate admin tools, whitelisting specific administrative account names or management servers.

* Problem 3: Masquerading via Renamed Binaries
  * The Issue: Attackers rename native Windows utilities (such as net.exe or cmd.exe) to custom filenames to bypass path-based filters.
  * The Fix: Incorporate Masquerade-resistant criteria, matching on OriginalFileName rather than the static Image path in process creation logs.
