### 1. Technique Breakdown: T1546.008

* What it is: Event Triggered Execution: Accessibility Features. Windows accessibility tools (e.g., Sticky Keys `sethc.exe`, Utility Manager `utilman.exe`, On-Screen Keyboard `osk.exe`, Magnifier `magnify.exe`) can be launched using key combinations before a user logs in. Adversaries exploit this behavior by replacing these binary files with a command prompt (`cmd.exe`) or modifying the Image File Execution Options (IFEO) registry keys to assign a "Debugger" string pointing to a shell. When the accessibility feature is activated from the logon screen, Windows executes the shell instead, granting the attacker a `SYSTEM` shell.
* Log Source Requirements: Endpoint logs capturing process creation and command line parameters (Sysmon Event ID 1 / Windows Security Event ID 4688).
* Coverage Check:
* HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 captures parent process paths, target process paths, and command-line arguments. This allows detection of accessibility binaries spawning command interpreters, or registry tools writing to IFEO keys.
* NIDS (Suricata + Zeek): NO. Accessibility feature abuse is entirely local to the Windows subsystem and logon screen. No network-level events are generated.

---

### 2. Custom Sigma Rule

This rule utilizes Sysmon Event ID 1 (Process Creation) to detect accessibility features spawning command shells, or command-line registry operations configuring debugger overrides under the Image File Execution Options registry key.

```yaml
title: Accessibility Features Abuse Detection (AN0094)
id: 6f7f8d19-1145-45ad-9984-63649b4f8ace
status: experimental
description: Detects command-line execution of registry commands setting debugger options for accessibility binaries (like sethc.exe or utilman.exe), or detects accessibility applications launching shell interpreters.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_parent:
        ParentImage|endswith:
            - '\sethc.exe'
            - '\utilman.exe'
            - '\osk.exe'
            - '\magnify.exe'
            - '\narrator.exe'
            - '\displayswitch.exe'
            - '\AtBroker.exe'
        Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
    selection_ifeo:
        CommandLine|contains:
            - 'Image File Execution Options'
        CommandLine|contains:
            - 'debugger'
        CommandLine|contains:
            - 'sethc.exe'
            - 'utilman.exe'
            - 'osk.exe'
            - 'magnify.exe'
            - 'narrator.exe'
            - 'displayswitch.exe'
            - 'AtBroker.exe'
    condition: selection_parent or selection_ifeo
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Highly unlikely. Accessibility features spawning shell commands or registry debugging modifications on these paths are not standard administrative practices.
level: critical
tags:
    - attack.persistence
    - attack.privilege-escalation
    - attack.t1546.008
    - analytic.an0094
    - detection.det0042
```

### OCSF Normalized
```xml
title: Accessibility Features Abuse Detection (AN0094) (OCSF-Normalized)
id: 6f7f8d19-1145-45ad-9984-63649b4f8ace
status: experimental
description: Detects command-line execution of registry commands setting debugger options for accessibility binaries (like sethc.exe or utilman.exe), or detects accessibility applications launching shell interpreters.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_parent:
        actor.process.file.path|endswith:
            - '\sethc.exe'
            - '\utilman.exe'
            - '\osk.exe'
            - '\magnify.exe'
            - '\narrator.exe'
            - '\displayswitch.exe'
            - '\AtBroker.exe'
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
    selection_ifeo:
        process.cmd_line|contains:
            - 'process.file.path File Execution Options'
        process.cmd_line|contains:
            - 'debugger'
        process.cmd_line|contains:
            - 'sethc.exe'
            - 'utilman.exe'
            - 'osk.exe'
            - 'magnify.exe'
            - 'narrator.exe'
            - 'displayswitch.exe'
            - 'AtBroker.exe'
    condition: selection_parent or selection_ifeo
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Standard administrative tasks or baselined activity.
level: critical
tags:
    - attack.privilege-escalation
    - attack.t1546.008
    - analytic.an0094
    - detection.det0042
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Command Line Obfuscation
  * The Issue: Adversaries can obfuscate command-line arguments using environment variables, caret escapes (^), or base64 encoding, evading simple string matching.
  * The Fix: Implement PowerShell Script Block Logging and utilize regex or script-decoding scripts in the SIEM to check arguments.

* Problem 2: Administrative Activity Noise
  * The Issue: IT administrators legitimately run command-line tools or installers that trigger these rules during system maintenance.
  * The Fix: Establish a baseline of legitimate admin tools, whitelisting specific administrative account names or management servers.

* Problem 3: Masquerading via Renamed Binaries
  * The Issue: Attackers rename native Windows utilities (such as net.exe or cmd.exe) to custom filenames to bypass path-based filters.
  * The Fix: Incorporate Masquerade-resistant criteria, matching on OriginalFileName rather than the static Image path in process creation logs.
