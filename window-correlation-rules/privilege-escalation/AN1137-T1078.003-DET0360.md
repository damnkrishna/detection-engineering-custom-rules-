### **1. Technique Breakdown: T1078.003**

* **What it is:** **Valid Accounts: Local Accounts**. Local accounts exist on individual systems and are managed locally (e.g., the built-in local `Administrator` account). Adversaries exploit local accounts to move laterally between endpoints (using Logon Type 3 network connections) by leveraging credential reuse (where multiple workstations share the same local administrator password). Monitoring successful network logons using local administrative accounts is a highly effective method to identify lateral movement.
* **Log Source Requirements:** Windows Security Event Log capturing authentication events (Event ID 4624 for successful logon) containing user names, domain names (specifically local domains or Workgroups), logon types, and source IP addresses.
* **Coverage Check:**
* **HIDS (Sysmon + Wazuh): YES.** Windows Security Event ID 4624 logs network logon sessions on endpoints. Wazuh aggregates these logs.
* **NIDS (Suricata + Zeek): YES (Partial).** Network monitoring tools can trace NTLM/SMB handshakes over the network, but cannot verify if the credentials were successfully validated by the local Security Accounts Manager (SAM) database.

---

### **2. Custom Sigma Rule**

This rule utilizes **Windows Security Event ID 4624 (Successful Logon)** to detect network logons (Logon Type 3) using local administrator accounts, indicating credential reuse or lateral movement.

```yaml
title: Local Administrator Network Logon (AN1137)
id: defed6a9-15e0-4767-b56d-37529bbfcb51
status: experimental
description: Detects successful network logons (Logon Type 3) using local administrator accounts, which is a common indicator of lateral movement or credential reuse.
author: Krishna Gupta
date: 2026-06-07
logsource:
    product: windows
    service: security
    # Relies on Windows Security Event ID 4624
detection:
    selection_event:
        EventID: 4624
    selection_logon_type:
        LogonType:
            - 3 # Network Logon
    selection_local_admin:
        TargetUserName:
            - 'Administrator'
    selection_domain_exclusion:
        # On domain joined machines, local logins will list the computer name or NT AUTHORITY/WORKGROUP as domain, not the Active Directory domain
        TargetDomainName:
            - 'WORKGROUP'
            - 'NT AUTHORITY'
            - 'NT SERVICE'
    condition: selection_event and selection_logon_type and selection_local_admin and selection_domain_exclusion
falsepositives:
    - Automated inventory scanners or local vulnerability assessment agents utilizing local admin credentials.
level: high
tags:
    - attack.defense_evasion
    - attack.persistence
    - attack.privilege_escalation
    - attack.t1078.003
    - analytics.an1137
    - detection.det0360
```

### OCSF Normalized
```xml
title: Local Administrator Network Logon (OCSF-Normalized)
id: defed6a9-15e0-4767-b56d-37529bbfcb51
status: experimental
description: Detects successful network logons (Logon Type 3) using local administrator accounts, mapped to OCSF authentication.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: authentication
detection:
    # Based on OCSF mapping, user name is user.name, user domain is user.domain, logon type is authentication.logon_type_id, and source IP is activity.src_endpoint.ip
    selection_logon_type:
        authentication.logon_type_id:
            - 3
    selection_local_admin:
        user.name:
            - 'Administrator'
    selection_domain_exclusion:
        user.domain:
            - 'WORKGROUP'
            - 'NT AUTHORITY'
            - 'NT SERVICE'
    condition: selection_logon_type and selection_local_admin and selection_domain_exclusion
falsepositives:
    - Automated inventory scanners or local vulnerability assessment agents utilizing local admin credentials.
level: high
tags:
    - attack.defense_evasion
    - attack.persistence
    - attack.privilege_escalation
    - attack.t1078.003
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Remote UAC Restrictions (LocalAccountTokenFilterPolicy)**
* *The Issue:* By default, Windows blocks remote network logons by local administrative accounts (except the built-in RID 500 administrator) under User Account Control. If attackers cannot enable `LocalAccountTokenFilterPolicy` in the registry, their network logins will fail (generating Event ID 4625), bypassing this successful logon rule.
* *The Fix:* Implement a companion rule targeting Windows Security Event ID 4625 (Logon Failure) for local administrator accounts with Logon Type 3 to detect brute-forcing or lateral movement attempts.


* **Problem 2: Non-Standard Local Admin Account Names**
* *The Issue:* Systems built with customized OS images may rename the local administrator account to something unique (e.g., `local_sysadm`), which avoids matching `Administrator`.
* *The Fix:* Target the SID `S-1-5-21-*-500` (which uniquely identifies the built-in local administrator account regardless of renaming) in the Security Log.


* **Problem 3: Vulnerability/Asset Scanners Noise**
* *The Issue:* Corporate vulnerability assessment scanners (like Nessus, Qualys) require local administrative access to audit endpoints and routinely perform remote network logons using local administrative credentials, generating hundreds of alerts.
* *The Fix:* Define static IPs for approved vulnerability scanners and exclude their source addresses from the rule's active alerting parameters.
