### **1. Technique Breakdown: T1548**

* **What it is:** **Abuse Elevation Control Mechanism**. User Account Control (UAC) is a key Windows security feature that limits application privileges to standard user levels until an administrator authorizes an elevation. Adversaries bypass UAC by hijacking auto-elevated system binaries or hijacking COM objects and Registry paths to execute command payloads at High Integrity without triggering consent dialogs.
* **Log Source Requirements:** Endpoint logs capturing process creation, parent-child relationships, and command-line execution parameters (Sysmon Event ID 1 / Windows Security Event ID 4688).
* **Coverage Check:**
* **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 captures parent process paths, target process paths, and full command-line arguments. Wazuh aggregates these to detect UAC bypass signatures.
* **NIDS (Suricata + Zeek): NO.** Elevation of privileges is entirely a local operating system event. It does not generate network traffic.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect auto-elevated Windows binaries spawning shell processes, or registry modifications targeting common UAC bypass command keys via command line tools.

```yaml
title: Abuse of UAC Elevation Control Mechanisms (AN0975)
id: 6a088cec-84e0-4bfe-aedf-a07307bcccba
status: experimental
description: Detects bypass of User Account Control (UAC) by monitoring auto-elevating Windows binaries spawning shell utilities or command-line modifications of registry entries targeting UAC bypass paths.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_parent:
        ParentImage|endswith:
            - '\control.exe'
            - '\sdclt.exe'
            - '\fodhelper.exe'
            - '\eventvwr.exe'
            - '\computerdefaults.exe'
        Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
    selection_registry:
        CommandLine|contains:
            - '\Software\Classes\ms-settings\shell\open\command'
            - '\Software\Classes\mscfile\shell\open\command'
            - '\Software\Classes\exefile\shell\runas\command'
        CommandLine|contains:
            - 'reg'
            - 'add'
            - 'New-ItemProperty'
            - 'Set-ItemProperty'
    condition: selection_parent or selection_registry
falsepositives:
    - Administrative maintenance or backup tools using auto-elevating scripts.
level: high
tags:
    - attack.privilege_escalation
    - attack.defense_evasion
    - attack.t1548
    - analytics.an0975
    - detection.det0345
```

### OCSF Normalized
```xml
title: Abuse of UAC Elevation Control Mechanisms (OCSF-Normalized)
id: 6a088cec-84e0-4bfe-aedf-a07307bcccba
status: experimental
description: Detects bypass of User Account Control (UAC) by monitoring auto-elevating Windows binaries spawning shell utilities or command-line modifications of registry entries targeting UAC bypass paths, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    # Based on OCSF mapping, process information is under process.file.path and parent process is process.parent_process.file.path
    selection_parent:
        process.parent_process.file.path|endswith:
            - '\control.exe'
            - '\sdclt.exe'
            - '\fodhelper.exe'
            - '\eventvwr.exe'
            - '\computerdefaults.exe'
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
    selection_registry:
        process.cmd_line|contains:
            - '\Software\Classes\ms-settings\shell\open\command'
            - '\Software\Classes\mscfile\shell\open\command'
            - '\Software\Classes\exefile\shell\runas\command'
        process.cmd_line|contains:
            - 'reg'
            - 'add'
            - 'New-ItemProperty'
            - 'Set-ItemProperty'
    condition: selection_parent or selection_registry
falsepositives:
    - Administrative maintenance or backup tools using auto-elevating scripts.
level: high
tags:
    - attack.privilege_escalation
    - attack.defense_evasion
    - attack.t1548
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Fileless COM Object Hijacking**
* *The Issue:* Attackers can perform UAC bypasses using COM interfaces directly in memory (e.g., scripting objects or PowerShell in-memory COM calls) without spawning typical auto-elevated binaries or modifying standard registry command keys.
* *The Fix:* Implement PowerShell script block logging (Event ID 4104) and monitor registry modifications to CLSIDs under `HKCU\Software\Classes\CLSID\`.


* **Problem 2: Registry Value Manipulation Bypass**
* *The Issue:* Attackers can use non-standard UAC registry bypass keys, or modify the binary names to masquerade as legitimate tools, bypassing string match exclusions.
* *The Fix:* Monitor all registry write events to `HKCU\Software\Classes` and alert on any write actions targeting `\shell\open\command` keys.


* **Problem 3: Administration/Deployment Noise**
* *The Issue:* Legitimate management software or system admin scripts might trigger UAC-bypass-like patterns when launching configuration dialogs.
* *The Fix:* Whitelist known administrative deployment tools and check user/group permissions to only alert on non-administrative users executing auto-elevating programs.
