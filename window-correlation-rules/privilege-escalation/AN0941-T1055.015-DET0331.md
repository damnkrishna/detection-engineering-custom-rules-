### **1. Technique Breakdown: T1055.015**

* **What it is:** **Process Injection: ListPlanting**. Adversaries may inject malicious code by abusing list-view controls (e.g., `SysListView32`) in legitimate Windows applications. They allocate memory in the target process (`VirtualAllocEx`), write their shellcode (`WriteProcessMemory`), and then send specialized window messages (like `LVM_SETITEMPOSITION` or `LVM_SORTITEMS`) to the list-view control using `PostMessage` or `SendMessage`. This forces the application to execute the shellcode disguised as a callback function used for sorting or positioning items.
* **Log Source Requirements:** Advanced EDR telemetry (API hooking for `SendMessage`, `PostMessage`), Sysmon Event ID 10 (ProcessAccess).
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES (Partial).** Sysmon logs `ProcessAccess` (Event ID 10) to the target GUI application, but lacks visibility into the specific window messages being sent.
    * **NIDS (Suricata + Zeek): NO.** Local GUI message abuse.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 10 (ProcessAccess)** to detect suspicious access requests targeting known GUI-heavy applications that frequently utilize list-view controls, which is a prerequisite for ListPlanting.

```yaml
title: Suspicious Process Access indicating ListPlanting (AN0941)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detects the potential use of message-based injection (ListPlanting) by monitoring for suspicious ProcessAccess requests to GUI applications.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: process_access
    product: windows
    # Relies on Sysmon Event ID 10 collected via Wazuh
detection:
    selection_target:
        TargetImage|endswith:
            - '\explorer.exe'
            - '\taskmgr.exe'
    selection_access:
        GrantedAccess|contains:
            - '0x1F0FFF' # All Access
            - '0x143A'
    filter_known:
        SourceImage|endswith:
            - '\MsMpEng.exe'
            - '\dwm.exe'
    condition: selection_target and selection_access and not filter_known
fields:
    - SourceImage
    - TargetImage
    - GrantedAccess
falsepositives:
    - Accessibility tools and legitimate UI automation software interacting with list views.
level: high
tags:
    - attack.privilege_escalation
    - attack.defense_evasion
    - attack.t1055.015
    - analytics.an0941
    - detection.det0331
```

### OCSF Normalized
```xml
title: Suspicious Process Access indicating ListPlanting (OCSF-Normalized)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detects process access for list planting, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_target:
        process.file.path|endswith:
            - '\explorer.exe'
            - '\taskmgr.exe'
    selection_access:
        unmapped.granted_access|contains:
            - '0x1F0FFF'
            - '0x143A'
    filter_known:
        unmapped.parent_image|endswith:
            - '\MsMpEng.exe'
            - '\dwm.exe'
    condition: selection_target and selection_access and not filter_known
fields:
    - unmapped.granted_access
    - process.file.path
    - unmapped.parent_image
falsepositives:
    - UI automation tools.
level: high
tags:
    - attack.privilege_escalation
    - attack.t1055.015
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Ubiquity of Windows Messages**
    * *The Issue:* `SendMessage` and `PostMessage` are core to how the Windows OS functions. Hooking these APIs globally in an EDR often causes severe performance degradation, making direct detection of the `LVM_SETITEMPOSITION` message very difficult.
    * *The Fix:* EDRs should rely on memory scanning (Yara) of the target process (`explorer.exe`) looking for known ListPlanting shellcode stubs, rather than hooking every window message.

* **Problem 2: Broad ProcessAccess Signatures**
    * *The Issue:* The Sigma rule relies heavily on `ProcessAccess` (Event ID 10) because it's the only native Sysmon artifact. This is prone to false positives from legitimate applications bridging GUI elements.
    * *The Fix:* Strictly correlate the `ProcessAccess` event with the lineage of the `SourceImage`. If `SourceImage` is a recently downloaded unverified executable dropping from a browser, the confidence of the alert skyrockets.
