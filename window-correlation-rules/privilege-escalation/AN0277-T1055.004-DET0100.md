### **1. Technique Breakdown: T1055.004**

* **What it is:** **Process Injection: Asynchronous Procedure Call (APC)**. Adversaries may inject malicious code into a legitimate process by attaching the code to the APC queue of a thread within that process. When the thread enters an alterable state, it executes the queued APC (the malicious payload). This is often achieved via `QueueUserAPC` or `NtQueueApcThread` APIs.
* **Log Source Requirements:** Sysmon Event ID 8 (CreateRemoteThread), Sysmon Event ID 10 (ProcessAccess), or EDR memory telemetry.
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES (Partial).** Sysmon does not natively log `QueueUserAPC` directly. However, it logs `ProcessAccess` (Event ID 10) when the injecting process opens a handle to the target, and `CreateRemoteThread` (Event ID 8) if a thread is explicitly created.
    * **NIDS (Suricata + Zeek): NO.** Memory injection is strictly a local host activity.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 10 (ProcessAccess)** to detect suspicious access rights typically requested prior to memory allocation and APC queuing (e.g., `PROCESS_VM_WRITE`, `PROCESS_VM_OPERATION`).

```yaml
title: Suspicious Process Access for Memory Injection (AN0277)
id: d7f8g9h0-1a2b-3c4d-5e6f-7a8b9c0d1e2f
status: experimental
description: Detects malicious injection behavior involving memory allocation and altered thread context by monitoring suspicious ProcessAccess requests (e.g., 0x1F0FFF or specific VM Write access) to common target processes.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: process_access
    product: windows
    # Relies on Sysmon Event ID 10 collected via Wazuh
detection:
    selection_access:
        # 0x1F0FFF is PROCESS_ALL_ACCESS
        # 0x1438 is PROCESS_VM_OPERATION | PROCESS_VM_READ | PROCESS_VM_WRITE | PROCESS_QUERY_INFORMATION
        GrantedAccess:
            - '0x1f0fff'
            - '0x1438'
            - '0x143a'
    selection_targets:
        TargetImage|endswith:
            - '\explorer.exe'
            - '\svchost.exe'
            - '\lsass.exe'
            - '\winlogon.exe'
            - '\spoolsv.exe'
    filter_known_apps:
        SourceImage|endswith:
            - '\MsMpEng.exe' # Windows Defender
            - '\taskmgr.exe'
    condition: selection_access and selection_targets and not filter_known_apps
fields:
    - SourceImage
    - TargetImage
    - GrantedAccess
falsepositives:
    - Legitimate debugging tools or EDR agents opening processes with high privileges.
    - Software installers injecting helper DLLs into explorer.exe.
level: high
tags:
    - attack.privilege_escalation
    - attack.defense_evasion
    - attack.t1055.004
    - analytics.an0277
    - detection.det0100
```

### OCSF Normalized
```xml
title: Suspicious Process Access for Memory Injection (OCSF-Normalized)
id: d7f8g9h0-1a2b-3c4d-5e6f-7a8b9c0d1e2f
status: experimental
description: Detects suspicious process access for injection, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: system
    product: ocsf
    class: process_activity
    # Note: OCSF process_activity covers OpenProcess/access events via action_id
detection:
    selection_access:
        unmapped.granted_access:
            - '0x1f0fff'
            - '0x1438'
    selection_targets:
        process.file.path|endswith:
            - '\explorer.exe'
            - '\svchost.exe'
            - '\lsass.exe'
    filter_known_apps:
        unmapped.parent_image|endswith:
            - '\MsMpEng.exe'
    condition: selection_access and selection_targets and not filter_known_apps
fields:
    - unmapped.granted_access
    - process.file.path
    - unmapped.parent_image
falsepositives:
    - EDR agents and debuggers.
level: high
tags:
    - attack.privilege_escalation
    - attack.t1055.004
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Lack of Native APC Logging in Sysmon**
    * *The Issue:* Sysmon does not log the actual `QueueUserAPC` API call. Relying on `ProcessAccess` (Event ID 10) is noisy and circumstantial.
    * *The Fix:* Utilize advanced EDR telemetry or Microsoft Defender for Endpoint (MDE) which natively monitors API calls like `QueueUserAPC` and `VirtualAllocEx` inside host processes.

* **Problem 2: Tuning ProcessAccess**
    * *The Issue:* Event ID 10 generates a massive volume of logs in Windows environments. Alerting on `0x1F0FFF` to `svchost.exe` will cause thousands of false positives daily.
    * *The Fix:* Strictly correlate Event ID 10 with the `SourceImage`. If an untrusted, unsigned binary dropped into `%TEMP%` requests `0x1F0FFF` to `explorer.exe`, alert immediately. If `GoogleUpdate.exe` does it, it is likely benign.
