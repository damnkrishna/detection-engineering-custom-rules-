### **1. Technique Breakdown: T1134**

* **What it is:** **Access Token Manipulation**. Windows uses access tokens to determine the ownership and privileges of a process or thread. Adversaries can duplicate a privileged access token (e.g., from `lsass.exe` or `winlogon.exe` running as `SYSTEM`) using APIs like `DuplicateTokenEx`, and then use `CreateProcessWithTokenW` to spawn a new process under that privileged security context, escalating their privileges without requiring credentials.
* **Log Source Requirements:** Process creation logs (Sysmon Event ID 1 / Windows Security 4688), Windows Security Event Log (Event ID 4624 - Logon, Event ID 4672 - Special Privileges Assigned).
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 logs the `LogonId` and `User`. Correlating the parent user (e.g., standard user) with a child process user (e.g., `SYSTEM`) detects token impersonation.
    * **NIDS (Suricata + Zeek): NO.** Token manipulation is a core OS-level function occurring entirely in memory.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect the behavioral outcome of token manipulation: a child process spawning under the `NT AUTHORITY\SYSTEM` context when its direct parent is running under a standard user context.

```yaml
title: Suspicious Privilege Escalation via Token Manipulation (AN0786)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detection of suspicious token manipulation chains resulting in a process spawning as SYSTEM from a standard user parent process.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_child_user:
        User|contains: 'NT AUTHORITY\SYSTEM'
    selection_parent_user:
        # Exclude known SYSTEM parents, looking for standard users
        ParentUser|exclude: 
            - 'NT AUTHORITY\SYSTEM'
            - 'NT AUTHORITY\NETWORK SERVICE'
            - 'NT AUTHORITY\LOCAL SERVICE'
    filter_known_elevators:
        # Whitelist legitimate services that elevate context
        ParentImage|endswith:
            - '\services.exe'
            - '\svchost.exe'
            - '\lsm.exe'
            - '\winlogon.exe'
            - '\User Account Control' # consent.exe
    condition: selection_child_user and selection_parent_user and not filter_known_elevators
fields:
    - User
    - ParentUser
    - Image
    - ParentImage
falsepositives:
    - Custom in-house administrative elevation wrappers.
    - Certain third-party software updaters that utilize scheduled tasks inappropriately.
level: critical
tags:
    - attack.privilege_escalation
    - attack.t1134
    - analytics.an0786
    - detection.det0283
```

### OCSF Normalized
```xml
title: Suspicious Privilege Escalation via Token Manipulation (OCSF-Normalized)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detects process spawning as SYSTEM from standard user, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_child_user:
        user.name|contains: 'SYSTEM'
    selection_parent_user:
        actor.user.name|exclude:
            - 'SYSTEM'
            - 'NETWORK SERVICE'
            - 'LOCAL SERVICE'
    filter_known_elevators:
        unmapped.parent_image|endswith:
            - '\services.exe'
            - '\svchost.exe'
            - '\winlogon.exe'
    condition: selection_child_user and selection_parent_user and not filter_known_elevators
fields:
    - user.name
    - actor.user.name
    - process.file.path
    - unmapped.parent_image
falsepositives:
    - Administrative elevation tools.
level: critical
tags:
    - attack.privilege_escalation
    - attack.t1134
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: In-Memory Thread Impersonation**
    * *The Issue:* The attacker doesn't create a *new* process. Instead, they duplicate a SYSTEM token and call `SetThreadToken` on their current running thread. The process `User` remains "StandardUser", but the thread operates as `SYSTEM`. Sysmon Event ID 1 will miss this.
    * *The Fix:* Monitor Windows Security Event ID 4624 (Logon) for Logon Type 9 (NewCredentials) combined with Event ID 4672 (Special Privileges Assigned) assigned to anomalous processes.

* **Problem 2: Named Pipe Impersonation**
    * *The Issue:* A common technique (like RottenPotato/JuicyPotato) involves coercing a SYSTEM process to authenticate to a malicious Named Pipe. The attacker then calls `ImpersonateNamedPipeClient` to steal the token.
    * *The Fix:* Monitor Sysmon Event ID 17/18 (Pipe Created/Connected) for suspicious pipe names commonly used in Potato attacks (e.g., `\pipe\*\pipe\spoolss`) originating from non-SYSTEM processes.

* **Problem 3: Scheduled Tasks as an Evasion**
    * *The Issue:* Instead of direct token manipulation, attackers often create a scheduled task set to run as `SYSTEM` via `schtasks.exe`. The parent will legitimately be `svchost.exe`, bypassing the parent/child mismatch rule.
    * *The Fix:* Create parallel detections focused on Event ID 4698 (A scheduled task was created) checking the `Principal` node in the task XML for `SYSTEM`, especially when created from an interactive user session.
