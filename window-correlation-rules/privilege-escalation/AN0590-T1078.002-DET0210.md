### **1. Technique Breakdown: T1078.002**

* **What it is:** **Valid Accounts: Domain Accounts**. Adversaries compromise domain accounts (specifically Domain Administrators) to move laterally across an Active Directory environment, execute remote commands, and access sensitive resources. Under standard security best practices, Domain Administrator accounts should only log onto Domain Controllers or secure administrative jump boxes. A Domain Admin logging onto standard workstations or member servers suggests lateral movement or credential harvesting.
* **Log Source Requirements:** Windows Security Event Log capturing authentication events (Event ID 4624 for successful logon) containing user names, domain names, logon types, and source IP addresses.
* **Coverage Check:**
* **HIDS (Sysmon + Wazuh): YES.** Windows Security Event ID 4624 tracks domain account logon attempts on endpoints. Wazuh aggregates these logs.
* **NIDS (Suricata + Zeek): YES (Partial).** Network monitoring can capture Kerberos ticket requests (AS-REQ/TGS-REQ) and NTLM session handshakes, but endpoint-level logging is necessary to confirm successful host access.

---

### **2. Custom Sigma Rule**

This rule utilizes **Windows Security Event ID 4624 (Successful Logon)** to detect network or remote interactive logons using domain administrator accounts onto non-domain-controller workstations or member servers.

```yaml
title: Domain Administrator Logon on Workstation (AN0590)
id: 447dcbae-8166-4d6e-9980-8b9914df56b6
status: experimental
description: Detects successful remote network or RDP logons by domain administrator accounts onto standard workstation or member server endpoints, which may indicate lateral movement.
author: Krishna Gupta
date: 2026-06-07
logsource:
    product: windows
    service: security
    # Relies on Windows Security Event ID 4624
detection:
    selection_event:
        EventID: 4624
    selection_logon_type:
        LogonType:
            - 3 # Network
            - 10 # Remote Interactive
    selection_domain_admin:
        TargetUserName|contains:
            - 'da_'
            - 'domainadmin'
            - 'domadmin'
    selection_domain:
        # Check that it's a domain account, not local
        TargetDomainName|contains:
            - 'NT AUTHORITY'
            - 'NT Service'
            - 'Window Manager'
    condition: selection_event and selection_logon_type and selection_domain_admin and not selection_domain
fields:
    - EventID
    - LogonType
    - TargetDomainName
    - TargetUserName
falsepositives:
    - Authorized emergency troubleshooting by domain administrators directly on user workstations.
level: high
tags:
    - attack.defense_evasion
    - attack.persistence
    - attack.privilege_escalation
    - attack.t1078.002
    - analytics.an0590
    - detection.det0210
```

### OCSF Normalized
```xml
title: Domain Administrator Logon on Workstation (OCSF-Normalized)
id: 447dcbae-8166-4d6e-9980-8b9914df56b6
status: experimental
description: Detects successful remote network or RDP logons by domain administrator accounts onto standard workstation or member server endpoints, mapped to OCSF authentication.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: authentication
detection:
    # Based on OCSF mapping, user name is user.name, user domain is user.domain, logon type is authentication.logon_type_id, and source IP is activity.src_endpoint.ip
    selection_logon_type:
        authentication.logon_type_id:
            - 3
            - 10
    selection_domain_admin:
        user.name|contains:
            - 'da_'
            - 'domainadmin'
            - 'domadmin'
    selection_domain:
        user.domain|contains:
            - 'NT AUTHORITY'
            - 'NT Service'
            - 'Window Manager'
    condition: selection_logon_type and selection_domain_admin and not selection_domain
fields:
    - authentication.logon_type_id
    - user.domain
    - user.name
falsepositives:
    - Authorized emergency troubleshooting by domain administrators directly on user workstations.
level: high
tags:
    - attack.defense_evasion
    - attack.persistence
    - attack.privilege_escalation
    - attack.t1078.002
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Pass-the-Hash (PtH) Lateral Movement**
* *The Issue:* Pass-the-Hash attacks use NTLM hash injection to authenticate. In Windows logs, this generates a Logon Event ID 4624, but often with Logon Type 3 (Network) and NTLM authentication package. If the attacker uses the workstation's local credentials, PtH bypasses domain admin rules.
* *The Fix:* Correlate network logons (Type 3) with Logon Process `NtLmSsp` and Key Length `0`, which frequently indicates PtH or credential dumping tools like PsExec/Mimikatz.


* **Problem 2: Domain Admin Group Renaming/Custom Groups**
* *The Issue:* Active Directory environments often use custom group memberships or delegated administrative accounts (e.g., tier-specific admins like `T1-Admin`) instead of naming strings like `da_` or `domainadmin`, which bypasses simple string matching on usernames.
* *The Fix:* Integrate the SIEM with active directory LDAP directories to dynamically update a lookup list of accounts belonging to the "Domain Admins" or tier-0/tier-1 administrative security groups.


* **Problem 3: Remote Administration Tools (RATs) / Monitoring Agents**
* *The Issue:* Systems management systems (e.g., Microsoft Intune, SCCM, or helpdesk remote tools) execute background tasks using service accounts mapped to domain administration privileges, flooding logs.
* *The Fix:* Baseline legitimate remote administration tools, whitelist approved software deployment service account IPs, and focus alerts on interactive logons (Type 10/RDP).
