### **1. Technique Breakdown: T1134.003**

* **What it is:** **Access Token Manipulation: Make and Impersonate Token**. Adversaries create a brand-new logon session and associated token using APIs like `LogonUser` or `LsaLogonUser` (often passing credentials explicitly, or using the `/netonly` flag). They then assign or impersonate this new token using `SetThreadToken` or `ImpersonateLoggedOnUser` to execute actions under that freshly created security context. This is heavily used to pivot to different accounts or access network shares without dropping a new process to disk.
* **Log Source Requirements:** Windows Security Event Log (Event ID 4624 - Logon).
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Windows Security logs natively capture the `LogonUser` API call as an Event ID 4624 Logon event (typically Logon Type 2, 3, or 9).
    * **NIDS (Suricata + Zeek): NO.** This is a local authentication/token generation mechanism.

---

### **2. Custom Sigma Rule**

This rule utilizes **Windows Security Event ID 4624 (Logon)** to detect the creation of a new token via explicit credentials, specifically focusing on Logon Type 9 (NewCredentials), which is commonly generated by `runas /netonly` or PowerShell scripts invoking `LogonUserA`.

```yaml
title: Make and Impersonate Token (NewCredentials Logon) (AN1375)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detects the creation of a new token via LogonUser (Logon Type 9 - NewCredentials), often used by attackers to authenticate to network resources.
author: Krishna Gupta
date: 2026-06-09
logsource:
    product: windows
    service: security
    # Relies on Windows Security Logs (Event ID 4624)
detection:
    selection_event:
        EventID: 4624
    selection_type:
        LogonType: 9
    filter_known_processes:
        # Whitelist legitimate services that use Type 9 logons
        ProcessName|endswith:
            - '\svchost.exe'
            - '\lsass.exe'
    condition: selection_event and selection_type and not filter_known_processes
fields:
    - EventID
    - LogonType
    - ProcessName
    - TargetUserName
falsepositives:
    - Administrators legitimately using runas /netonly to access network shares from untrusted domains.
    - Application pools in IIS running under specific domain accounts.
level: high
tags:
    - attack.privilege_escalation
    - attack.t1134.003
    - analytics.an1375
    - detection.det0498
```

### OCSF Normalized
```xml
title: Make and Impersonate Token (NewCredentials Logon) (OCSF-Normalized)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detects Logon Type 9 (NewCredentials) indicative of LogonUser API abuse, mapped to OCSF authentication.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: iam
    product: ocsf
    class: authentication
detection:
    selection_event:
        activity_id: 4624
    selection_type:
        authentication.logon_type_id: 9
    filter_known_processes:
        unmapped.process_name|endswith:
            - '\svchost.exe'
            - '\lsass.exe'
    condition: selection_event and selection_type and not filter_known_processes
fields:
    - activity_id
    - authentication.logon_type_id
    - unmapped.process_name
    - user.name
falsepositives:
    - Admins using runas /netonly.
level: high
tags:
    - attack.privilege_escalation
    - attack.t1134.003
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Logon Type 9 Noise**
    * *The Issue:* `LogonType: 9` is extremely common in enterprise environments for database connections, application pools, and remote administration scripts. Alerting on every Type 9 logon will flood the SIEM.
    * *The Fix:* Correlate the Logon event with the resulting process tree. If a Type 9 logon is immediately followed by Sysmon Event ID 3 (Network Connection) to an unusual internal IP on port 445 (SMB) or 135 (RPC), it strongly indicates lateral movement using the newly minted token.

* **Problem 2: Missing the Impersonation Step**
    * *The Issue:* Event ID 4624 only proves a token was *made*. It does not prove it was *impersonated* via `SetThreadToken`.
    * *The Fix:* Look for Windows Event ID 4672 (Special Privileges Assigned to New Logon) matching the `TargetLogonId` of the 4624 event. If the new token receives `SeDebugPrivilege` or `SeImpersonatePrivilege`, the risk is critical.

* **Problem 3: Network Service Accounts**
    * *The Issue:* Network Service and Local Service accounts frequently make tokens.
    * *The Fix:* Filter out `TargetUserName` values ending in `$` (computer accounts) and focus strictly on user accounts or high-value service accounts (like `sqladmin`) being generated from interactive user workstations.
