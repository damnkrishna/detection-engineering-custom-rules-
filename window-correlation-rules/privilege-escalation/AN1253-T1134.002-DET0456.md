### **1. Technique Breakdown: T1134.002**

* **What it is:** **Access Token Manipulation: Create Process with Token**. Adversaries may create a new process with an elevated token to escalate privileges. They typically steal a token from a high-privilege process (like `winlogon.exe` running as `SYSTEM`) using `DuplicateTokenEx`, and then use APIs like `CreateProcessWithTokenW` or `CreateProcessAsUserW` to launch their payload (e.g., `cmd.exe`) under that duplicated, highly-privileged security context. 
* **Log Source Requirements:** Process creation logs (Sysmon Event ID 1 / Windows Security 4688), Windows Security Event Log (Event ID 4624 - Logon).
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon logs the creation of the new process. If a standard user process spawns a child process running as `SYSTEM`, it clearly indicates context switching.
    * **NIDS (Suricata + Zeek): NO.** Token manipulation is a local OS mechanism.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect the behavioral manifestation of `CreateProcessWithTokenW`: a child process executing under a different, higher-privileged security context (`User`) than its parent (`ParentUser`).

```yaml
title: Privilege Escalation via CreateProcessWithToken (AN1253)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detects a process spawning a new process whose security context (User) differs from its parent (ParentUser), indicating token duplication/impersonation.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_child_user:
        User|contains: 'NT AUTHORITY\SYSTEM'
    selection_parent_user:
        ParentUser|exclude: 
            - 'NT AUTHORITY\SYSTEM'
            - 'NT AUTHORITY\NETWORK SERVICE'
            - 'NT AUTHORITY\LOCAL SERVICE'
    selection_child_image:
        Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
    filter_known_elevators:
        ParentImage|endswith:
            - '\services.exe'
            - '\svchost.exe'
            - '\winlogon.exe'
    condition: selection_child_user and selection_parent_user and selection_child_image and not filter_known_elevators
fields:
    - User
    - ParentUser
    - Image
    - ParentImage
falsepositives:
    - Approved enterprise elevation solutions (e.g., CyberArk EPM, BeyondTrust).
level: critical
tags:
    - attack.privilege_escalation
    - attack.t1134.002
    - analytics.an1253
    - detection.det0456
```

### OCSF Normalized
```xml
title: Privilege Escalation via CreateProcessWithToken (OCSF-Normalized)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detects process token switching, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_child_user:
        user.name|contains: 'SYSTEM'
    selection_parent_user:
        actor.user.name|exclude:
            - 'SYSTEM'
            - 'NETWORK SERVICE'
            - 'LOCAL SERVICE'
    selection_child_image:
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
    filter_known_elevators:
        unmapped.parent_image|endswith:
            - '\services.exe'
            - '\svchost.exe'
    condition: selection_child_user and selection_parent_user and selection_child_image and not filter_known_elevators
fields:
    - user.name
    - actor.user.name
    - process.file.path
    - unmapped.parent_image
falsepositives:
    - Privilege Access Management (PAM) software.
level: critical
tags:
    - attack.privilege_escalation
    - attack.t1134.002
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Runas Utility**
    * *The Issue:* The native Windows `runas.exe` utility explicitly performs this action (launching a process under a different context).
    * *The Fix:* While the Sigma rule catches the generic token switch, explicitly monitor for `runas.exe` being used in scripts or from `cmd.exe` by standard users, specifically targeting the `/savecred` flag which is heavily abused.

* **Problem 2: Thread-Level Impersonation**
    * *The Issue:* If the attacker uses `SetThreadToken` instead of `CreateProcessWithTokenW`, the primary process `User` field remains the same; only the specific executing thread changes its context. Sysmon Event ID 1 will not detect this.
    * *The Fix:* Monitor Windows Event ID 4624 (Logon) for Type 9 (NewCredentials) combined with Event ID 4672 (Special Privileges) to detect thread-level token elevation.

* **Problem 3: Spoofed Parent Processes**
    * *The Issue:* If an attacker steals a SYSTEM token and uses `CreateProcessWithTokenW`, they can *also* spoof the parent process using `UpdateProcThreadAttribute`. They could make it look like `services.exe` (a SYSTEM process) spawned their payload, matching the `filter_known_elevators` exclusion.
    * *The Fix:* Use Sysmon Event ID 1's `ParentProcessGuid` and correlate it against ETW telemetry to verify if the lineage is logically sound (detecting PPID spoofing).
