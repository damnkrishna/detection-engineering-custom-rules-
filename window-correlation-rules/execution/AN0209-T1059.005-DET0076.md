### **1. Technique Breakdown: T1059.005**

* **What it is:** **Visual Basic**. Adversaries abuse the Windows Script Host (WSH) engine (via interpreters like `wscript.exe` and `cscript.exe`) to execute malicious Visual Basic scripts (`.vbs`, `.vbe`, or `.vba`). This is a common execution method used in phishing attacks, where a malicious macro in an Office document (Word, Excel) spawns a VBScript file, or a user runs a downloaded `.vbs` script disguised as a document.
* **Log Source Requirements:** Endpoint logs capturing process creation and command line parameters (specifically targeting built-in script hosts spawned by Office applications or running from temporary directories).
* **Coverage Check:**
* **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 (Process Creation) logs the execution of `wscript.exe` or `cscript.exe` and records command-line flags showing the executed script paths. It also logs parent-child process relationships (e.g., Office applications spawning script hosts).
* **NIDS (Suricata + Zeek): NO.** VBScript execution is a local endpoint activity. Network security tools cannot inspect host-internal script execution (though they might alert on the initial download of the `.vbs` file or outbound C2 traffic).

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect when Windows Script Host interpreters (`wscript.exe`, `cscript.exe`) are executed under suspicious contexts: either spawned by Microsoft Office applications (indicating macro exploitation) or executing scripts from user-writable temporary directories.

```yaml
title: Suspicious Visual Basic Script Execution (AN0209)
id: c57f8e9a-1b2c-4d92-8c8d-6f7d8e9a0b0c
status: experimental
description: Detects suspicious execution of wscript.exe or cscript.exe spawned by Microsoft Office applications or executing VBScripts from user-writable directories.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_images:
        - Image|endswith:
            - '\wscript.exe'
            - '\cscript.exe'
        - OriginalFileName:
            - 'wscript.exe'
            - 'cscript.exe'
    selection_office_parents:
        ParentImage|endswith:
            - '\winword.exe'
            - '\excel.exe'
            - '\powerpnt.exe'
            - '\outlook.exe'
    selection_temp_paths:
        CommandLine|contains:
            - '\AppData\Local\Temp\'
            - '\Users\Public\'
            - '\Windows\Temp\'
            - '\AppData\Roaming\'
    condition: selection_images and (selection_office_parents or selection_temp_paths)
fields:
    - CommandLine
    - Image
    - OriginalFileName
    - ParentImage
falsepositives:
    - Administrative logon scripts or internal deployment tools executing VBScripts.
    - Specialized local applications executing macro tasks during update processes.
level: high
tags:
    - attack.execution
    - attack.t1059.005
    - analytics.an0209
    - detection.det0076
```

### OCSF Normalized
```xml
title: Suspicious Visual Basic Script Execution (OCSF-Normalized)
id: c57f8e9a-1b2c-4d92-8c8d-6f7d8e9a0b0c
status: experimental
description: Detects suspicious execution of wscript.exe or cscript.exe spawned by Microsoft Office applications or executing VBScripts from user-writable directories, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    # Based on OCSF mapping, process identification is under process.file.path
    selection_images:
        process.file.path|endswith:
            - '\wscript.exe'
            - '\cscript.exe'
            
    # Targeting Office parent processes in OCSF unmapped.parent_image
    selection_office_parents:
        unmapped.parent_image|endswith:
            - '\winword.exe'
            - '\excel.exe'
            - '\powerpnt.exe'
            - '\outlook.exe'
            
    # Checking command line arguments using OCSF process.cmd_line
    selection_temp_paths:
        process.cmd_line|contains:
            - '\AppData\Local\Temp\'
            - '\Users\Public\'
            - '\Windows\Temp\'
            - '\AppData\Roaming\'
            
    condition: selection_images and (selection_office_parents or selection_temp_paths)
fields:
    - unmapped.parent_image
    - process.cmd_line
    - process.file.path
falsepositives:
    - Administrative logon scripts or internal deployment tools executing VBScripts.
    - Specialized local applications executing macro tasks during update processes.
level: high
tags:
    - attack.execution
    - attack.t1059.005
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Execution via MSI wrappers or non-standard parents**
* *The Issue:* Attackers can wrap VBScripts in MSI files, custom installer binaries, or load them through helper DLLs (`scrobj.dll` via `regsvr32.exe`), which will bypass the direct parent-child check of Office processes spawning `wscript.exe`.
* *The Fix:* In addition to monitoring parent processes, monitor the execution of `regsvr32.exe` or `rundll32.exe` pointing to local `.sct` files, and monitor the creation of files with `.vbs`/`.vbe` extensions in user-writable folders.


* **Problem 2: Command-Line Obfuscation**
* *The Issue:* Attackers can rename the script file to a non-standard extension (e.g., renaming `script.vbs` to `script.txt`) and pass it to `wscript.exe /e:VBScript script.txt`. The `CommandLine` filters checking only for `.vbs` extension names can be bypassed.
* *The Fix:* The rule monitors `wscript.exe` and `cscript.exe` executing from suspicious directories regardless of extension. However, to tune it, monitor for the `/e:` engine parameter forcing VBScript execution on unexpected file types.


* **Problem 3: Administrative Scripting Noise**
* *The Issue:* Network administrators regularly use VBS scripts for active directory logon configurations, printer mappings, or hardware inventory gathering, leading to high false-positive alerts.
* *The Fix:* Maintain a repository of authorized logon scripts and whitelist their hashes, or exclude known, signed system administrative accounts from the alerting rule.
