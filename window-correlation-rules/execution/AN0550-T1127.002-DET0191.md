### **1. Technique Breakdown: T1127.002**

* **What it is:** **Trusted Developer Utilities Proxy Execution: ClickOnce**. Adversaries abuse the Windows ClickOnce deployment technology (`dfshim.dll` and `dfsvc.exe`) to execute malicious applications. ClickOnce allows users to install and run Windows applications by clicking a link in a web page or document. Attackers can abuse this by using `rundll32.exe` to call `dfshim.dll` with a URL pointing to a malicious ClickOnce application, bypassing application control rules because both `rundll32.exe` and `dfsvc.exe` (the ClickOnce engine) are legitimate, trusted Microsoft utilities.
* **Log Source Requirements:** Endpoint logs capturing process creation and command line parameters (specifically targeting `rundll32.exe` loading `dfshim.dll` or `dfsvc.exe` spawning unexpected child processes).
* **Coverage Check:**
* **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 (Process Creation) logs the command line arguments when `rundll32.exe` is called with `dfshim.dll` and when `dfsvc.exe` spawns follow-on processes.
* **NIDS (Suricata + Zeek): NO.** ClickOnce execution is an endpoint-local application loading process. Network monitoring cannot differentiate a malicious ClickOnce application download from a legitimate one (though network logs may capture the SMB or HTTP requests fetching the `.application` file).

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect the abuse of ClickOnce utility proxies, specifically focusing on `rundll32.exe` calling `dfshim.dll` to launch remote apps, or the ClickOnce service engine (`dfsvc.exe`) spawning command shell interpreters or other suspicious child processes.

```yaml
title: ClickOnce Proxy Execution Abuse (AN0550)
id: c46e7f8a-1c2b-4d92-8c8d-6f7d8e9a0b0d
status: experimental
description: Detects ClickOnce utility abuse, including rundll32.exe loading dfshim.dll to execute applications or dfsvc.exe spawning unexpected child shells.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_rundll:
        - Image|endswith: '\rundll32.exe'
        - OriginalFileName: 'RUNDLL32.EXE'
    selection_dfshim_args:
        CommandLine|contains|all:
            - 'dfshim.dll'
        CommandLine|contains:
            - 'ShOpenVerbApplication'
            - 'ShOpenVerbShortcut'
    selection_dfsvc:
        ParentImage|endswith: '\dfsvc.exe'
    selection_suspicious_children:
        - Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\regsvr32.exe'
            - '\rundll32.exe'
            - '\mshta.exe'
            - '\csc.exe'
            - '\msbuild.exe'
    condition: (selection_rundll and selection_dfshim_args) or (selection_dfsvc and selection_suspicious_children)
falsepositives:
    - Legitimate corporate ClickOnce application deployments launching administrative scripts or self-updating utilities.
level: high
tags:
    - attack.execution
    - attack.defense_evasion
    - attack.t1127.002
    - analytics.an0550
    - detection.det0191
```

### OCSF Normalized
```xml
title: ClickOnce Proxy Execution Abuse (OCSF-Normalized)
id: c46e7f8a-1c2b-4d92-8c8d-6f7d8e9a0b0d
status: experimental
description: Detects ClickOnce utility abuse, including rundll32.exe loading dfshim.dll to execute applications or dfsvc.exe spawning unexpected child shells, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    # Based on OCSF mapping, process identification is under process.file.path
    selection_rundll:
        process.file.path|endswith: '\rundll32.exe'
        
    # Checking ClickOnce execution arguments in OCSF process.cmd_line
    selection_dfshim_args:
        process.cmd_line|contains|all:
            - 'dfshim.dll'
        process.cmd_line|contains:
            - 'ShOpenVerbApplication'
            - 'ShOpenVerbShortcut'
            
    # Checking ClickOnce engine parent process in OCSF process.parent_process.file.path
    selection_dfsvc:
        process.parent_process.file.path|endswith: '\dfsvc.exe'
        
    # Checking suspicious child processes launched by ClickOnce engine in OCSF process.file.path
    selection_suspicious_children:
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\regsvr32.exe'
            - '\rundll32.exe'
            - '\mshta.exe'
            - '\csc.exe'
            - '\msbuild.exe'
            
    condition: (selection_rundll and selection_dfshim_args) or (selection_dfsvc and selection_suspicious_children)
falsepositives:
    - Legitimate corporate ClickOnce application deployments launching administrative scripts or self-updating utilities.
level: high
tags:
    - attack.execution
    - attack.defense_evasion
    - attack.t1127.002
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Direct API invocation of `dfshim.dll` via custom hosts**
* *The Issue:* Attackers can write their own loader executables (in C# or C++) that load `dfshim.dll` into their memory space and invoke the ClickOnce API functions directly without calling `rundll32.exe`. This bypasses the command line rule for `rundll32.exe`.
* *The Fix:* Monitor the file write activity of `.application` files or XML manifests under the ClickOnce cache folder (`%USERPROFILE%\AppData\Local\Apps\2.0\`).


* **Problem 2: Execution of Legitimate Non-Shell child processes**
* *The Issue:* If the ClickOnce application executes a legitimate helper application that is not in the `selection_suspicious_children` list (such as a browser or signed PDF reader), it will not trigger the child process alert, although it could be hijacked or used maliciously.
* *The Fix:* Implement system auditing policies to monitor network connections initiated by child processes of `dfsvc.exe`, flagging any outbound network traffic from ClickOnce sub-processes that is anomalous.


* **Problem 3: Corporate ClickOnce Application False Positives**
* *The Issue:* Many commercial software vendors (like customer relationship managers or remote support tools) use ClickOnce for software distribution, causing frequent alerts when those applications update or launch.
* *The Fix:* Maintain a baseline of trusted corporate ClickOnce application URLs and exclude their specific paths or digital signatures from triggering the alerts.
