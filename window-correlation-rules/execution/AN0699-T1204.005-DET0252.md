### **1. Technique Breakdown: T1204.005**

* **What it is:** **User Execution: Malicious File Installers**. Adversaries may trick users into running malicious installers or package managers (like `pip`, `npm`, `msiexec`) that execute malicious code disguised as legitimate software or libraries. In development environments, typosquatting packages in PyPI or NPM leads to immediate code execution upon package installation.
* **Log Source Requirements:** Process creation logs (Sysmon Event ID 1 / Windows Security Event ID 4688) and File Creation logs (Sysmon Event ID 11).
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon tracks the execution of `pip.exe`, `npm.cmd`, and `msiexec.exe`, as well as downstream script interpreter spawning and file drops.
    * **NIDS (Suricata + Zeek): PARTIAL.** NIDS will see HTTP/HTTPS requests to package repositories (like `registry.npmjs.org`), but payload inspection is difficult due to TLS.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect the execution of package managers dropping scripts into user directories followed by immediate execution of shells or interpreters.

```yaml
title: Suspicious Execution via Malicious Installer/Package Manager (AN0699)
id: f48b1112-9844-4632-901b-c1284a14f4e8
status: experimental
description: Detects the execution of pip, npm, or MSI installers followed by suspicious child processes (script interpreters) or writes to temporary user directories.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_parent:
        ParentImage|endswith:
            - '\pip.exe'
            - '\npm.cmd'
            - '\msiexec.exe'
    selection_child:
        Image|endswith:
            - '\python.exe'
            - '\node.exe'
            - '\cmd.exe'
            - '\powershell.exe'
            - '\rundll32.exe'
    selection_args:
        CommandLine|contains:
            - 'install'
            - 'setup.py'
            - '/q'
            - '/i'
    condition: selection_parent and selection_child and selection_args
fields:
    - CommandLine
    - Image
    - ParentImage
falsepositives:
    - Legitimate software developers installing known packages.
    - Automated CI/CD pipelines deploying software.
level: high
tags:
    - attack.execution
    - attack.t1204.005
    - analytics.an0699
    - detection.det0252
```

### OCSF Normalized
```xml
title: Suspicious Execution via Malicious Installer/Package Manager (OCSF-Normalized)
id: f48b1112-9844-4632-901b-c1284a14f4e8
status: experimental
description: Detects package managers spawning shells, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_parent:
        unmapped.parent_image|endswith:
            - '\pip.exe'
            - '\npm.cmd'
            - '\msiexec.exe'
    selection_child:
        process.file.path|endswith:
            - '\python.exe'
            - '\node.exe'
            - '\cmd.exe'
            - '\powershell.exe'
    selection_args:
        process.cmd_line|contains:
            - 'install'
            - 'setup.py'
            - '/q'
    condition: selection_parent and selection_child and selection_args
fields:
    - unmapped.parent_image
    - process.cmd_line
    - process.file.path
falsepositives:
    - CI/CD automated deployments.
level: high
tags:
    - attack.execution
    - attack.t1204.005
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Excessive Developer Noise**
    * *The Issue:* In an engineering environment, `pip install` or `npm install` executing `python.exe` (e.g., to run `setup.py`) happens hundreds of times a day.
    * *The Fix:* Baseline known developer endpoints and restrict alerts to specific unapproved networks or correlate with Sysmon Event ID 3 (Network Connections) looking for anomalous post-install C2 beaconing.

* **Problem 2: Silent Droppers**
    * *The Issue:* A malicious NPM package might not spawn `cmd.exe` directly but instead drop a backdoor DLL into a startup folder.
    * *The Fix:* Monitor Sysmon Event ID 11 (File Creation) where the parent process is `npm` or `pip`, writing `.dll`, `.exe`, or `.bat` files into `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\`.

* **Problem 3: Custom Installers**
    * *The Issue:* Attackers might use Inno Setup, NSIS, or custom wrapper binaries instead of `msiexec` or `pip`.
    * *The Fix:* Broaden the parent process selection to detect any process dropping files into `%TEMP%` and immediately executing them.
