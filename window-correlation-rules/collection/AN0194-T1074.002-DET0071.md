### **1. Technique Breakdown: T1074.002**

* **What it is:** **Data Staged: Remote Data Staging**. Adversaries copy collected data to a centralized remote staging location (such as a shared folder or storage server inside the compromised network) prior to exfiltration. Staging data remotely helps them aggregate logs or files from multiple systems before transmitting them out-of-band. On Windows systems, this is detected by identifying processes writing data archives (like `.zip`, `.7z`, `.rar`) to remote UNC paths (e.g. `\\server\share\`) or mapping staging drives.
* **Log Source Requirements:** Sysmon File Creation logs (Event ID 11) or process creation logs (Event ID 1) capturing network drive mappings.
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 11 captures file creations targeting UNC paths. Event ID 1 captures command parameters mapping drives (like `net.exe use`). Wazuh manages alerting.
    * **NIDS (Suricata + Zeek): YES.** Network logs capture SMB file write operations over ports 445/139, which confirms network staging.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 11 (File Creation)** to detect local processes writing archive files directly to remote UNC shares.

```yaml
title: Collection - Archive File Written to Remote UNC Share (AN0194)
id: 3520f684-7d35-48a7-8e40-62601ad4d27c
status: experimental
description: Detects processes writing compressed archive files (such as .zip, .7z, .rar) to remote UNC network paths, which may indicate remote data staging prior to exfiltration.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: file_event
    product: windows
    # Relies on Sysmon Event ID 11 collected via Wazuh
detection:
    selection_unc:
        TargetFilename|startswith: '\\'
    selection_archive:
        TargetFilename|endswith:
            - '.zip'
            - '.7z'
            - '.rar'
            - '.tar'
            - '.gz'
            - '.cab'
    filter_system:
        # Exclude standard system processes writing back up shares
        Image|endswith:
            - '\System32\ntbackup.exe'
            - '\System32\wbengine.exe'
    condition: selection_unc and selection_archive and not filter_system
fields:
    - Image
    - TargetFilename
falsepositives:
    - IT administrators copy-pasting system backup zips or patch files to network servers.
    - Automated scheduled backup software copying archives to target backup shares.
level: medium
tags:
    - attack.collection
    - attack.t1074.002
    - analytics.an0194
    - detection.det0071
```

### OCSF Normalized
```xml
title: Collection - Archive File Written to Remote UNC Share (OCSF-Normalized)
id: 3520f684-7d35-48a7-8e40-62601ad4d27c
status: experimental
description: Detects archive files written to remote UNC paths, mapped to OCSF file_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: file_activity
detection:
    # Based on OCSF mapping, created file is file.path
    selection_unc:
        file.path|startswith: '\\'
    selection_archive:
        file.path|endswith:
            - '.zip'
            - '.7z'
            - '.rar'
            - '.tar'
    condition: selection_unc and selection_archive
fields:
    - file.path
falsepositives:
    - Normal user or administrator remote file backups.
level: medium
tags:
    - attack.collection
    - attack.t1074.002
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Standard IT backup shares (Noise)**
    * *The Issue:* Scheduled administrative backup jobs copy compressed database dumps or application logs daily, triggering high-severity alerts.
    * *The Fix:* Baseline the environment to whitelist target backup UNC paths (e.g. `\\backup-srv\db-backups\`) or whitelist specific authorized service accounts.

* **Problem 2: Staging without Compression**
    * *The Issue:* If the attacker copies uncompressed raw text files (like `.csv`, `.txt`, `.docx`) to the remote share rather than zip archives, this archive-based rule will miss the activity.
    * *The Fix:* Add a complementary rule tracking anomalous volume write spikes to remote shares (Event ID 5145 in the Security log).

* **Problem 3: Renamed files (Bypassing extension checks)**
    * *The Issue:* Attackers can write the zip archive to the remote share using a decoy extension (e.g. `data.tmp` or `data.dat`) and rename it later, bypassing extension checks.
    * *The Fix:* Monitor process execution to detect subsequent file renames, or inspect the file header signature (magic bytes) using host AV/EDR.
