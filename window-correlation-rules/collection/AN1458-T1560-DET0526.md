### **1. Technique Breakdown: T1560**

* **What it is:** **Archive Collected Data**. Adversaries compress or encrypt collected data before exfiltration to minimize network bandwidth usage, speed up transfer times, and hide the contents of the files being stolen. They utilize standard administrative or packaging utilities (like `makecab.exe`, `7z.exe`, `rar.exe`, `tar.exe`) or built-in PowerShell commands. Host-based detection looks for command-line arguments specifying creation switches (e.g. `a`, `-cf`, `-p` for password protection) or using staging output directories.
* **Log Source Requirements:** Sysmon process creation logs (Event ID 1) or Windows Security Event ID 4688 capturing command-line parameters of archiving utilities.
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 logs process details, capturing command-line switches denoting archive creation and encryption (like adding passwords).
    * **NIDS (Suricata + Zeek): NO.** Local file archiving is a host-only filesystem operation.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect the execution of archiving tools or system utilities (`makecab.exe`, `7z.exe`, `rar.exe`, `tar.exe`) running with specific archive creation or password switches.

```yaml
title: Collection - Archiving and Encryption of Data via Utility (AN1458)
id: 6b73e685-e7ab-405d-a698-2acf95c6eb52
status: experimental
description: Detects compression utilities or native tools executing with specific command-line parameters used to create archives or encrypt files (like password switches), suggesting staging before exfiltration.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_makecab:
        - Image|endswith: '\makecab.exe'
        - OriginalFileName: 'makecab.exe'
    selection_utilities:
        - Image|endswith:
            - '\7z.exe'
            - '\rar.exe'
            - '\tar.exe'
            - '\zip.exe'
        - OriginalFileName:
            - '7z.exe'
            - 'rar.exe'
            - 'tar.exe'
            - 'zip.exe'
    selection_switches:
        CommandLine|contains:
            - ' a '       # Add to archive (7z, rar)
            - ' -p'       # Password protection/encryption (7z, rar)
            - ' -cf'      # Create file (tar)
            - ' -cz'      # Compress gzip (tar)
            - 'Compress-Archive'
    condition: selection_makecab or (selection_utilities and selection_switches)
fields:
    - CommandLine
    - Image
    - OriginalFileName
falsepositives:
    - Legitimate backup scripts, installer deployment tasks, or manual developer archiving.
level: medium
tags:
    - attack.collection
    - attack.t1560
    - analytics.an1458
    - detection.det0526
```

### OCSF Normalized
```xml
title: Collection - Archiving and Encryption of Data via Utility (OCSF-Normalized)
id: 6b73e685-e7ab-405d-a698-2acf95c6eb52
status: experimental
description: Detects archive utility execution with compression switches, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    # Based on OCSF mapping, process path is process.file.path and cmd line is process.cmd_line
    selection_utilities:
        process.file.path|endswith:
            - '\makecab.exe'
            - '\7z.exe'
            - '\rar.exe'
            - '\tar.exe'
    selection_switches:
        process.cmd_line|contains:
            - ' a '
            - ' -p'
            - ' -cf'
            - 'Compress-Archive'
    condition: selection_utilities and selection_switches
fields:
    - process.cmd_line
    - process.file.path
falsepositives:
    - Administrative file archiving and sync processes.
level: medium
tags:
    - attack.collection
    - attack.t1560
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Scripted Zip Assemblies**
    * *The Issue:* If the adversary uses PowerShell script assemblies (`[System.IO.Compression.ZipFile]::CreateFromDirectory`) instead of running command utilities, this process-based rule will not capture the activity.
    * *The Fix:* Enable PowerShell Script Block Logging (Event ID 4104) and write rules detecting calls to `System.IO.Compression` namespaces.

* **Problem 2: Renamed Archive Tools**
    * *The Issue:* Attackers can rename `7z.exe` to a system-like name (e.g. `svchost.exe`) to run compression, escaping process image endswith filters.
    * *The Fix:* Include `OriginalFileName` checks in Sysmon Event ID 1 (which tracks internal PE file headers) or monitor file creation events (Event ID 11) for massive numbers of files being packed into a single target file.

* **Problem 3: Daily Developer/User Compression**
    * *The Issue:* Developers and standard users create `.zip` or `.tar.gz` files daily for standard operations, causing frequent false positives.
    * *The Fix:* Focus alerting on non-standard directories (e.g. `/Windows/Temp` or `/Users/Public`) and baseline standard user behaviors.
