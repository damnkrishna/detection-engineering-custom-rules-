### **1. Technique Breakdown: T1213**

* **What it is:** **Data from Information Repositories**. Adversaries target and collect data from information repositories (like SharePoint, Confluence, internal wikis, or database repositories) to harvest intellectual property, credentials, or network diagrams. They may programmatically download directories or query tables using command-line tools (e.g. `sqlcmd.exe`), PowerShell scripts containing SharePoint client APIs (such as `Microsoft.SharePoint.Client`), or command-line web clients (e.g. `curl.exe`, `wget.exe`, `Invoke-WebRequest`).
* **Log Source Requirements:** Windows Security Event Log Event ID 4688 or Sysmon process creation logs (Event ID 1) capturing command-line parameters of database or repository query tools.
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 logs process details, capturing cmdline parameters that query or download from internal information systems.
    * **NIDS (Suricata + Zeek): YES.** Network filters capture high-volume HTTP/HTTPS requests to internal wikis or database ports (1433, 1521, 3306), indicating mass enumeration.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect processes executing command-line query tools or script engines targeting common information repository endpoints or using database query switches.

```yaml
title: Collection - Programmatic or Excessive Information Repository Access (AN1160)
id: 8f491583-6eee-4ecf-b2c8-5de62ff0c09f
status: experimental
description: Detects command-line utilities, PowerShell commands, or database clients targeting SharePoint, Confluence, or SQL databases to gather sensitive repository data.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 / Security Event ID 4688 collected via Wazuh
detection:
    selection_db_clients:
        - Image|endswith:
            - '\sqlcmd.exe'
            - '\osql.exe'
            - '\mysql.exe'
            - '\psql.exe'
        - OriginalFileName:
            - 'sqlcmd.exe'
            - 'osql.exe'
            - 'mysql.exe'
            - 'psql.exe'
    selection_repo_keywords:
        CommandLine|contains:
            - 'sharepoint'
            - 'confluence'
            - 'spweb'
            - 'spsite'
            - 'spfolder'
            - 'spfile'
            - 'Invoke-Sqlcmd'
            - 'Invoke-RestMethod'
            - 'Invoke-WebRequest'
    condition: selection_db_clients or selection_repo_keywords
fields:
    - CommandLine
    - Image
    - OriginalFileName
falsepositives:
    - Database administrators running routine maintenance scripts.
    - Automated system backup or synchronization tasks connecting to databases/SharePoint.
level: medium
tags:
    - attack.collection
    - attack.t1213
    - analytics.an1160
    - detection.det0413
```

### OCSF Normalized
```xml
title: Collection - Programmatic or Excessive Information Repository Access (OCSF-Normalized)
id: 8f491583-6eee-4ecf-b2c8-5de62ff0c09f
status: experimental
description: Detects programmatic access to databases or information repositories, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    # Based on OCSF mapping, process command line is process.cmd_line and path is process.file.path
    selection_db_clients:
        process.file.path|endswith:
            - '\sqlcmd.exe'
            - '\osql.exe'
            - '\mysql.exe'
            - '\psql.exe'
    selection_repo_keywords:
        process.cmd_line|contains:
            - 'sharepoint'
            - 'confluence'
            - 'spweb'
            - 'spsite'
            - 'Invoke-Sqlcmd'
            - 'Invoke-WebRequest'
    condition: selection_db_clients or selection_repo_keywords
fields:
    - process.cmd_line
    - process.file.path
falsepositives:
    - Admin scripts connecting to SQL/SharePoint databases.
level: medium
tags:
    - attack.collection
    - attack.t1213
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Standard IT Sync & Backup Services**
    * *The Issue:* Legitimate applications and scripts regularly sync with SharePoint or run database backups, producing a high volume of alerts.
    * *The Fix:* Baseline the environment and whitelist authorized service accounts (e.g., SharePoint sync account, Backup Admin) and specific source IPs.

* **Problem 2: Browser-Based Interactive Access**
    * *The Issue:* If the adversary manually logs into SharePoint or Confluence using a standard browser and downloads files, no command-line process triggers are observed.
    * *The Fix:* Implement application-level monitoring (SaaS audit logs from Microsoft 365 or Confluence Cloud audit logs) to detect high-frequency file download alerts from a single user session.

* **Problem 3: Encoded PowerShell Commands**
    * *The Issue:* Attackers can execute base64-encoded PowerShell scripts to query repositories, hiding the keywords from process command line monitoring.
    * *The Fix:* Enable and monitor PowerShell Script Block Logging (Event ID 4104) to audit the decrypted script contents before execution.
