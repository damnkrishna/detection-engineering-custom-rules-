### **1. Technique Breakdown: T1114**

* **What it is:** **Email Collection**. Adversaries may search local system directories or Exchange servers for email databases (such as Outlook `.pst` or `.ost` files) to extract sensitive emails, contacts, and calendar items. They copy these files to local staging folders using command-line utilities (like `xcopy.exe` or `robocopy.exe`), PowerShell scripting, or custom malware. They may also set up malicious inbox rules or auto-forwarding settings to exfiltrate email traffic in real time.
* **Log Source Requirements:** Sysmon process creation logs (Event ID 1) capturing command lines referencing Outlook data files, or Sysmon File Creation/Access events (Event ID 11) capturing access to `.pst`/`.ost` by non-Outlook processes.
* **Coverage Check:**
    * **HIDS (Sysmon + Wazuh): YES.** Sysmon Event ID 1 logs process details, capturing command lines that copy or compress `.pst` or `.ost` files. Sysmon Event ID 11 can be tuned to detect non-Outlook processes opening email store databases.
    * **NIDS (Suricata + Zeek): NO.** Local database discovery and copying are host-only filesystem operations.

---

### **2. Custom Sigma Rule**

This rule utilizes **Sysmon Event ID 1 (Process Creation)** to detect command-line utilities (e.g., copy utilities, scripting engines, compression tools) referencing Outlook Personal Folders (`.pst`) or Offline Folder (`.ost`) files.

```yaml
title: Collection - Local Email Database Access and Collection (AN1309)
id: e0be8fe4-ec61-4434-ae75-81f12e9e3a67
status: experimental
description: Detects command-line utilities or scripting engines (cmd, powershell, xcopy, robocopy, 7z) referencing Outlook .pst or .ost files, suggesting local email harvesting.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 collected via Wazuh
detection:
    selection_utilities:
        - Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\xcopy.exe'
            - '\robocopy.exe'
            - '\7z.exe'
            - '\tar.exe'
        - OriginalFileName:
            - 'cmd.exe'
            - 'PowerShell.EXE'
            - 'pwsh.dll'
            - 'xcopy.exe'
            - 'robocopy.exe'
            - '7z.exe'
            - 'tar.exe'
    selection_email_extensions:
        CommandLine|contains:
            - '.pst'
            - '.ost'
    filter_outlook:
        # Exclude official outlook operations
        Image|endswith:
            - '\OFFICE15\OUTLOOK.EXE'
            - '\OFFICE16\OUTLOOK.EXE'
            - '\outlook.exe'
    condition: selection_utilities and selection_email_extensions and not filter_outlook
fields:
    - CommandLine
    - Image
    - OriginalFileName
falsepositives:
    - IT backup scripts copying user Outlook data during migrations or system upgrades.
    - Local desktop search indexers or antivirus software scanning local mail folders.
level: high
tags:
    - attack.collection
    - attack.t1114
    - analytics.an1309
    - detection.det0476
```

### OCSF Normalized
```xml
title: Collection - Local Email Database Access and Collection (OCSF-Normalized)
id: e0be8fe4-ec61-4434-ae75-81f12e9e3a67
status: experimental
description: Detects local email database access and copying, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    # Based on OCSF mapping, process path is process.file.path and cmd line is process.cmd_line
    selection_utilities:
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\xcopy.exe'
            - '\robocopy.exe'
    selection_email_extensions:
        process.cmd_line|contains:
            - '.pst'
            - '.ost'
    filter_outlook:
        process.file.path|endswith:
            - '\outlook.exe'
    condition: selection_utilities and selection_email_extensions and not filter_outlook
fields:
    - process.cmd_line
    - process.file.path
falsepositives:
    - Legitimate IT migrations copying outlook files.
level: high
tags:
    - attack.collection
    - attack.t1114
```

---

### **3. Blind Spots & Tuning (The "Problems")**

* **Problem 1: Migration and Desktop Backup Tools**
    * *The Issue:* Enterprise desktop backup agents (like CrashPlan, Druva, or local scripts) copy `.pst`/`.ost` files daily, generating false positives.
    * *The Fix:* Whitelist approved backup service accounts and specific signed backup agent binaries.

* **Problem 2: Reading Files Without Extension (Bypass)**
    * *The Issue:* Attackers can rename the `.pst` or `.ost` file to a generic extension (e.g. `.dat` or `.tmp`) prior to copying or archiving, escaping simple extension checks.
    * *The Fix:* Monitor file access events (Sysmon Event ID 11) for processes accessing the default Outlook folder (`%USERPROFILE%\AppData\Local\Microsoft\Outlook\`) regardless of the target file extension.

* **Problem 3: Outlook API Scripting**
    * *The Issue:* Adversaries can write VBA macros or scripting hosts utilizing Outlook COM objects to programmatically export mails without directly calling command-line utilities.
    * *The Fix:* Monitor Outlook COM object initialization or execution of Office macros accessing external mail objects.
