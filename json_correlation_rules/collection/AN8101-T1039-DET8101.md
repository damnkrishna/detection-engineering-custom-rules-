### 1. Technique Breakdown: T1039

* What it is: Detection logic tailored for MITRE ATT&CK technique T1039 under the collection tactic.
* Log Source Requirements: Dependent on rule implementation (e.g., Sysmon Event ID 1, Windows Security 4688).
* Coverage Check:
  - HIDS (Sysmon + Windows Defender): YES
  - NIDS (Suricata + Zeek): PARTIAL
* Log Sources Covered:
  - Multi-stage correlation logic
* Conclusion: This complex behavior can be tracked natively through multi-stage correlation.

---

### 2. Custom Sigma Rule

Extracted Correlation Logic (AN8101):
```json
{
  "id": "USER_DISTINCT_SHARE_SWEEP",
  "description": "Single user accesses \u226510 distinct network share paths within 10 minutes \u2014 automated collection or insider staging, behavior-only (T1039, T1135)",
  "correlationKey": "username",
  "scoreMultiplier": 2.5,
  "windowMinutes": 10,
  "sensorFilter": [
    "HIDS"
  ],
  "stages": [
    {
      "label": "share_sweep",
      "tactics": [
        "TA0009",
        "TA0007"
      ],
      "techniques": [
        "T1039",
        "T1135"
      ],
      "conditions": [
        {
          "field": "tactic",
          "op": "IN",
          "values": [
            "TA0009",
            "TA0007"
          ]
        }
      ],
      "optional": false,
      "minOccurrences": 10,
      "signatureEquals": [
        "4663",
        "object access",
        "network share",
        "smb read",
        "UNC path accessed",
        "mapped drive",
        "net use",
        "share access"
      ]
    }
  ],
  "requireDistinctField": "entityIp"
}
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Legitimate Administrative Tools
  * The Issue: IT Administrators perform routine tasks that overlap with this technique.
  * The Fix: Filter benign activity using `Image|startswith: 'C:\Program Files\'`.
