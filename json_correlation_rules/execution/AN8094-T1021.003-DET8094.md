### 1. Technique Breakdown: T1021.003

* What it is: Detection logic tailored for MITRE ATT&CK technique T1021.003 under the execution tactic.
* Log Source Requirements: Dependent on rule implementation (e.g., Sysmon Event ID 1, Windows Security 4688).
* Coverage Check:
  - HIDS (Sysmon + Windows Defender): YES
  - NIDS (Suricata + Zeek): PARTIAL
* Log Sources Covered:
  - Multi-stage correlation logic
* Conclusion: This complex behavior can be tracked natively through multi-stage correlation.

---

### 2. Custom Sigma Rule

Extracted Correlation Logic (AN8094):
```json
{
  "id": "WMI_PS_LATERAL",
  "description": "WMI or PowerShell Remoting lateral movement \u2014 bypasses SMB/NTLM-only lateral patterns. T1021.003 (DCOM), T1021.006 (WinRM), T1047 (WMI).",
  "correlationKey": "username",
  "scoreMultiplier": 3.0,
  "windowMinutes": 60,
  "sensorFilter": [
    "HIDS"
  ],
  "stages": [
    {
      "label": "wmi_or_psremote",
      "techniques": [
        "T1021.003",
        "T1021.006",
        "T1047"
      ],
      "optional": false,
      "minOccurrences": 2,
      "signatureEquals": [
        "wmic /node",
        "Win32_Process Create",
        "Invoke-WmiMethod",
        "Invoke-Command -ComputerName",
        "Enter-PSSession",
        "WinRM",
        "5985",
        "5986",
        "WSMan",
        "DCOM",
        "lateral WMI",
        "remote WMI"
      ]
    },
    {
      "label": "remote_execution",
      "conditions": [
        {
          "field": "tactic",
          "op": "IN",
          "values": [
            "TA0002",
            "TA0003",
            "TA0004"
          ]
        }
      ],
      "optional": false,
      "maxGapSeconds": 600
    }
  ],
  "requireDistinctField": "entityIp"
}
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Installer Noise and Generic Admin Activity
  * The Issue: Software deployments via SCCM or Ansible often trigger execution/evasion alerts.
  * The Fix: Filter by `ParentImage|endswith: '\msiexec.exe'` or `\setup.exe`.
