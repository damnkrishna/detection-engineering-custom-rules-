### 1. Technique Breakdown: T1071

* What it is: Detection logic tailored for MITRE ATT&CK technique T1071 under the execution tactic.
* Log Source Requirements: Dependent on rule implementation (e.g., Sysmon Event ID 1, Windows Security 4688).
* Coverage Check:
  - HIDS (Sysmon + Windows Defender): YES
  - NIDS (Suricata + Zeek): PARTIAL
* Log Sources Covered:
  - Multi-stage correlation logic
* Conclusion: This complex behavior can be tracked natively through multi-stage correlation.

---

### 2. Custom Sigma Rule

Extracted Correlation Logic (AN8100):
```json
{
  "id": "RARE_PROC_NET_OUTBOUND",
  "description": "Non-browser signed-binary process makes first-ever outbound connection on non-standard port \u2014 implant or LOLBin C2 first-time activation (T1071, T1095)",
  "correlationKey": "entityIp",
  "scoreMultiplier": 3.0,
  "windowMinutes": 30,
  "sensorFilter": [
    "HIDS",
    "ZEEK"
  ],
  "stages": [
    {
      "label": "unusual_proc_start",
      "tactics": [
        "TA0002"
      ],
      "conditions": [
        {
          "field": "tactic",
          "op": "IN",
          "values": [
            "TA0002"
          ]
        },
        {
          "field": "sensor",
          "op": "IN",
          "values": [
            "HIDS"
          ]
        }
      ],
      "signatureContains": [
        "mshta.exe",
        "wscript.exe",
        "cscript.exe",
        "regsvr32.exe",
        "rundll32.exe",
        "forfiles.exe",
        "pcalua.exe",
        "odbcconf.exe",
        "msdt.exe",
        "first seen process",
        "unusual process"
      ],
      "optional": false
    },
    {
      "label": "outbound_uncommon_port",
      "tactics": [
        "TA0011"
      ],
      "conditions": [
        {
          "field": "tactic",
          "op": "IN",
          "values": [
            "TA0011"
          ]
        },
        {
          "field": "sensor",
          "op": "IN",
          "values": [
            "ZEEK"
          ]
        }
      ],
      "signatureContains": [
        "unusual port",
        "non-standard port",
        "outbound connection",
        "uncommon destination",
        "new connection"
      ],
      "optional": false,
      "maxGapSeconds": 300
    }
  ]
}
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Installer Noise and Generic Admin Activity
  * The Issue: Software deployments via SCCM or Ansible often trigger execution/evasion alerts.
  * The Fix: Filter by `ParentImage|endswith: '\msiexec.exe'` or `\setup.exe`.
