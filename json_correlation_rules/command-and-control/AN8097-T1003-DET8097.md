### 1. Technique Breakdown: T1003

* What it is: Detection logic tailored for MITRE ATT&CK technique T1003 under the command-and-control tactic.
* Log Source Requirements: Dependent on rule implementation (e.g., Sysmon Event ID 1, Windows Security 4688).
* Coverage Check:
  - HIDS (Sysmon + Windows Defender): YES
  - NIDS (Suricata + Zeek): PARTIAL
* Log Sources Covered:
  - Multi-stage correlation logic
* Conclusion: This complex behavior can be tracked natively through multi-stage correlation.

---

### 2. Custom Sigma Rule

Extracted Correlation Logic (AN8097):
```json
{
  "id": "NIDS_C2_HIDS_CRED_DUMP",
  "description": "Cross-sensor: NIDS detects C2 beacon + HIDS detects credential dumping on same host \u2014 active post-exploitation operator confirmed, not automated malware",
  "correlationKey": "entityIp",
  "scoreMultiplier": 4.5,
  "windowMinutes": 120,
  "sensorFilter": [
    "NIDS",
    "HIDS"
  ],
  "stages": [
    {
      "label": "nids_c2_beacon",
      "tactics": [
        "TA0011"
      ],
      "conditions": [
        {
          "field": "sensor",
          "op": "IN",
          "values": [
            "NIDS"
          ]
        }
      ],
      "optional": false
    },
    {
      "label": "hids_cred_dump",
      "techniques": [
        "T1003"
      ],
      "conditions": [
        {
          "field": "tactic",
          "op": "IN",
          "values": [
            "TA0006"
          ]
        },
        {
          "field": "sensor",
          "op": "IN",
          "values": [
            "HIDS"
          ]
        }
      ],
      "optional": false,
      "maxGapSeconds": 7200
    }
  ]
}
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Legitimate Administrative Tools
  * The Issue: IT Administrators perform routine tasks that overlap with this technique.
  * The Fix: Filter benign activity using `Image|startswith: 'C:\Program Files\'`.
