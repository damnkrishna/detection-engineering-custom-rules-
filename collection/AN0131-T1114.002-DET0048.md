### 1. Technique Breakdown: T1114.002

* What it is: Email Collection: Remote Email Collection. Adversaries target and collect emails from remote mail servers (such as Microsoft Exchange or Office 365) using compromised credentials. They access mail databases programmatically via Exchange Web Services (EWS) API, MAPI, or PowerShell command blocks (using Exchange cmdlets like `Export-Mailbox` or EWS managed APIs). Host-based detection checks for unexpected processes (like scripting engines) loading mail API assemblies or executing exchange query cmdlets.
* Log Source Requirements: Windows Security Event Log Event ID 4624 (Logon) or Sysmon process creation logs (Event ID 1) combined with PowerShell script block logs (Event ID 4104).
* Coverage Check:
    * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 logs process details, and PowerShell logging captures the script blocks compiling mail retrieval commands.
    * NIDS (Suricata + Zeek): YES. Network filters capture SSL connections to remote mail servers (e.g. `outlook.office365.com`), but cannot easily decode HTTPS encrypted API payloads.

---

### 2. Custom Sigma Rule

```yaml
title: Collection - Remote Email Collection via Exchange API Query (AN0131)
id: c0b55e4e-2d95-44cf-8f9a-2001142e3cdf
status: experimental
description: Detects command-line parameters or PowerShell script blocks indicating remote email collection via Exchange Web Services (EWS) or Exchange Management cmdlets, indicating potential data theft.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1 / PowerShell Event 4104
detection:
    selection_cmd:
        CommandLine|contains:
            - 'Microsoft.Exchange.WebServices.Data'
            - 'Export-Mailbox'
            - 'Search-Mailbox'
            - 'Get-MailboxFolderStatistics'
            - 'ExchangeService'
    selection_powershell:
        EventID: 4104
        ScriptBlockText|contains:
            - 'Microsoft.Exchange.WebServices.Data.ExchangeService'
            - 'Get-Mailbox'
            - 'Export-Mailbox'
            - 'Search-Mailbox'
    condition: selection_cmd or selection_powershell
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Exchange administrators running routine mailbox cleanups or compliance exports.
level: high
tags:
    - attack.collection
    - attack.t1114.002
    - analytic.an0131
    - detection.det0048
```

---

### 3. OCSF Normalized Rule

```yaml
title: 'Collection - Remote Email Collection via Exchange API Query (AN0131) (OCSF-Normalized)'
id: 'c0b55e4e-2d95-44cf-8f9a-2001142e3cdf'
status: experimental
description: 'OCSF Normalized rule for Collection - Remote Email Collection via Exchange API Query (AN0131)'
author: Krishna Gupta
logsource:
    product: ocsf
    class: process_activity
detection:
    selection_cmd:
        process.cmd_line|contains:
            - 'Microsoft.Exchange.WebServices.Data'
            - 'Export-Mailbox'
            - 'Search-Mailbox'
            - 'Get-MailboxFolderStatistics'
            - 'ExchangeService'
    selection_powershell:
        event_id: 4104
        unmapped.script_block_text|contains:
            - 'Microsoft.Exchange.WebServices.Data.ExchangeService'
            - 'Get-Mailbox'
            - 'Export-Mailbox'
            - 'Search-Mailbox'
    condition: selection_cmd or selection_powershell
fields:
    - time
level: high
tags:
    - attack.collection
    - attack.t1114.002
    - analytic.an0131
    - detection.det0048
```

---

### 4. Blind Spots & Tuning (The "Problems")

- Authorized IT administration activities.
- Tune with allowlists for known trusted execution paths.

---

### 5. Test Cases (Engine Pipeline Verification)

#### True Positives
* True Positive 1A: Standard Execution (Collection - Remote Email Collection via Exchange API Query (AN0131))
  * The Log: Activity simulating malicious behavior.
  * Expected Outcome: MUST trigger an alert.

* True Positive 1B: Malformed JSON Robustness Test (Collection - Remote Email Collection via Exchange API Query (AN0131))
  * The Log: Simulating 1A but intentionally omitting the `unmapped` object entirely.
  * Expected Outcome: Engine parser should degrade gracefully. MUST trigger an alert.

* True Positive 1C: Schema Type Validation Failure (Collection - Remote Email Collection via Exchange API Query (AN0131))
  * The Log: Exact match of 1A, but `activity_id` is passed as a string `"1"` instead of an integer `1`.
  * Expected Outcome: Tests if the engine's JSON parser coerces the type gracefully. MUST trigger an alert.

* True Positive 1D: Exact Duplicate Event (Collection - Remote Email Collection via Exchange API Query (AN0131))
  * The Log: Exact duplicate of 1A with the exact same timestamp.
  * Expected Outcome: Tests deduplication window.

#### Benign / True Negatives
* Benign 1: Allowed Activity (Collection - Remote Email Collection via Exchange API Query (AN0131))
  * The Log: Activity matching legitimate patterns.
  * Expected Outcome: Matches exclusion filters. MUST NOT trigger an alert.

---

### 6. Raw Telemetry Dataset (OCSF JSON)

#### Benign Telemetry
```jsonl
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Windows\\System32\\cmd.exe", "name": "cmd.exe"}, "cmd_line": "cmd.exe"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"generic": "benign_test"}, "metadata": {"test_case": "TN-01", "description": "Collection - Remote Email Collection via Exchange API Query (AN0131) (TN-01)", "expected_alert": false, "rule_uid": "c0b55e4e-2d95-44cf-8f9a-2001142e3cdf"}, "activity_id": 1, "time": 1718000010}
```

#### True Positive Telemetry
```jsonl
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Windows\\System32\\cmd.exe", "name": "cmd.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c Microsoft.Exchange.WebServices.Data"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"script_block_text": "Invoke-Command Microsoft.Exchange.WebServices.Data.ExchangeService"}, "metadata": {"test_case": "TP-01A", "description": "Collection - Remote Email Collection via Exchange API Query (AN0131) (TP-01A)", "expected_alert": true, "rule_uid": "c0b55e4e-2d95-44cf-8f9a-2001142e3cdf"}, "activity_id": 1, "time": 1718000100}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Windows\\System32\\cmd.exe", "name": "cmd.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c Microsoft.Exchange.WebServices.Data"}, "actor": {"user": {"name": "TargetUser"}}, "metadata": {"test_case": "TP-01B", "description": "Collection - Remote Email Collection via Exchange API Query (AN0131) (TP-01B)", "expected_alert": true, "rule_uid": "c0b55e4e-2d95-44cf-8f9a-2001142e3cdf"}, "activity_id": 1, "time": 1718000101}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Windows\\System32\\cmd.exe", "name": "cmd.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c Microsoft.Exchange.WebServices.Data"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"script_block_text": "Invoke-Command Microsoft.Exchange.WebServices.Data.ExchangeService"}, "metadata": {"test_case": "TP-01C", "description": "Collection - Remote Email Collection via Exchange API Query (AN0131) (TP-01C)", "expected_alert": true, "rule_uid": "c0b55e4e-2d95-44cf-8f9a-2001142e3cdf"}, "activity_id": "1", "time": 1718000102}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Windows\\System32\\cmd.exe", "name": "cmd.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c Microsoft.Exchange.WebServices.Data"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"script_block_text": "Invoke-Command Microsoft.Exchange.WebServices.Data.ExchangeService"}, "metadata": {"test_case": "TP-01D", "description": "Collection - Remote Email Collection via Exchange API Query (AN0131) (TP-01D)", "expected_alert": true, "rule_uid": "c0b55e4e-2d95-44cf-8f9a-2001142e3cdf"}, "activity_id": 1, "time": 1718000100}
```
