### 1. Technique Breakdown: T1505.004

* What it is: IIS Components. Adversaries install malicious Internet Information Services (IIS) components — including ISAPI filters, ISAPI extensions, or IIS native modules — to achieve persistent code execution on Windows web servers. These components are DLLs that the IIS worker process (w3wp.exe) loads automatically, intercepting or manipulating HTTP requests and responses. Adversaries use the IIS administration tool appcmd.exe to register the DLL, or directly modify IIS configuration files (applicationHost.config).
* Log Source Requirements: Endpoint logs capturing process creation events and file creation events in web server directories.
* Coverage Check:
  - HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 captures appcmd.exe or PowerShell module registrations. Event ID 11 captures DLL file drops in system folders.
  - NIDS (Suricata + Zeek): NO. Host-local configuration changes.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 11 (file_event)
* Log Sources Missing / Unused:
  - None.

---

### 2. Custom Sigma Rule

The detection logic is split into process creation monitoring of registration tools, and file event monitoring of DLL drops to IIS directories.

Rule 1 of 2: Malicious IIS Module or ISAPI Filter Installation (Process Creation)
```yaml
title: Malicious IIS Module or ISAPI Filter Installation (AN0184)
id: 67145796-ad3c-459b-aa04-a821af14396b
status: experimental
description: Detects adversarial installation of malicious IIS components (native modules, ISAPI filters/extensions) via appcmd.exe or PowerShell web configuration cmdlets.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
detection:
    selection_appcmd:
        - Image|endswith: '\appcmd.exe'
        - OriginalFileName: 'appcmd.exe'
    selection_appcmd_install:
        CommandLine|contains:
            - ' install '
            - ' add '
            - 'module'
            - 'isapi'
            - 'filter'
            - 'handler'
    selection_powershell:
        - Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
        - OriginalFileName:
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_ps_iis:
        CommandLine|contains:
            - 'New-WebConfiguration'
            - 'Add-WebConfiguration'
            - 'isapi'
            - 'GlobalModules'
            - 'system.webServer/modules'
    filter_installer:
        ParentImage|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: ((selection_appcmd and selection_appcmd_install) or (selection_powershell and selection_ps_iis)) and not filter_installer
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Legitimate IIS module installation by administrators during upgrades.
level: high
tags:
    - attack.persistence
    - attack.t1505.004
    - analytic.an0184
    - detection.det0068
```

Rule 2 of 2: Malicious IIS Component File Drop (File Event)
```yaml
title: Malicious IIS Component File Drop (AN0184-B)
id: ad6cb72e-8c2b-4e0b-8d7a-b5da38cf7ba8
status: experimental
description: Detects drops of new DLL or config files to IIS system directories, representing the dropping phase of a malicious IIS native module or filter.
author: Krishna Gupta
date: 2026-06-17
logsource:
    category: file_event
    product: windows
detection:
    selection_paths:
        TargetFilename|contains:
            - '\System32\inetsrv\'
            - '\inetsrv\config\'
    selection_extensions:
        TargetFilename|endswith:
            - '.dll'
            - '.config'
    condition: selection_paths and selection_extensions
fields:
    - TargetFilename
    - Image
    - User
falsepositives:
    - Software updates or deployment scripts updating IIS configuration files.
level: high
tags:
    - attack.persistence
    - attack.t1505.004
    - analytic.an0184
    - detection.det0068
```

### OCSF Normalized

Rule 1 of 2: Malicious IIS Module or ISAPI Filter Installation (Process Activity)
```xml
title: Malicious IIS Module or ISAPI Filter Installation (AN0184) (OCSF-Normalized)
id: 67145796-ad3c-459b-aa04-a821af14396b
status: experimental
description: Detects appcmd.exe module installation or PowerShell IIS configuration changes, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_appcmd:
        process.file.path|endswith: '\appcmd.exe'
    selection_appcmd_args:
        process.cmd_line|contains:
            - ' install '
            - ' add '
            - 'module'
            - 'isapi'
            - 'filter'
    selection_powershell:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
    selection_ps_iis:
        process.cmd_line|contains:
            - 'New-WebConfiguration'
            - 'Add-WebConfiguration'
            - 'GlobalModules'
            - 'system.webServer/modules'
    filter_installer:
        unmapped.parent_image|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: ((selection_appcmd and selection_appcmd_args) or (selection_powershell and selection_ps_iis)) and not filter_installer
fields:
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Legitimate IIS administrators deploying approved web application modules.
level: high
tags:
    - attack.persistence
    - attack.t1505.004
```

Rule 2 of 2: Malicious IIS Component File Drop (File Activity)
```xml
title: Malicious IIS Component File Drop (OCSF-Normalized) - Rule 2 of 2
id: ad6cb72e-8c2b-4e0b-8d7a-b5da38cf7ba8
status: experimental
description: Detects drops of DLL or config files to IIS system directories, mapped to OCSF file_activity.
author: Krishna Gupta
date: 2026-06-17
logsource:
    category: system
    product: ocsf
    class: file_activity
detection:
    selection_paths:
        file.path|contains:
            - '\System32\inetsrv\'
            - '\inetsrv\config\'
    selection_extensions:
        file.name|endswith:
            - '.dll'
            - '.config'
    condition: selection_paths and selection_extensions
fields:
    - file.name
    - file.path
    - process.file.path
    - actor.user.name
falsepositives:
    - Standard admin registry updates or configuration changes.
level: high
tags:
    - attack.persistence
    - attack.t1505.004
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Direct applicationHost.config Modification
  - The Issue: Adversaries may directly modify the IIS config file without invoking appcmd.exe.
  - The Fix: Monitor file changes on applicationHost.config via file audit logs.

* Problem 2: Malicious DLL Dropped but Not Yet Registered
  - The Issue: DLL drops may happen before registration commands are executed.
  - The Fix: The file drop rule monitors System32\inetsrv folder additions.

* Problem 3: IIS Worker Process Anomalous Behavior
  - The Issue: Once registered, code executes inside trusted w3wp.exe process.
  - The Fix: Establish a behavioral baseline for w3wp.exe.
