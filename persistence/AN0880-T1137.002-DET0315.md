# AN0880 - Office Application Startup: Office Test (T1137.002)

## Technique Breakdown
Adversaries can achieve persistence by modifying the `Office Test` registry key. When an Office application starts, it checks this key, and if configured, it will load the specified DLL. This allows attackers to silently load a malicious payload whenever a user opens an Office application like Word or Excel. Detection focuses on monitoring the creation or modification of the `Office Test` key.

## Custom Sigma Rule
```yaml
title: 'AN0880 - Office Application Startup: Office Test'
id: 'AN0880'
status: experimental
description: 'Detects the creation or modification of the Microsoft Office Test registry key, a known persistence mechanism used to load arbitrary DLLs.'
author: Krishna Gupta
logsource:
  product: windows
  category: registry_set
detection:
  selection_registry:
    TargetObject|contains: '\Software\Microsoft\Office Test\Special\Perf'
  condition: selection_registry
fields:
  - EventID
  - TargetObject
  - Image
  - Details
level: high
tags:
  - attack.persistence
  - attack.t1137.002
  - analytic.an0880
```

### OCSF Normalized Rule
```yaml
title: 'AN0880 - Office Application Startup: Office Test (OCSF)'
id: 'AN0880'
status: experimental
description: 'Detects the creation or modification of the Microsoft Office Test registry key, a known persistence mechanism used to load arbitrary DLLs.'
author: Krishna Gupta
logsource:
  product: windows
  category: registry_set
detection:
  selection_signature:
    signature_name|contains:
      - 'Office Test'
      - 'DET0315'
  condition: selection_signature
fields:
  - event_id
  - signature_name
  - unmapped.parent_image
  - unmapped.api_operation
  - actor.user.name
  - unmapped.granted_access
level: high
tags:
  - attack.persistence
  - attack.t1137.002
  - analytic.an0880
```

## Blind Spots & Tuning
- Extremely rare false positives. The `Office Test` key is typically not used in standard enterprise production environments outside of specific developer debugging.
- Legitimate software debugging tools may use this, so tune for known developer workstations if applicable.
