### 1. Technique Breakdown: T1189

* What it is: Drive-by Compromise. Adversaries gain initial access by exploiting a user's web browser when they visit a compromised or malicious website. Successful browser exploitation is identified by the browser process (e.g., Chrome, Edge, Firefox) directly spawning shell interpreters, scripting hosts, binary loaders, or downloading and launching payloads from temporary user directories.
* Log Source Requirements: Sysmon process creation logs (Event ID 1) capturing parent-child process relationships, and PowerShell script block logs (Event ID 4104).
* Coverage Check:
  * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 captures browser processes spawning shell/script hosts. Sysmon Event ID 4104 (Script Block) is effective at catching scripting activity containing browser context references.
  * NIDS (Suricata + Zeek): YES. Network monitoring can identify downloads of binary payloads with anomalous headers or from high-risk domains, but host-based process monitoring is needed to verify execution.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 4104 (ps_script)
* Log Sources Missing / Unused:
  - None.
* Conclusion: Drive-by compromise execution can be detected using process creation and PowerShell script block logs.

---

### 2. Custom Sigma Rule

The detection logic is split into web browser child process monitoring and PowerShell script block content monitoring.

Rule 1 of 2: Drive-by Compromise - Suspicious Child Process Spawned by Browser (Process Creation)
```yaml
title: Drive-by Compromise - Suspicious Child Process Spawned by Browser (AN0498-A)
id: 4bc0789f-5d87-43bb-86f3-637cef4eb0a0
status: experimental
description: Detects suspicious processes (e.g., cmd.exe, powershell.exe, wscript.exe, rundll32.exe, mshta.exe) spawned directly by web browser processes, which indicates browser exploitation or user opening dangerous downloaded files.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
detection:
    selection_parent:
        ParentImage|endswith:
            - '\chrome.exe'
            - '\msedge.exe'
            - '\firefox.exe'
            - '\brave.exe'
            - '\opera.exe'
            - '\iexplore.exe'
            - '\safari.exe'
    selection_child:
        - Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\rundll32.exe'
            - '\regsvr32.exe'
            - '\certutil.exe'
            - '\bitsadmin.exe'
            - '\curl.exe'
            - '\scrcons.exe'
        - OriginalFileName:
            - 'Cmd.Exe'
            - 'PowerShell.EXE'
            - 'wscript.exe'
            - 'cscript.exe'
            - 'mshta.exe'
            - 'rundll32.exe'
            - 'regsvr32.exe'
            - 'certutil.exe'
            - 'bitsadmin.exe'
            - 'curl.exe'
    condition: selection_parent and selection_child
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Legitimate web applications running on localhost (e.g., local development servers, admin dashboards) that spawn shell windows.
    - Automated browser-based test tools (e.g. Selenium, Puppeteer) spawning command prompts to run testing scripts.
level: high
tags:
    - attack.initial-access
    - attack.t1189
    - analytic.an0498
    - detection.det0176
```

Rule 2 of 2: Drive-by Compromise - PowerShell Script Block executing from Web Browser Parent (Script Block)
```yaml
title: Drive-by Compromise - PowerShell Script Block executing from Web Browser Parent (AN0498-B)
id: 9a17f58c-f886-4e86-8d69-dd311b18497e
status: experimental
description: Detects PowerShell script block logs executing commands associated with browser parent processes, suggesting browser-compromise execution.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: ps_script
    product: windows
detection:
    selection_cmd:
        ScriptBlockText|contains:
            - 'Invoke-Expression'
            - 'iex'
            - 'Invoke-WebRequest'
            - 'iwr'
            - 'Net.WebClient'
            - 'DownloadFile'
            - 'DownloadString'
    selection_browsers:
        ScriptBlockText|contains:
            - 'chrome'
            - 'msedge'
            - 'firefox'
            - 'brave'
            - 'opera'
            - 'iexplore'
    condition: selection_cmd and selection_browsers
fields:
    - ScriptBlockText
falsepositives:
    - Developer scripting tools or administrative tasks triggered locally.
level: medium
tags:
    - attack.initial-access
    - attack.t1189
    - analytic.an0498
    - detection.det0176
```

### OCSF Normalized

Rule 1 of 2: Drive-by Compromise - Suspicious Child Process Spawned by Browser (Process Activity)
```xml
title: Drive-by Compromise - Suspicious Child Process Spawned by Browser (OCSF-Normalized) - Rule 1 of 2
id: 4bc0789f-5d87-43bb-86f3-637cef4eb0a0
status: experimental
description: Detects suspicious child processes spawned by web browser applications, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_parent:
        actor.process.file.path|endswith:
            - '\chrome.exe'
            - '\msedge.exe'
            - '\firefox.exe'
            - '\iexplore.exe'
    selection_child:
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\rundll32.exe'
            - '\regsvr32.exe'
    condition: selection_parent and selection_child
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Development environments launching interactive shells from local browser pages.
level: high
tags:
    - attack.initial-access
    - attack.t1189
```

Rule 2 of 2: Drive-by Compromise - PowerShell Script Block executing from Web Browser Parent (Process Activity)
```xml
title: Drive-by Compromise - PowerShell Script Block executing from Web Browser Parent (OCSF-Normalized) - Rule 2 of 2
id: 9a17f58c-f886-4e86-8d69-dd311b18497e
status: experimental
description: Detects PowerShell script block logs executing commands associated with browser parents, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_cmd:
        process.cmd_line|contains:
            - 'Invoke-Expression'
            - 'Invoke-WebRequest'
            - 'Net.WebClient'
            - 'DownloadFile'
            - 'DownloadString'
    selection_browsers:
        process.cmd_line|contains:
            - 'chrome'
            - 'msedge'
            - 'firefox'
            - 'iexplore'
    condition: selection_cmd and selection_browsers
fields:
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Web browser development sessions spawning automated tests.
level: medium
tags:
    - attack.initial-access
    - attack.t1189
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Development Server localhost Exceptions
  * The Issue: Web developers run local services (like Jupyter Notebooks, Node.js development servers, or local admin web GUIs) that legitimately spawn command prompts or shell tools, causing excessive alerting.
  * The Fix: Add exclusions to whitelist process chains when the command line matches local-host development setups, or when the parent browser process was started with specific developer command-line options.

* Problem 2: Browser Extensions spawning helper applications
  * The Issue: Native messaging browser extensions (like password managers, security scanners, or smart-card signing extensions) legitimately spawn helper binaries, which may trigger generic parent-child alerts.
  * The Fix: Add explicit whitelist exclusions for the specific, signed binary paths associated with verified native-messaging browser extensions.

* Problem 3: Sandboxed Exploit Execution (No Child Process)
  * The Issue: If a browser exploit does not spawn a new process on the system, but executes shellcode completely inside the browser process memory space (e.g. executing a remote web socket connection within Chrome's sandboxed renderer process), parent-child process checks will see nothing.
  * The Fix: Implement system exploit mitigation monitoring (DEP/ASLR enforcement, AppLocker, or Windows Defender Exploit Guard events) to detect injection attempts, and monitor browser network sockets for connections to unusual ports.

