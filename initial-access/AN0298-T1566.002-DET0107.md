### 1. Technique Breakdown: T1566.002

* What it is: Phishing: Spearphishing Link. Adversaries deliver links in phishing emails or chat messages pointing to malicious resources (such as drive-by download sites, credential harvesting landing pages, or directly linking to malicious files hosted on cloud services). When a user clicks the link, the mail client or messaging app spawns the default system web browser with the URL in the command-line arguments.
* Log Source Requirements: Sysmon process creation logs (Event ID 1) capturing parent-child process chains, and PowerShell script block logs (Event ID 4104).
* Coverage Check:
  * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 captures parent process details (e.g., `outlook.exe`, `teams.exe`) spawning browser processes. Sysmon Event ID 4104 (Script Block) captures PowerShell scripts attempting to fetch external resources or parse links.
  * NIDS (Suricata + Zeek): YES. Network monitoring records HTTP GET requests to external hosts, but correlating the web traffic with the local application context requires host-based process data.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 4104 (ps_script)
* Log Sources Missing / Unused:
  - None.
* Conclusion: Spearphishing link execution can be detected using process creation and PowerShell script block logs.

---

### 2. Custom Sigma Rule

The detection logic is split into browser processes spawned by mail/chat apps and PowerShell scripts invoking download commands containing email link keywords.

Rule 1 of 2: Spearphishing Link - Browser Spawned by Mail/Chat App with Suspicious URL (Process Creation)
```yaml
title: Spearphishing Link - Browser Spawned by Mail/Chat App with Suspicious URL (AN0298-A)
id: 43f1108d-b079-4a5d-b151-6e2f9c631d6a
status: experimental
description: Detects default web browsers spawned by email clients or collaboration apps (Outlook, Teams, Discord, Slack) containing command-line arguments that reference direct file shares (e.g., file://, UNC paths) or suspicious downloadable file extensions.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
detection:
    selection_parent:
        ParentImage|endswith:
            - '\outlook.exe'
            - '\thunderbird.exe'
            - '\teams.exe'
            - '\slack.exe'
            - '\discord.exe'
            - '\Telegram.exe'
    selection_browser:
        Image|endswith:
            - '\chrome.exe'
            - '\msedge.exe'
            - '\firefox.exe'
            - '\brave.exe'
            - '\opera.exe'
    selection_args:
        CommandLine|contains:
            - 'file://'
            - '\\\\' # UNC path
            - '.zip'
            - '.iso'
            - '.img'
            - '.lnk'
            - '.bat'
            - '.cmd'
            - '.vbs'
            - '.exe'
            - '.scr'
    condition: selection_parent and selection_browser and selection_args
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Users clicking links in emails to download legitimate company files hosted on internal file servers or official repositories.
level: medium
tags:
    - attack.initial-access
    - attack.t1566.002
    - analytic.an0298
    - detection.det0107
```

Rule 2 of 2: Spearphishing Link - Script Block executing Email Link Parser (Script Block)
```yaml
title: Spearphishing Link - Script Block executing Email Link Parser (AN0298-B)
id: 7f689728-ebdc-4958-a302-ee5c6df01818
status: experimental
description: Detects PowerShell script block logs executing commands associated with parsing emails, extracting links, or downloading contents from links (using Invoke-WebRequest or Net.WebClient).
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: ps_script
    product: windows
detection:
    selection_cmd:
        ScriptBlockText|contains:
            - 'Invoke-WebRequest'
            - 'iwr '
            - 'Invoke-RestMethod'
            - 'irm '
            - 'Net.WebClient'
            - 'DownloadFile'
            - 'DownloadString'
    selection_keywords:
        ScriptBlockText|contains:
            - 'outlook'
            - 'mail'
            - 'link'
            - 'url'
            - 'click'
    condition: selection_cmd and selection_keywords
fields:
    - ScriptBlockText
falsepositives:
    - Legitimate automation scripts retrieving information from web servers using URL-related parameters.
level: medium
tags:
    - attack.initial-access
    - attack.t1566.002
    - analytic.an0298
    - detection.det0107
```

### OCSF Normalized

Rule 1 of 2: Spearphishing Link - Browser Spawned by Mail/Chat App with Suspicious URL (Process Activity)
```xml
title: Spearphishing Link - Browser Spawned by Mail/Chat App with Suspicious URL (OCSF-Normalized) - Rule 1 of 2
id: 43f1108d-b079-4a5d-b151-6e2f9c631d6a
status: experimental
description: Detects default web browsers spawned by email clients or collaboration apps containing command-line arguments pointing to file downloads or shares, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_parent:
        actor.process.file.path|endswith:
            - '\outlook.exe'
            - '\teams.exe'
            - '\slack.exe'
            - '\discord.exe'
    selection_browser:
        process.file.path|endswith:
            - '\chrome.exe'
            - '\msedge.exe'
            - '\firefox.exe'
    selection_args:
        process.cmd_line|contains:
            - 'file://'
            - '\\\\'
            - '.zip'
            - '.iso'
            - '.lnk'
            - '.exe'
    condition: selection_parent and selection_browser and selection_args
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Clicking corporate document links pointing to local file share portals.
level: medium
tags:
    - attack.initial-access
    - attack.t1566.002
```

Rule 2 of 2: Spearphishing Link - Script Block executing Email Link Parser (Process Activity)
```xml
title: Spearphishing Link - Script Block executing Email Link Parser (OCSF-Normalized) - Rule 2 of 2
id: 7f689728-ebdc-4958-a302-ee5c6df01818
status: experimental
description: Detects PowerShell script block logs executing commands associated with parsing emails or downloading links, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_cmd:
        process.cmd_line|contains:
            - 'Invoke-WebRequest'
            - 'Invoke-RestMethod'
            - 'Net.WebClient'
            - 'DownloadFile'
            - 'DownloadString'
    selection_keywords:
        process.cmd_line|contains:
            - 'outlook'
            - 'mail'
            - 'link'
            - 'url'
    condition: selection_cmd and selection_keywords
fields:
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Automated business reports retrieving links from files or emails.
level: medium
tags:
    - attack.initial-access
    - attack.t1566.002
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: HTTP Redirects and URL Shorteners
  * The Issue: Attackers hide the final payload URL behind url shorteners (e.g., bit.ly) or open redirects. The browser command-line will only show the shortened URL, bypassing any string checks for file extensions.
  * The Fix: Correlate process launching with web proxy logs that trace HTTP redirection paths, or inspect subsequent browser file writes to disk.

* Problem 2: Browser Web App SSO / Internal Links
  * The Issue: Office/Outlook routinely opens browser sessions for authentication (SSO) or Microsoft system pages, which can contain parameters that look complex or trigger generic string-matching filters.
  * The Fix: Specifically exclude trusted enterprise SSO domains (e.g., `*.microsoftonline.com`, `*.okta.com`) from the command-line query alerts.

* Problem 3: Standard Browser Session Reuse
  * The Issue: If Chrome is already running, clicking a link in Outlook might not spawn a new `chrome.exe` process. Instead, Outlook sends an IPC message to the existing Chrome instance, which opens a new tab. In this case, no process creation event is generated for the new URL.
  * The Fix: Deploy endpoint browser extension logging or query local browser history files and proxy logs to detect the URL visits, as host process auditing will not see the tab creation.

