### 1. Technique Breakdown: T1190

* What it is: Exploit Public-Facing Application. Adversaries exploit vulnerabilities in internet-facing web servers (such as IIS, Apache Tomcat, Exchange, or Java-based web portals) to run arbitrary code on the host. This post-exploitation activity routinely starts with the web server process spawning shell environments (like cmd or PowerShell) or admin utilities (like `whoami.exe`, `ping.exe`, or `net.exe`) to perform initial reconnaissance or drop web shells.
* Log Source Requirements: Sysmon process creation logs (Event ID 1) tracking parent-child process chains, and Sysmon network connection logs (Event ID 3).
* Coverage Check:
  * HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 records parent process paths (e.g., `w3wp.exe`, `tomcat.exe`, `java.exe`) spawning child processes. Sysmon Event ID 3 records network connections initiated by these processes. Wazuh parses and raises high-severity alerts.
  * NIDS (Suricata + Zeek): YES. Network monitoring can identify exploitation attempts (e.g. SQLi, directory traversal) in HTTP payloads, but host-based logs are critical to confirm post-exploit execution success.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 3 (network_connection)
* Log Sources Missing / Unused:
  - None.
* Conclusion: Exploitation of public-facing applications can be detected using process creation and network connection logs.

---

### 2. Custom Sigma Rule

The detection logic is split into command-line process relationship monitoring and network connection monitoring.

Rule 1 of 2: Exploit Public-Facing Application - Suspicious Child Process of Web Server (Process Creation)
```yaml
title: Exploit Public-Facing Application - Suspicious Child Process of Web Server (AN0219-A)
id: eec27a76-4b82-4220-bf16-e478d2f9ed36
status: experimental
description: Detects command-line interpreters or administrative utilities spawned by web server processes (e.g., IIS, Tomcat, Apache, Java web services), suggesting successful exploitation or web shell deployment.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
detection:
    selection_parent:
        ParentImage|endswith:
            - '\w3wp.exe'
            - '\httpd.exe'
            - '\nginx.exe'
            - '\tomcat.exe'
            - '\tomcat7.exe'
            - '\tomcat8.exe'
            - '\tomcat9.exe'
            - '\tomcat10.exe'
            - '\httpd.exe'
            - '\java.exe'
            - '\javaw.exe'
    selection_child:
        - Image|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\sh.exe'
            - '\bash.exe'
            - '\whoami.exe'
            - '\net.exe'
            - '\nltest.exe'
            - '\systeminfo.exe'
            - '\ipconfig.exe'
            - '\arp.exe'
            - '\curl.exe'
        - OriginalFileName:
            - 'Cmd.Exe'
            - 'PowerShell.EXE'
            - 'wscript.exe'
            - 'cscript.exe'
            - 'whoami.exe'
            - 'net.exe'
            - 'nltest.exe'
            - 'systeminfo.exe'
            - 'ipconfig.exe'
            - 'arp.exe'
            - 'curl.exe'
    condition: selection_parent and selection_child
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Legitimate administrative scripts triggered by web application dashboards (e.g., automated server backup scripts triggered from an admin panel).
level: critical
tags:
    - attack.initial-access
    - attack.t1190
    - analytic.an0219
    - detection.det0080
```

Rule 2 of 2: Exploit Public-Facing Application - Outbound Network Connection from Web Server (Network Connection)
```yaml
title: Exploit Public-Facing Application - Outbound Network Connection from Web Server (AN0219-B)
id: ea6a62c8-4f78-4cf1-a47c-6a718b773c04
status: experimental
description: Detects outbound network connections initiated by web server processes, suggesting potential reverse shell or payload download activity following exploitation.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: network_connection
    product: windows
detection:
    selection_parent:
        Image|endswith:
            - '\w3wp.exe'
            - '\tomcat.exe'
            - '\tomcat7.exe'
            - '\tomcat8.exe'
            - '\tomcat9.exe'
            - '\tomcat10.exe'
            - '\httpd.exe'
            - '\nginx.exe'
            - '\java.exe'
            - '\javaw.exe'
    selection_network:
        Initiated: 'true'
    filter_local:
        DestinationIp|startswith:
            - '127.'
            - '169.254.'
            - '10.'
            - '172.16.'
            - '172.17.'
            - '172.18.'
            - '172.19.'
            - '172.20.'
            - '172.21.'
            - '172.22.'
            - '172.23.'
            - '172.24.'
            - '172.25.'
            - '172.26.'
            - '172.27.'
            - '172.28.'
            - '172.29.'
            - '172.30.'
            - '172.31.'
            - '192.168.'
            - 'fe80:'
            - '::1'
    condition: selection_parent and selection_network and not filter_local
fields:
    - Image
    - CommandLine
    - DestinationIp
    - DestinationPort
    - User
falsepositives:
    - Web applications designed to contact external services, APIs, or database servers.
level: high
tags:
    - attack.initial-access
    - attack.t1190
    - analytic.an0219
    - detection.det0080
```

### OCSF Normalized

Rule 1 of 2: Exploit Public-Facing Application - Suspicious Child Process of Web Server (Process Activity)
```xml
title: Exploit Public-Facing Application - Suspicious Child Process of Web Server (OCSF-Normalized) - Rule 1 of 2
id: eec27a76-4b82-4220-bf16-e478d2f9ed36
status: experimental
description: Detects command interpreters or admin tools spawned by web servers, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_parent:
        actor.process.file.path|endswith:
            - '\w3wp.exe'
            - '\tomcat.exe'
            - '\nginx.exe'
            - '\java.exe'
    selection_child:
        process.file.path|endswith:
            - '\cmd.exe'
            - '\powershell.exe'
            - '\whoami.exe'
            - '\net.exe'
            - '\systeminfo.exe'
    condition: selection_parent and selection_child
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Legitimate web applications calling shell programs as part of backend tasks (requires strict path exclusions).
level: critical
tags:
    - attack.initial-access
    - attack.t1190
```

Rule 2 of 2: Exploit Public-Facing Application - Outbound Network Connection from Web Server (Network Activity)
```xml
title: Exploit Public-Facing Application - Outbound Network Connection from Web Server (OCSF-Normalized) - Rule 2 of 2
id: ea6a62c8-4f78-4cf1-a47c-6a718b773c04
status: experimental
description: Detects outbound network connections initiated by web server processes, mapped to OCSF network_activity.
author: Krishna Gupta
date: 2026-06-16
logsource:
    category: system
    product: ocsf
    class: network_activity
detection:
    selection_process:
        process.file.path|endswith:
            - '\w3wp.exe'
            - '\tomcat.exe'
            - '\nginx.exe'
            - '\java.exe'
    selection_network:
        connection_info.direction: 'outbound'
    condition: selection_process and selection_network
fields:
    - process.file.path
    - activity.http_request.host
    - activity.dst_endpoint.ip
    - activity.dst_endpoint.port
    - activity.src_endpoint.ip
    - activity.src_endpoint.port
falsepositives:
    - Outbound connections to external databases or APIs required for legitimate application functionality.
level: high
tags:
    - attack.initial-access
    - attack.t1190
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Legitimate PDF/Office Generation tools
  * The Issue: Web applications often generate reports by spawning helper processes (e.g. printing engines or converters) which can trigger general child-process alerts.
  * The Fix: Scope exclusions to allow only specific, fully-parameterized command lines from trusted bin directories (e.g., whitelisting specific report converter binary execution).

* Problem 2: Execution via Java wrapper shells (Double fork)
  * The Issue: If the Java process spawns an intermediate system wrapper service (such as `wrapper.exe`) which in turn launches the malicious commands, the parent process path is no longer `java.exe`.
  * The Fix: Implement multi-tiered parent-child correlation rules checking the grandparent process context, or scan for non-standard executables running under service accounts.

* Problem 3: In-Memory Web Shells (Filter/Listener Injection)
  * The Issue: Modern web shells can run completely in-memory by registering a Servlet Filter or Endpoint Listener inside the Tomcat/Java JVM. They handle incoming requests and run Java code without spawning external command shells.
  * The Fix: Monitor JVM class-loading behaviors for dynamically compiled classes, and alert on unexpected file creation events inside Java servlet directory structures.

