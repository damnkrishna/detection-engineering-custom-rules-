### 1. Technique Breakdown: T1021.003

* **Description**: Detects lateral movement via Distributed COM (DCOM) where an inbound RPC/DCOM session on port 135 (Zeek) is followed by Microsoft Excel (`excel.exe`) spawning process shells (such as `cmd.exe`, `powershell.exe`, `pwsh.exe`, `cscript.exe`, or `wscript.exe`) on the target host within 15 minutes.
* **Author**: Krishna Gupta
* **Log Source Requirements**:
  * Zeek connection logs auditing RPC traffic on port 135 (DCOM port) with target anchoring.
  * Sysmon Process Creation (Event ID 1) where the parent process is `excel.exe` and the child process is a shell or scripting interpreter.
* **Coverage Check**:
  * HIDS (Sysmon): YES. Mapped to process creation. When Excel runs via DCOM, it is instantiated by `svchost.exe -k DcomLaunch`, and if it spawns a command shell, that process runs as a child of `excel.exe`.
  * NIDS (Zeek): YES. Captures the initial RPC bind calls and DCOM activations on port 135.

---

### 2. Custom Correlation Rule

```json
{
  "ruleId": "CR0097_T1021_T1204",
  "ruleName": "DCOM Remote Execution via Excel.Application Object",
  "ruleFormat": "IR_v1",
  "ruleType": "THRESHOLD",
  "ruleOrigin": "custom",
  "description": "Detects RPC traffic followed by excel.exe spawning cmd.exe or powershell.exe on the target host within 15 minutes.",
  "enabled": true,
  "version": 1,
  "correlationKey": "entity_key",
  "eventSources": [
    "zeek",
    "windows_sysmon"
  ],
  "window": {
    "type": "sliding",
    "minSpanSeconds": 0,
    "durationSeconds": 900
  },
  "output": {
    "tags": [
      "atomic-red-team",
      "bzar"
    ],
    "message": "DCOM RPC connection followed by excel.exe spawning command shell detected",
    "severity": "MEDIUM",
    "entityField": "host.name",
    "mitreTactics": [
      "TA0008",
      "TA0002"
    ],
    "includeFields": [],
    "riskScoreDelta": 3,
    "mitreTechniques": [
      "T1021.003"
    ]
  },
  "stages": [
    {
      "stageIndex": 0,
      "match": {
        "groups": [
          {
            "groups": [],
            "operator": "OR",
            "conditions": [
              {
                "field": "signature_name",
                "value": "RPC traffic",
                "values": [],
                "operator": "contains"
              },
              {
                "field": "signature_name",
                "value": "DCOM connection established",
                "values": [],
                "operator": "contains"
              }
            ]
          }
        ],
        "operator": "AND",
        "conditions": [
          {
            "field": "dest_port",
            "value": "135",
            "values": [],
            "operator": "equals"
          },
          {
            "field": "entity_key_direction",
            "value": "target",
            "values": [],
            "operator": "equals"
          }
        ]
      },
      "minGapMs": 0,
      "aggregate": {
        "field": null,
        "groupBy": [
          "host.name"
        ],
        "function": "count",
        "threshold": 1,
        "distinctField": null
      },
      "eventSources": [
        "zeek"
      ],
      "withinSeconds": 0,
      "minOccurrences": 0,
      "correlation_key": "host.name",
      "minDistinctCount": 0,
      "requireDistinctField": null,
      "excludeSignatureContains": []
    },
    {
      "stageIndex": 1,
      "match": {
        "groups": [
          {
            "groups": [],
            "operator": "OR",
            "conditions": [
              {
                "field": "process_name",
                "value": "cmd.exe",
                "values": [
                  "cmd.exe",
                  "powershell.exe",
                  "pwsh.exe",
                  "cscript.exe",
                  "wscript.exe"
                ],
                "operator": "in"
              }
            ]
          }
        ],
        "operator": "AND",
        "conditions": [
          {
            "field": "parent_process_name",
            "value": "excel.exe",
            "values": [],
            "operator": "equals"
          }
        ]
      },
      "minGapMs": 0,
      "aggregate": {
        "field": null,
        "groupBy": [
          "host.name"
        ],
        "function": "count",
        "threshold": 1,
        "distinctField": null
      },
      "eventSources": [
        "windows_sysmon"
      ],
      "withinSeconds": 900,
      "minOccurrences": 0,
      "correlation_key": "host.name",
      "minDistinctCount": 0,
      "requireDistinctField": null,
      "excludeSignatureContains": []
    }
  ],
  "suppression": {
    "groupBy": [
      "host.name"
    ],
    "durationSeconds": 300
  },
  "maxGapSeconds": 900,
  "behavioralConstraints": null,
  "maxConcurrentInstances": 0
}
```

---

### 3. Blind Spots & Tuning (The "Problems")

* **Problem 1: Financial Report Automation**
  * *Issue*: Legacy business software may automate Excel sheet processing via DCOM calls, generating false alerts if Excel executes external add-ins.
  * *Fix*: Ignore Excel processes spawning known, signed reporting scripts or restrict the alert to generic shell commands (e.g. `cmd.exe /c` or `powershell.exe -enc`).
* **Problem 2: DCOM Launching Latency**
  * *Issue*: An active DCOM session might remain open, and user interaction within the spreadsheet long after could spawn a shell.
  * *Fix*: Keep the correlation window relatively short (15 minutes or less) to ensure the execution is coupled with the initial remote command request.
