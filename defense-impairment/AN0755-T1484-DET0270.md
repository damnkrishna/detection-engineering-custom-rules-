### 1. Technique Breakdown: T1484

* What it is: Domain or Tenant Policy Modification. Adversaries modify domain or tenant policies to establish persistence, elevate privileges, or facilitate lateral movement. In Active Directory, this includes modifying GPOs, altering domain trust relationships (to allow access from external malicious domains), or changing directory service object permissions (to grant replication/delegation rights). Adversaries use CLI tools like `netdom.exe`, `nltest.exe`, or PowerShell AD modules to make these modifications.
* Log Source Requirements: Endpoint logs capturing process creation and command line parameters (specifically targeting administrative Active Directory and trust management utilities).
* Coverage Check:
* HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 (Process Creation) logs the execution of utilities like `netdom.exe`, `nltest.exe`, or PowerShell Active Directory cmdlets.
* NIDS (Suricata + Zeek): NO. Trust and policy modifications are local configurations or RPC-based changes. Network logs can capture the replication or RPC calls (like `DRSUAPI`), but do not parse them as policy modification events.
* Log Sources Covered:
  - Sysmon Event ID 1 (process_creation)
  - Sysmon Event ID 4104 (ps_script)
* Log Sources Missing / Unused:
  - None.

---

### 2. Custom Sigma Rule

The detection logic is split into process creation monitoring and PowerShell script block logging of Active Directory trust cmdlet usage.

Rule 1 of 2: Command-Line Domain Trust or Policy Modification (Process Creation)
```yaml
title: Command-Line Domain Trust or Policy Modification (AN0755)
id: c58b2e1c-4f3b-4a2e-9d8c-6b7c8d9e0f0a
status: experimental
description: Detects command-line execution of netdom.exe, nltest.exe, or PowerShell Active Directory cmdlets used to modify domain trust relationships, register rogue domain controllers, or alter domain policy.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_netdom:
        - Image|endswith: '\netdom.exe'
        - OriginalFileName: 'netdom.exe'
    selection_netdom_args:
        CommandLine|contains:
            - 'trust'
            - 'member'
    selection_nltest:
        - Image|endswith: '\nltest.exe'
        - OriginalFileName: 'nltest.exe'
    selection_nltest_args:
        CommandLine|contains:
            - '/trust_commit'
            - '/dsreg'
            - '/domain_trusts'
    selection_powershell:
        - Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
        - OriginalFileName:
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_powershell_cmdlets:
        CommandLine|contains:
            - 'Add-ADDomainTrust'
            - 'Remove-ADDomainTrust'
            - 'Set-ADDomainTrust'
    condition: (selection_netdom and selection_netdom_args) or (selection_nltest and selection_nltest_args) or (selection_powershell and selection_powershell_cmdlets)
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Authorized active directory migration activities or configuration of cross-forest trusts by enterprise domain administrators.
level: high
tags:
    - attack.defense-evasion
    - attack.privilege-escalation
    - attack.t1484
    - analytic.an0755
    - detection.det0270
```

Rule 2 of 2: Domain Trust or Policy Modification via Script Block (PowerShell Script Block)
```yaml
title: Domain Trust or Policy Modification via Script Block (AN0755-B)
id: 7b373ce9-b605-44b5-89dc-e11a83ba2c0a
status: experimental
description: Detects executing PowerShell script blocks containing Active Directory trust modification cmdlets, indicating direct configuration tampering.
author: Krishna Gupta
date: 2026-06-17
logsource:
    category: ps_script
    product: windows
detection:
    selection:
        ScriptBlockText|contains:
            - 'Add-ADDomainTrust'
            - 'Remove-ADDomainTrust'
            - 'Set-ADDomainTrust'
    condition: selection
fields:
    - ScriptBlockText
    - User
falsepositives:
    - Authorized administration script automation.
level: high
tags:
    - attack.defense-evasion
    - attack.privilege-escalation
    - attack.t1484
    - analytic.an0755
    - detection.det0270
```

### OCSF Normalized

Rule 1 of 2: Command-Line Domain Trust or Policy Modification (Process Activity)
```xml
title: Command-Line Domain Trust or Policy Modification (AN0755) (OCSF-Normalized)
id: c58b2e1c-4f3b-4a2e-9d8c-6b7c8d9e0f0a
status: experimental
description: Detects command-line execution of netdom.exe, nltest.exe, or PowerShell Active Directory cmdlets used to modify domain trust relationships, register rogue domain controllers, or alter domain policy.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_netdom:
        - process.file.path|endswith: '\netdom.exe'
        - process.file.path: 'netdom.exe'
    selection_netdom_args:
        process.cmd_line|contains:
            - 'trust'
            - 'member'
    selection_nltest:
        - process.file.path|endswith: '\nltest.exe'
        - process.file.path: 'nltest.exe'
    selection_nltest_args:
        process.cmd_line|contains:
            - '/trust_commit'
            - '/dsreg'
            - '/domain_trusts'
    selection_powershell:
        - process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
        - process.file.path:
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_powershell_cmdlets:
        process.cmd_line|contains:
            - 'Add-ADDomainTrust'
            - 'Remove-ADDomainTrust'
            - 'Set-ADDomainTrust'
    condition: (selection_netdom and selection_netdom_args) or (selection_nltest and selection_nltest_args) or (selection_powershell and selection_powershell_cmdlets)
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Standard administrative tasks or baselined activity.
level: high
tags:
    - attack.defense-evasion
    - attack.t1484
```

Rule 2 of 2: Domain Trust or Policy Modification via Script Block (Process Activity)
```xml
title: Domain Trust or Policy Modification via Script Block (OCSF-Normalized) - Rule 2 of 2
id: 7b373ce9-b605-44b5-89dc-e11a83ba2c0a
status: experimental
description: Detects executing PowerShell script blocks containing Active Directory trust modification cmdlets, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-17
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection:
        process.cmd_line|contains:
            - 'Add-ADDomainTrust'
            - 'Remove-ADDomainTrust'
            - 'Set-ADDomainTrust'
    condition: selection
fields:
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Administrative automation scripts.
level: high
tags:
    - attack.defense-evasion
    - attack.t1484
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Command Line Obfuscation
  * The Issue: Adversaries can obfuscate command-line arguments using environment variables, caret escapes (^), or base64 encoding, evading simple string matching.
  * The Fix: Implement PowerShell Script Block Logging and utilize regex or script-decoding scripts in the SIEM to check arguments.

* Problem 2: Administrative Activity Noise
  * The Issue: IT administrators legitimately run command-line tools or installers that trigger these rules during system maintenance.
  * The Fix: Establish a baseline of legitimate admin tools, whitelisting specific administrative account names or management servers.

* Problem 3: Masquerading via Renamed Binaries
  * The Issue: Attackers rename native Windows utilities (such as net.exe or cmd.exe) to custom filenames to bypass path-based filters.
  * The Fix: Incorporate Masquerade-resistant criteria, matching on OriginalFileName rather than the static Image path in process creation logs.
