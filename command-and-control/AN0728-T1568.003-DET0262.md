### 1. Technique Breakdown: T1568.003

* What it is: Dynamic Resolution: DNS Calculation. Adversaries derive dynamic command and control ports or IPs mathematically from DNS queries. This is often done by querying DNS TXT records, which can contain base64 encoded payloads or configuration data, or using specific octets from a resolved IP address to determine a custom port number for the subsequent C2 socket connection.
* Log Source Requirements: Host process creation and command line logs (Sysmon Event ID 1 / Windows Security Event ID 4688) and PowerShell Script Block Logs (Event ID 4104).
* Coverage Check:
    * HIDS (Sysmon + Windows Defender): YES. Sysmon Event ID 1 captures execution of nslookup or dig. Event ID 4104 captures PowerShell script blocks querying TXT records.
    * Windows Security Logs: PARTIAL. Security logs EID 4688 captures command lines if enabled.
* Log Sources Covered:
    - PowerShell Event ID 4104 (ps_script)
    - Sysmon Event ID 1 (process_creation)
* Log Sources Missing / Unused:
    - None.

---

### 2. Custom Sigma Rule

The detection logic is split into two rules targeting script block execution and command-line execution separately.

Rule 1 of 2: DNS TXT Query via PowerShell Script Block (Script Block)
```yaml
title: DNS TXT Query via PowerShell Script Block (AN0728-A)
id: 9c148ad0-cafa-41e4-8301-28fc980d7c3e
status: experimental
description: Detects PowerShell script blocks invoking DNS queries for TXT records, which can indicate dynamic resolution or C2 configuration retrieval.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: ps_script
    product: windows
    # Relies on PowerShell Script Block Logging (Event ID 4104)
detection:
    selection_keywords:
        ScriptBlockText|contains:
            - 'Resolve-DnsName'
            - '[System.Net.Dns]'
            - 'GetHostAddresses'
    selection_txt:
        ScriptBlockText|contains:
            - 'TXT'
            - '16'
    condition: selection_keywords and selection_txt
fields:
    - ScriptBlockText
falsepositives:
    - Legitimate administrative script tasks auditing DNS SPF/TXT records.
level: high
tags:
    - attack.command-and-control
    - attack.t1568.003
    - analytic.an0728
    - detection.det0262
```

Rule 2 of 2: DNS TXT Query via CLI (Process Creation)
```yaml
title: DNS TXT Query via CLI (AN0728-B)
id: 47288ad0-cafa-41e4-8301-28fc980d7c3e
status: experimental
description: Detects command line execution of utilities querying DNS TXT records, indicating dynamic C2 calculation attempts.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_process:
        - Image|endswith:
            - '\nslookup.exe'
            - '\dig.exe'
            - '\host.exe'
        - OriginalFileName:
            - 'nslookup.exe'
            - 'dig.exe'
    selection_args:
        CommandLine|contains:
            - 'txt'
            - 'type=txt'
            - '-query=txt'
    condition: selection_process and selection_args
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - System administrators performing manual DNS checks or SPF queries.
level: high
tags:
    - attack.command-and-control
    - attack.t1568.003
    - analytic.an0728
    - detection.det0262
```

### OCSF Normalized

Rule 1 of 2: DNS TXT Query via PowerShell Script Block (Process Activity)
```xml
title: DNS TXT Query via PowerShell Script Block (OCSF-Normalized) - Rule 1 of 2
id: 9c148ad0-cafa-41e4-8301-28fc980d7c3e
status: experimental
description: Detects PowerShell script blocks querying DNS TXT records, mapped to OCSF process_activity (representing PowerShell code execution).
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_keywords:
        process.cmd_line|contains:
            - 'Resolve-DnsName'
            - '[System.Net.Dns]'
            - 'GetHostAddresses'
    selection_txt:
        process.cmd_line|contains:
            - 'TXT'
            - '16'
    condition: selection_keywords and selection_txt
fields:
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Legitimate administrative queries.
level: high
tags:
    - attack.command-and-control
    - attack.t1568.003
```

Rule 2 of 2: DNS TXT Query via CLI (Process Activity)
```xml
title: DNS TXT Query via CLI (OCSF-Normalized) - Rule 2 of 2
id: 47288ad0-cafa-41e4-8301-28fc980d7c3e
status: experimental
description: Detects CLI tools querying DNS TXT records, mapped to OCSF process_activity.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_process:
        process.file.path|endswith:
            - '\nslookup.exe'
            - '\dig.exe'
            - '\host.exe'
    selection_args:
        process.cmd_line|contains:
            - 'txt'
            - 'type=txt'
            - '-query=txt'
    condition: selection_process and selection_args
fields:
    - process.file.path
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Manual administrative DNS verification.
level: high
tags:
    - attack.command-and-control
    - attack.t1568.003
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Stateful Multi-Event Correlation Requirements
  * The Issue: Real DNS calculation involves comparing the IP resolved in Event ID 22 with the destination IP/Port in Event ID 3. This mathematical verification is too complex for standard stateless Sigma rules, which can lead to missed alerts if the attacker uses a standard A record but connects to derived ports.
  * The Fix: Implement a stateful correlation rule in the SIEM engine (e.g. using Splunk SPL or Elasticsearch EQL) that extracts the resolved IP from the DNS event, calculates the derived port, and alerts if a network connection matches those specific parameters within a 60-second window.

* Problem 2: DNS over HTTPS (DoH) / DNS over TLS (DoT)
  * The Issue: If the malware uses a public resolver via HTTPS (DoH), host queries are encrypted and will not appear in local Windows DNS log Event ID 22 or Zeek DNS logs.
  * The Fix: Enforce the use of internal DNS servers via enterprise Group Policies, block direct egress connections to known public DoH/DoT IPs on the perimeter firewall, and alert on non-browser processes communicating with public DoH endpoints.

* Problem 3: Administrative Domain Verification Checks
  * The Issue: System administrators regularly query TXT records for DNS auditing, SPF verification, or domain configuration validation, generating false alarms.
  * The Fix: Whitelist known administrative systems and limit this detection to user workstations.
