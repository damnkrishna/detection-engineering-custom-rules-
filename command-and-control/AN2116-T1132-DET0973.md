### 1. Technique Breakdown: T1132

* What it is: Data Encoding. Adversaries may encode data (e.g., using Base64, Hexadecimal, or custom schemes) to obscure payload delivery or command and control traffic. This helps evade basic content filters or signature-based intrusion detection systems. Detecting the CLI execution of command parameters that invoke encoding functions (such as PowerShell base64/hex conversions) helps flag these obfuscation attempts.
* Log Source Requirements:
  * Sysmon Process Creation (Event ID 1) capturing process command-lines containing explicit data encoding/decoding keywords.
* Coverage Check:
  * HIDS (Sysmon): YES. Processes run with encoding/decoding arguments leave command-line logs.
  * NIDS (Zeek/Suricata): PARTIAL. Network logs can capture encoded payloads, but endpoint rules correlate the action to specific local processes.
* Log Sources Covered:
  * Sysmon Event ID 1 (process_creation)
* Log Sources Missing / Unused:
  * None.

---

### 2. Custom Sigma Rule

```yaml
title: Process Command Line Data Encoding or Decoding Activity (AN2116)
id: 0b8d5d5f-9644-4d79-a400-6a3c3fb9ddaa
status: experimental
description: "Detects process command-line parameters indicating the execution of data encoding or decoding operations (e.g., PowerShell conversion classes, certutil hex transformations, or base64 decoding utilities) commonly used to mask payload execution or exfiltration streams."
author: Krishna Gupta
date: 2026-07-01
logsource:
    category: process_creation
    product: windows
detection:
    selection_images:
        Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\certutil.exe'
            - '\wscript.exe'
            - '\cscript.exe'
    selection_ofn:
        OriginalFileName:
            - 'PowerShell.EXE'
            - 'pwsh.exe'
            - 'Cmd.Exe'
            - 'certutil.exe'
            - 'wscript.exe'
            - 'cscript.exe'
    selection_cli:
        CommandLine|contains:
            - 'FromBase64String'
            - 'ToBase64String'
            - '[System.Convert]'
            - '-encodehex'
            - '-decodehex'
            - 'base64 -d'
            - 'base64 --decode'
    filter_installer:
        ParentImage|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: (selection_images or selection_ofn) and selection_cli and not filter_installer
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - Legitimate administrative scripts or software installation tools that decode configuration values or license keys from base64 or hex during setup.
level: medium
tags:
    - attack.command-and-control
    - attack.t1132
    - analytic.an2116
    - detection.det0973
```

---

### 3. OCSF Normalized Rule

```yaml
title: 'Process Command Line Data Encoding or Decoding Activity (AN2116) (OCSF-Normalized)'
id: '0b8d5d5f-9644-4d79-a400-6a3c3fb9ddaa'
status: experimental
description: 'OCSF Normalized rule for Process Command Line Data Encoding or Decoding Activity (AN2116)'
author: Krishna Gupta
logsource:
    product: ocsf
    class: process_activity
detection:
    selection_images:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\certutil.exe'
            - '\wscript.exe'
            - '\cscript.exe'
    selection_ofn:
        process.file.name:
            - 'PowerShell.EXE'
            - 'pwsh.exe'
            - 'Cmd.Exe'
            - 'certutil.exe'
            - 'wscript.exe'
            - 'cscript.exe'
    selection_cli:
        process.cmd_line|contains:
            - 'FromBase64String'
            - 'ToBase64String'
            - '[System.Convert]'
            - '-encodehex'
            - '-decodehex'
            - 'base64 -d'
            - 'base64 --decode'
    filter_installer:
        unmapped.parent_image|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: (selection_images or selection_ofn) and selection_cli and not filter_installer
fields:
    - time
level: high
tags:
    - attack.command-and-control
    - attack.t1132
    - analytic.an2116
    - detection.det0973
```

---

### 4. Blind Spots & Tuning (The "Problems")

- Authorized IT administration activities.
- Tune with allowlists for known trusted execution paths.

---

### 5. Test Cases (Engine Pipeline Verification)

#### True Positives
* True Positive 1A: Standard Execution (Process Command Line Data Encoding or Decoding Activity (AN2116))
  * The Log: Activity simulating malicious behavior.
  * Expected Outcome: MUST trigger an alert.

* True Positive 1B: Malformed JSON Robustness Test (Process Command Line Data Encoding or Decoding Activity (AN2116))
  * The Log: Simulating 1A but intentionally omitting the `unmapped` object entirely.
  * Expected Outcome: Engine parser should degrade gracefully. MUST trigger an alert.

* True Positive 1C: Schema Type Validation Failure (Process Command Line Data Encoding or Decoding Activity (AN2116))
  * The Log: Exact match of 1A, but `activity_id` is passed as a string `"1"` instead of an integer `1`.
  * Expected Outcome: Tests if the engine's JSON parser coerces the type gracefully. MUST trigger an alert.

* True Positive 1D: Exact Duplicate Event (Process Command Line Data Encoding or Decoding Activity (AN2116))
  * The Log: Exact duplicate of 1A with the exact same timestamp.
  * Expected Outcome: Tests deduplication window.

#### Benign / True Negatives
* Benign 1: Allowed Activity (Process Command Line Data Encoding or Decoding Activity (AN2116))
  * The Log: Activity matching legitimate patterns.
  * Expected Outcome: Matches exclusion filters. MUST NOT trigger an alert.

---

### 6. Raw Telemetry Dataset (OCSF JSON)

#### Benign Telemetry
```jsonl
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Windows\\System32\\cmd.exe", "name": "cmd.exe"}, "cmd_line": "cmd.exe"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"generic": "benign_test"}, "metadata": {"test_case": "TN-01", "description": "Process Command Line Data Encoding or Decoding Activity (AN2116) (TN-01)", "expected_alert": false, "rule_uid": "0b8d5d5f-9644-4d79-a400-6a3c3fb9ddaa"}, "activity_id": 1, "time": 1718000010}
```

#### True Positive Telemetry
```jsonl
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\powershell.exe", "name": "PowerShell.EXE"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c FromBase64String"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"parent_image": "\\msiexec.exe"}, "metadata": {"test_case": "TP-01A", "description": "Process Command Line Data Encoding or Decoding Activity (AN2116) (TP-01A)", "expected_alert": true, "rule_uid": "0b8d5d5f-9644-4d79-a400-6a3c3fb9ddaa"}, "activity_id": 1, "time": 1718000100}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\powershell.exe", "name": "PowerShell.EXE"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c FromBase64String"}, "actor": {"user": {"name": "TargetUser"}}, "metadata": {"test_case": "TP-01B", "description": "Process Command Line Data Encoding or Decoding Activity (AN2116) (TP-01B)", "expected_alert": true, "rule_uid": "0b8d5d5f-9644-4d79-a400-6a3c3fb9ddaa"}, "activity_id": 1, "time": 1718000101}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\powershell.exe", "name": "PowerShell.EXE"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c FromBase64String"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"parent_image": "\\msiexec.exe"}, "metadata": {"test_case": "TP-01C", "description": "Process Command Line Data Encoding or Decoding Activity (AN2116) (TP-01C)", "expected_alert": true, "rule_uid": "0b8d5d5f-9644-4d79-a400-6a3c3fb9ddaa"}, "activity_id": "1", "time": 1718000102}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\powershell.exe", "name": "PowerShell.EXE"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c FromBase64String"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"parent_image": "\\msiexec.exe"}, "metadata": {"test_case": "TP-01D", "description": "Process Command Line Data Encoding or Decoding Activity (AN2116) (TP-01D)", "expected_alert": true, "rule_uid": "0b8d5d5f-9644-4d79-a400-6a3c3fb9ddaa"}, "activity_id": 1, "time": 1718000100}
```
