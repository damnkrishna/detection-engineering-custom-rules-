### 1. Technique Breakdown: T1102.001

* What it is: Web Service: Dead Drop Resolver. Adversaries host C2 endpoint IP addresses or connection configurations on high-reputation, legitimate web services (such as GitHub, Pastebin, Reddit, or Twitter). Malicious scripts or binaries execute on the target host and connect to these services to resolve and fetch the secondary C2 IP. Because these web services are trusted by IT departments, network connections to these domains are rarely inspected or blocked.
* Log Source Requirements: Endpoint network connection logs (Sysmon Event ID 3) and PowerShell Script Block Logs (Event ID 4104).
* Coverage Check:
    * HIDS (Sysmon + Windows Defender): YES. Sysmon Event ID 3 captures network activity. Event ID 4104 captures PowerShell scripts.
    * Windows Security Logs: NO. Local network activity and script content is primarily captured by Sysmon.
* Log Sources Covered:
    - Sysmon Event ID 3 (network_connection)
    - PowerShell Event ID 4104 (ps_script)
* Log Sources Missing / Unused:
    - None.

---

### 2. Custom Sigma Rule

The detection logic is split into two rules targeting network connections and scripting block contents separately.

Rule 1 of 2: Scripting Host Connection to Dead Drop Web Service (Network Connection)
```yaml
title: Scripting Host Connection to Dead Drop Web Service (AN0158-A)
id: b08fc96e-3217-4b6a-a336-2dd33e36b368
status: experimental
description: Detects scripting hosts or system command-line interpreters initiating network connections to known dead drop resolver domains (e.g., github.com, pastebin.com, reddit.com).
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: network_connection
    product: windows
    # Relies on Sysmon Event ID 3
detection:
    selection_process:
        Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\cmd.exe'
    selection_domains:
        DestinationHostname|contains:
            - 'github.com'
            - 'raw.githubusercontent.com'
            - 'pastebin.com'
            - 'reddit.com'
            - 'twitter.com'
            - 'instagram.com'
    condition: selection_process and selection_domains
fields:
    - Image
    - CommandLine
    - User
    - DestinationHostname
    - DestinationIp
    - DestinationPort
    - Initiated
    - SourceIp
falsepositives:
    - Legitimate administrative tasks or developers downloading code files and packages.
level: medium
tags:
    - attack.command-and-control
    - attack.t1102.001
    - analytic.an0158
    - detection.det0058
```

Rule 2 of 2: Dead Drop Web Service via PowerShell Script Block (Script Block)
```yaml
title: Dead Drop Web Service via PowerShell Script Block (AN0158-B)
id: 4158c96e-3217-4b6a-a336-2dd33e36b368
status: experimental
description: Detects PowerShell script blocks referencing dead drop resolver domains (e.g., github.com, pastebin.com, reddit.com) to resolve C2 endpoints.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: ps_script
    product: windows
    # Relies on PowerShell Script Block Logging (Event ID 4104)
detection:
    selection_keywords:
        ScriptBlockText|contains:
            - 'github.com'
            - 'raw.githubusercontent.com'
            - 'pastebin.com'
            - 'reddit.com'
            - 'twitter.com'
            - 'instagram.com'
    condition: selection_keywords
fields:
    - ScriptBlockText
falsepositives:
    - Administrative scripts updating local config or software from GitHub.
level: medium
tags:
    - attack.command-and-control
    - attack.t1102.001
    - analytic.an0158
    - detection.det0058
```

### OCSF Normalized

Rule 1 of 2: Scripting Host Connection to Dead Drop Web Service (Network Activity)
```xml
title: Scripting Host Connection to Dead Drop Web Service (OCSF-Normalized) - Rule 1 of 2
id: b08fc96e-3217-4b6a-a336-2dd33e36b368
status: experimental
description: Detects scripting hosts initiating network connections to dead drop resolver domains, mapped to OCSF network_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: network_activity
detection:
    selection_process:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\wscript.exe'
            - '\cscript.exe'
            - '\mshta.exe'
            - '\cmd.exe'
    selection_domains:
        activity.http_request.host|contains:
            - 'github.com'
            - 'raw.githubusercontent.com'
            - 'pastebin.com'
            - 'reddit.com'
            - 'twitter.com'
            - 'instagram.com'
    condition: selection_process and selection_domains
fields:
    - process.file.path
    - activity.http_request.host
    - activity.dst_endpoint.ip
    - activity.dst_endpoint.port
    - actor.user.name
falsepositives:
    - Legitimate administrative downloads or updates.
level: medium
tags:
    - attack.command-and-control
    - attack.t1102.001
```

Rule 2 of 2: Dead Drop Web Service via PowerShell Script Block (Process Activity)
```xml
title: Dead Drop Web Service via PowerShell Script Block (OCSF-Normalized) - Rule 2 of 2
id: 4158c96e-3217-4b6a-a336-2dd33e36b368
status: experimental
description: Detects PowerShell script blocks referencing dead drop resolver domains, mapped to OCSF process_activity (representing PowerShell code execution).
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_keywords:
        process.cmd_line|contains:
            - 'github.com'
            - 'raw.githubusercontent.com'
            - 'pastebin.com'
            - 'reddit.com'
            - 'twitter.com'
            - 'instagram.com'
    condition: selection_keywords
fields:
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Administrative scripting tasks downloading updates or tools.
level: medium
tags:
    - attack.command-and-control
    - attack.t1102.001
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Use of Custom or Obscure Dead Drop Platforms
  * The Issue: Attackers can use smaller, less common web forums, blogs, or obscure paste sites that are not included in the whitelisted domain rules, bypassing static domain checks.
  * The Fix: Implement heuristics that alert on any scripting engine querying external IP addresses or domains that are not part of the local system's baseline, and monitor execution of scripts downloading text files.

* Problem 2: Domain Fronting / CDN Routing
  * The Issue: Attackers can route traffic to a dead drop resolver through a Content Delivery Network (CDN), causing the DNS query to target a benign generic CDN domain name (like fastly.net or cloudfront.net), obscuring the true destination page.
  * The Fix: Enable SSL/TLS decryption to inspect the HTTP host header or URI path, and look for anomalous script interactions with generic CDN endpoints.

* Problem 3: Standard Administrative Scripting Noise
  * The Issue: Network engineers and automation tools frequently write scripts that fetch packages or configuration updates directly from public repositories (like GitHub or GitLab).
  * The Fix: Restrict the rule to monitor standard user workstation subnets, whitelisting developer and automation systems, and require script signing for administrative scripts.
