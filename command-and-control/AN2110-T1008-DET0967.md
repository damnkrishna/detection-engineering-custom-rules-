### 1. Technique Breakdown: T1008

* What it is: Fallback Channels. Adversaries may deploy secondary or redundant command and control (C2) channels to preserve system access in the event that primary communication pathways are discovered and severed by defender response actions. This commonly involves running local proxy or network tunneling daemons that tunnel traffic through alternative ports or protocols (e.g., Tor or proxy-over-DNS).
* Log Source Requirements:
  * Sysmon Process Creation (Event ID 1) to audit C2 proxy binaries and socks tunnel wrappers.
* Coverage Check:
  * HIDS (Sysmon): YES. Spawning fallback tools or proxies triggers process creation logging.
  * NIDS (Zeek/Suricata): PARTIAL. Network tools capture Tor handshake packets or DNS tunneling, but endpoint rules provide context on which local process initiated the channel.
* Log Sources Covered:
  * Sysmon Event ID 1 (process_creation)
* Log Sources Missing / Unused:
  * None.

---

### 2. Custom Sigma Rule

```yaml
title: Redundant Fallback Channel Proxy Execution (AN2110)
id: d4e5f6a7-b8c9-4d0e-9f0a-1b2c3d4e5f80
status: experimental
description: "Detects the execution of Tor daemons, tunneling clients, or socks proxy wrappers configured with fallback command-line parameters (e.g., configuring proxy bypasses or redundant routing) to establish secondary command and control paths."
author: Krishna Gupta
date: 2026-07-01
logsource:
    category: process_creation
    product: windows
detection:
    selection_anomalous_images:
        Image|endswith:
            - '\tor.exe'
            - '\dnscrypt-proxy.exe'
            - '\frpc.exe'
            - '\chisel.exe'
    selection_anomalous_ofn:
        OriginalFileName:
            - 'tor.exe'
            - 'dnscrypt-proxy.exe'
            - 'frpc.exe'
            - 'chisel.exe'
    selection_plink_image:
        Image|endswith:
            - '\plink.exe'
    selection_plink_ofn:
        OriginalFileName:
            - 'plink.exe'
    selection_plink_cli:
        CommandLine|contains:
            - ' -R '
            - ' -L '
            - ' -D '
            - ' -socks '
            - ' -N '
    filter_installer:
        ParentImage|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: (selection_anomalous_images or selection_anomalous_ofn or ((selection_plink_image or selection_plink_ofn) and selection_plink_cli)) and not filter_installer
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - User
falsepositives:
    - IT administrators using SSH/Plink or Chisel for legitimate remote support tunnels.
    - Security tools performing vulnerability scans with custom tunneling binaries.
level: high
tags:
    - attack.command-and-control
    - attack.t1008
    - analytic.an2110
    - detection.det0967
```

---

### 3. OCSF Normalized Rule

```yaml
title: 'Redundant Fallback Channel Proxy Execution (AN2110) (OCSF-Normalized)'
id: 'd4e5f6a7-b8c9-4d0e-9f0a-1b2c3d4e5f80'
status: experimental
description: 'OCSF Normalized rule for Redundant Fallback Channel Proxy Execution (AN2110)'
author: Krishna Gupta
logsource:
    product: ocsf
    class: process_activity
detection:
    selection_anomalous_images:
        process.file.path|endswith:
            - '\tor.exe'
            - '\dnscrypt-proxy.exe'
            - '\frpc.exe'
            - '\chisel.exe'
    selection_anomalous_ofn:
        process.file.name:
            - 'tor.exe'
            - 'dnscrypt-proxy.exe'
            - 'frpc.exe'
            - 'chisel.exe'
    selection_plink_image:
        process.file.path|endswith:
            - '\plink.exe'
    selection_plink_ofn:
        process.file.name:
            - 'plink.exe'
    selection_plink_cli:
        process.cmd_line|contains:
            - ' -R '
            - ' -L '
            - ' -D '
            - ' -socks '
            - ' -N '
    filter_installer:
        unmapped.parent_image|endswith:
            - '\msiexec.exe'
            - '\setup.exe'
    condition: (selection_anomalous_images or selection_anomalous_ofn or ((selection_plink_image or selection_plink_ofn) and selection_plink_cli)) and not filter_installer
fields:
    - time
level: high
tags:
    - attack.command-and-control
    - attack.t1008
    - analytic.an2110
    - detection.det0967
```

---

### 4. Blind Spots & Tuning (The "Problems")

- Authorized IT administration activities.
- Tune with allowlists for known trusted execution paths.

---

### 5. Test Cases (Engine Pipeline Verification)

#### True Positives
* True Positive 1A: Standard Execution (Redundant Fallback Channel Proxy Execution (AN2110))
  * The Log: Activity simulating malicious behavior.
  * Expected Outcome: MUST trigger an alert.

* True Positive 1B: Malformed JSON Robustness Test (Redundant Fallback Channel Proxy Execution (AN2110))
  * The Log: Simulating 1A but intentionally omitting the `unmapped` object entirely.
  * Expected Outcome: Engine parser should degrade gracefully. MUST trigger an alert.

* True Positive 1C: Schema Type Validation Failure (Redundant Fallback Channel Proxy Execution (AN2110))
  * The Log: Exact match of 1A, but `activity_id` is passed as a string `"1"` instead of an integer `1`.
  * Expected Outcome: Tests if the engine's JSON parser coerces the type gracefully. MUST trigger an alert.

* True Positive 1D: Exact Duplicate Event (Redundant Fallback Channel Proxy Execution (AN2110))
  * The Log: Exact duplicate of 1A with the exact same timestamp.
  * Expected Outcome: Tests deduplication window.

#### Benign / True Negatives
* Benign 1: Allowed Activity (Redundant Fallback Channel Proxy Execution (AN2110))
  * The Log: Activity matching legitimate patterns.
  * Expected Outcome: Matches exclusion filters. MUST NOT trigger an alert.

---

### 6. Raw Telemetry Dataset (OCSF JSON)

#### Benign Telemetry
```jsonl
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Windows\\System32\\cmd.exe", "name": "cmd.exe"}, "cmd_line": "cmd.exe"}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"generic": "benign_test"}, "metadata": {"test_case": "TN-01", "description": "Redundant Fallback Channel Proxy Execution (AN2110) (TN-01)", "expected_alert": false, "rule_uid": "d4e5f6a7-b8c9-4d0e-9f0a-1b2c3d4e5f80"}, "activity_id": 1, "time": 1718000010}
```

#### True Positive Telemetry
```jsonl
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\tor.exe", "name": "tor.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c  -R "}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"parent_image": "\\msiexec.exe"}, "metadata": {"test_case": "TP-01A", "description": "Redundant Fallback Channel Proxy Execution (AN2110) (TP-01A)", "expected_alert": true, "rule_uid": "d4e5f6a7-b8c9-4d0e-9f0a-1b2c3d4e5f80"}, "activity_id": 1, "time": 1718000100}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\tor.exe", "name": "tor.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c  -R "}, "actor": {"user": {"name": "TargetUser"}}, "metadata": {"test_case": "TP-01B", "description": "Redundant Fallback Channel Proxy Execution (AN2110) (TP-01B)", "expected_alert": true, "rule_uid": "d4e5f6a7-b8c9-4d0e-9f0a-1b2c3d4e5f80"}, "activity_id": 1, "time": 1718000101}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\tor.exe", "name": "tor.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c  -R "}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"parent_image": "\\msiexec.exe"}, "metadata": {"test_case": "TP-01C", "description": "Redundant Fallback Channel Proxy Execution (AN2110) (TP-01C)", "expected_alert": true, "rule_uid": "d4e5f6a7-b8c9-4d0e-9f0a-1b2c3d4e5f80"}, "activity_id": "1", "time": 1718000102}
{"category_uid": 1, "class_uid": 1007, "class_name": "Process Activity", "activity_name": "Launch", "process": {"file": {"path": "C:\\Temp\\tor.exe", "name": "tor.exe"}, "cmd_line": "C:\\Windows\\System32\\cmd.exe /c  -R "}, "actor": {"user": {"name": "TargetUser"}}, "unmapped": {"parent_image": "\\msiexec.exe"}, "metadata": {"test_case": "TP-01D", "description": "Redundant Fallback Channel Proxy Execution (AN2110) (TP-01D)", "expected_alert": true, "rule_uid": "d4e5f6a7-b8c9-4d0e-9f0a-1b2c3d4e5f80"}, "activity_id": 1, "time": 1718000100}
```
