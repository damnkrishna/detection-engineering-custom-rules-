### 1. Technique Breakdown: T1102.002

* What it is: Web Service: Bidirectional Communication. Adversaries leverage legitimate web services (such as Slack, Telegram, Discord, Dropbox, or Box APIs) as bidirectional Command and Control channels. They pull commands from the API (GET requests) and upload stolen data or command output (POST requests). Because these services are TLS-encrypted and trusted by organizations, this C2 channel successfully evades basic network boundary restrictions.
* Log Source Requirements: Endpoint network connection logs (Sysmon Event ID 3) and PowerShell Script Block Logs (Event ID 4104).
* Coverage Check:
    * HIDS (Sysmon + Windows Defender): YES. Sysmon Event ID 3 captures network activity. Event ID 4104 captures PowerShell scripts.
    * Windows Security Logs: NO. Local network activity and script content is primarily captured by Sysmon.
* Log Sources Covered:
    - Sysmon Event ID 3 (network_connection)
    - PowerShell Event ID 4104 (ps_script)
* Log Sources Missing / Unused:
    - None.

---

### 2. Custom Sigma Rule

The detection logic is split into two rules to target process execution connections and scripting block contents separately.

Rule 1 of 2: Bidirectional C2 Communication via Web Service API (Network Connection)
```yaml
title: Bidirectional C2 Communication via Web Service API (AN0100-A)
id: 933d2bd3-ba04-4d5d-95b5-39c4e3a225a6
status: experimental
description: Detects command-line interpreters or scripting hosts initiating network connections to common web service API domains (e.g., Telegram, Slack, Dropbox) used for bidirectional C2 communication.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: network_connection
    product: windows
    # Relies on Sysmon Event ID 3
detection:
    selection_process:
        Image|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\rundll32.exe'
            - '\mshta.exe'
            - '\regsvr32.exe'
            - '\wscript.exe'
            - '\cscript.exe'
    selection_domains:
        DestinationHostname|contains:
            - 'api.telegram.org'
            - 'api.slack.com'
            - 'slack.com'
            - 'dropboxapi.com'
            - 'discordapp.com'
            - 'discord.com'
            - 'api.box.com'
    condition: selection_process and selection_domains
fields:
    - Image
    - CommandLine
    - User
    - DestinationHostname
    - DestinationIp
    - DestinationPort
    - Initiated
    - SourceIp
    - SourcePort
falsepositives:
    - Legitimate administrative IT scripts using automation channels (such as Slack alerting bots or Box backup utilities).
level: high
tags:
    - attack.command-and-control
    - attack.t1102.002
    - analytic.an0100
    - detection.det0035
```

Rule 2 of 2: Bidirectional C2 via PowerShell Script Block (Script Block)
```yaml
title: Bidirectional C2 via PowerShell Script Block (AN0100-B)
id: 410a2bd3-ba04-4d5d-95b5-39c4e3a225a6
status: experimental
description: Detects PowerShell script blocks invoking web request methods targeting common web service API domains (Telegram, Slack, Dropbox, Discord, Box) used for Command and Control.
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: ps_script
    product: windows
    # Relies on PowerShell Script Block Logging (Event ID 4104)
detection:
    selection_keywords:
        ScriptBlockText|contains:
            - 'api.telegram.org'
            - 'api.slack.com'
            - 'slack.com'
            - 'dropboxapi.com'
            - 'discordapp.com'
            - 'discord.com'
            - 'api.box.com'
    condition: selection_keywords
fields:
    - ScriptBlockText
falsepositives:
    - Legitimate administrative or developer integration scripts communicating with authorized SaaS platforms.
level: high
tags:
    - attack.command-and-control
    - attack.t1102.002
    - analytic.an0100
    - detection.det0035
```

### OCSF Normalized

Rule 1 of 2: Bidirectional C2 Communication via Web Service API (Network Activity)
```xml
title: Bidirectional C2 Communication via Web Service API (OCSF-Normalized) - Rule 1 of 2
id: 933d2bd3-ba04-4d5d-95b5-39c4e3a225a6
status: experimental
description: Detects command-line interpreters or scripting hosts initiating network connections to common web service API domains, mapped to OCSF network_activity.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: network_activity
detection:
    selection_process:
        process.file.path|endswith:
            - '\powershell.exe'
            - '\pwsh.exe'
            - '\cmd.exe'
            - '\rundll32.exe'
            - '\mshta.exe'
            - '\regsvr32.exe'
            - '\wscript.exe'
            - '\cscript.exe'
    selection_domains:
        activity.http_request.host|contains:
            - 'api.telegram.org'
            - 'api.slack.com'
            - 'slack.com'
            - 'dropboxapi.com'
            - 'discordapp.com'
            - 'discord.com'
            - 'api.box.com'
    condition: selection_process and selection_domains
fields:
    - process.file.path
    - activity.http_request.host
    - activity.dst_endpoint.ip
    - activity.dst_endpoint.port
    - activity.src_endpoint.ip
    - activity.src_endpoint.port
falsepositives:
    - Legitimate administrative IT scripts using automation channels (such as Slack alerting bots or Box backup utilities).
level: high
tags:
    - attack.command-and-control
    - attack.t1102.002
```

Rule 2 of 2: Bidirectional C2 via PowerShell Script Block (Process Activity)
```xml
title: Bidirectional C2 via PowerShell Script Block (OCSF-Normalized) - Rule 2 of 2
id: 410a2bd3-ba04-4d5d-95b5-39c4e3a225a6
status: experimental
description: Detects PowerShell script blocks invoking web request methods targeting common web service API domains, mapped to OCSF process_activity (representing PowerShell code execution).
author: Krishna Gupta
date: 2026-06-08
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_keywords:
        process.cmd_line|contains:
            - 'api.telegram.org'
            - 'api.slack.com'
            - 'slack.com'
            - 'dropboxapi.com'
            - 'discordapp.com'
            - 'discord.com'
            - 'api.box.com'
    condition: selection_keywords
fields:
    - process.cmd_line
    - actor.user.name
falsepositives:
    - Legitimate administration scripts.
level: high
tags:
    - attack.command-and-control
    - attack.t1102.002
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Payload Obfuscation and Custom Webhooks
  * The Issue: Attackers can use custom webhook URLs on less common platforms or host their own cloud infrastructure, meaning simple static string checks for slack.com or telegram.org will not trigger.
  * The Fix: Implement egress profiling to alert on any unknown process executing network connections to standard external cloud service domains, and monitor for high-frequency connection patterns.

* Problem 2: API Usage over Legitimate Browsers
  * The Issue: If the attacker executes their API commands through a browser process (e.g., using headless Chrome or browser automation hooks), the network connection will appear under chrome.exe, causing this rule to miss it.
  * The Fix: Profile browser parentage to detect headless browser executions (such as Chrome spawned by Python, CMD, or PowerShell) and monitor browser extensions.

* Problem 3: Administrative API integrations
  * The Issue: Many IT organizations use Slack alerts or automated Dropbox backup routines, creating repeated alerts.
  * The Fix: Baseline and whitelist specific IT administration servers or service account usernames that are allowed to make these API connections.
