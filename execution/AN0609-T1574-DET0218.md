### 1. Technique Breakdown: T1574

* What it is: Hijack Execution Flow. Adversaries modify the environment Windows uses to locate and execute programs (such as DLL load paths, service binary configurations, or application pathways) to hijack execution flow. By changing the `binPath` of a service or pointing a service's `ServiceDll` registry key to a malicious module, they force a high-privilege system process to run their payload.
* Log Source Requirements: Endpoint logs capturing process creation and command line parameters (specifically targeting tools that modify service parameters or add service keys).
* Coverage Check:
* HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 (Process Creation) logs when utilities like `sc.exe` or `reg.exe` are called to modify service executable paths or service parameters.
* NIDS (Suricata + Zeek): NO. Service and execution flow modifications are local host tasks. They do not generate network logs.

---

### 2. Custom Sigma Rule

This rule utilizes Sysmon Event ID 1 (Process Creation) to detect command-line attempts to hijack system execution flow by modifying service configurations (`sc.exe config binpath=`) or adding registry keys that change service binary or library paths.

```yaml
title: Service Execution Flow Hijacking via CLI (AN0609)
id: c4e7d1b2-2e1f-4b92-8c7a-5f6d7e8a9b0d
status: experimental
description: Detects command-line service modifications or registry additions that alter the execution flow of system services (such as modifying service binPath or adding custom ServiceDlls).
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_sc:
        - Image|endswith: '\sc.exe'
        - OriginalFileName: 'sc.exe'
    selection_sc_args:
        CommandLine|contains|all:
            - 'config'
            - 'binpath'
    selection_reg:
        - Image|endswith:
            - '\reg.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
        - OriginalFileName:
            - 'reg.exe'
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_reg_args:
        CommandLine|contains|all:
            - 'System\CurrentControlSet\Services'
            - 'add'
        CommandLine|contains:
            - 'ImagePath'
            - 'ServiceDll'
    condition: (selection_sc and selection_sc_args) or (selection_reg and selection_reg_args)
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Legitimate software installers configuring service binary paths or creating custom services during application deployment.
level: high
tags:
    - attack.execution
    - attack.persistence
    - attack.privilege-escalation
    - attack.t1574
    - analytic.an0609
    - detection.det0218
```

### OCSF Normalized
```xml
title: Service Execution Flow Hijacking via CLI (AN0609) (OCSF-Normalized)
id: c4e7d1b2-2e1f-4b92-8c7a-5f6d7e8a9b0d
status: experimental
description: Detects command-line service modifications or registry additions that alter the execution flow of system services (such as modifying service binPath or adding custom ServiceDlls).
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_sc:
        - process.file.path|endswith: '\sc.exe'
        - process.file.path: 'sc.exe'
    selection_sc_args:
        process.cmd_line|contains|all:
            - 'config'
            - 'binpath'
    selection_reg:
        - process.file.path|endswith:
            - '\reg.exe'
            - '\powershell.exe'
            - '\pwsh.exe'
        - process.file.path:
            - 'reg.exe'
            - 'PowerShell.EXE'
            - 'pwsh.dll'
    selection_reg_args:
        process.cmd_line|contains|all:
            - 'System\CurrentControlSet\Services'
            - 'add'
        process.cmd_line|contains:
            - 'ImagePath'
            - 'ServiceDll'
    condition: (selection_sc and selection_sc_args) or (selection_reg and selection_reg_args)
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Standard administrative tasks or baselined activity.
level: high
tags:
    - attack.execution
    - attack.t1574
    - analytic.an0609
    - detection.det0218
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Command Line Obfuscation
  * The Issue: Adversaries can obfuscate command-line arguments using environment variables, caret escapes (^), or base64 encoding, evading simple string matching.
  * The Fix: Implement PowerShell Script Block Logging and utilize regex or script-decoding scripts in the SIEM to check arguments.

* Problem 2: Administrative Activity Noise
  * The Issue: IT administrators legitimately run command-line tools or installers that trigger these rules during system maintenance.
  * The Fix: Establish a baseline of legitimate admin tools, whitelisting specific administrative account names or management servers.

* Problem 3: Masquerading via Renamed Binaries
  * The Issue: Attackers rename native Windows utilities (such as net.exe or cmd.exe) to custom filenames to bypass path-based filters.
  * The Fix: Incorporate Masquerade-resistant criteria, matching on OriginalFileName rather than the static Image path in process creation logs.
