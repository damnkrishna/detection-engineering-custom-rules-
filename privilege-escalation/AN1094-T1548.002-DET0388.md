### 1. Technique Breakdown: T1548.002

* What it is: Bypass User Account Control. Windows User Account Control (UAC) allows a program to run with administrative privileges if approved by the user. Adversaries bypass this mechanism by exploiting auto-elevating programs that run with High Integrity without prompting. They do this by modifying registry entries (e.g., `isolatedCommand`, `DelegateExecute`, or `shell\open\command` keys) under the current user hive (`HKCU`), causing the auto-elevating binary to load a custom command instead of the default.
* Log Source Requirements: Endpoint logs capturing process creation and command line arguments (Sysmon Event ID 1 / Windows Security Event ID 4688).
* Coverage Check:
* HIDS (Sysmon + Wazuh): YES. Sysmon Event ID 1 captures command line arguments for tools modifying registry keys (`reg.exe`, `powershell.exe`). Sysmon Event ID 13 (Registry Value Set) also tracks direct registry modifications.
* NIDS (Suricata + Zeek): NO. Registry modifications are internal Windows operating system events and do not generate network activity.

---

### 2. Custom Sigma Rule

This rule utilizes Sysmon Event ID 1 (Process Creation) to detect command-line registry modifications targeting auto-elevation keys like `isolatedCommand`, `DelegateExecute`, or shell command overrides.

```yaml
title: Registry Modification for UAC Bypass (AN1094)
id: f70aef68-a67d-46d0-97bb-8d2d5a5a632a
status: experimental
description: Detects command-line registry modifications targeting UAC auto-elevation registry paths such as isolatedCommand, DelegateExecute, or shell\open\command keys under the user hive.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: process_creation
    product: windows
    # Relies on Sysmon Event ID 1
detection:
    selection_cmd:
        CommandLine|contains:
            - '\shell\open\command'
            - '\isolatedCommand'
            - '\DelegateExecute'
        CommandLine|contains:
            - 'reg'
            - 'add'
            - 'Set-ItemProperty'
            - 'New-ItemProperty'
            - 'setx'
    condition: selection_cmd
fields:
    - Image
    - OriginalFileName
    - CommandLine
    - ParentImage
    - ParentCommandLine
    - CurrentDirectory
    - User
    - LogonId
falsepositives:
    - Legitimate installers or administration scripts executing setup tasks.
level: high
tags:
    - attack.privilege-escalation
    - attack.defense-evasion
    - attack.t1548.002
    - analytic.an1094
    - detection.det0388
```

### OCSF Normalized
```xml
title: Registry Modification for UAC Bypass (AN1094) (OCSF-Normalized)
id: f70aef68-a67d-46d0-97bb-8d2d5a5a632a
status: experimental
description: Detects command-line registry modifications targeting UAC auto-elevation registry paths such as isolatedCommand, DelegateExecute, or shell\open\command keys under the user hive.
author: Krishna Gupta
date: 2026-06-07
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_cmd:
        process.cmd_line|contains:
            - '\shell\open\command'
            - '\isolatedCommand'
            - '\DelegateExecute'
        process.cmd_line|contains:
            - 'reg'
            - 'add'
            - 'Set-ItemProperty'
            - 'New-ItemProperty'
            - 'setx'
    condition: selection_cmd
fields:
    - actor.process.file.path
    - process.file.path
    - process.cmd_line
    - actor.user.name
    - unmapped.parent_image
    - unmapped.parent_cmd_line
falsepositives:
    - Standard administrative tasks or baselined activity.
level: high
tags:
    - attack.privilege-escalation
    - attack.t1548.002
    - analytic.an1094
    - detection.det0388
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Command Line Obfuscation
  * The Issue: Adversaries can obfuscate command-line arguments using environment variables, caret escapes (^), or base64 encoding, evading simple string matching.
  * The Fix: Implement PowerShell Script Block Logging and utilize regex or script-decoding scripts in the SIEM to check arguments.

* Problem 2: Administrative Activity Noise
  * The Issue: IT administrators legitimately run command-line tools or installers that trigger these rules during system maintenance.
  * The Fix: Establish a baseline of legitimate admin tools, whitelisting specific administrative account names or management servers.

* Problem 3: Masquerading via Renamed Binaries
  * The Issue: Attackers rename native Windows utilities (such as net.exe or cmd.exe) to custom filenames to bypass path-based filters.
  * The Fix: Incorporate Masquerade-resistant criteria, matching on OriginalFileName rather than the static Image path in process creation logs.
