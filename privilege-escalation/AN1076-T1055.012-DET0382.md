### 1. Technique Breakdown: T1055.012

* What it is: Process Injection: Process Hollowing. Adversaries spawn a legitimate system process (e.g., `svchost.exe`, `explorer.exe`) in a suspended state using the `CREATE_SUSPENDED` flag. They then unmap (hollow out) the original code from memory using `NtUnmapViewOfSection`. Next, they allocate new memory, write their malicious payload into the hollowed process space, modify the thread context to point to the payload, and resume the thread. The malicious code now runs disguised as a trusted system binary.
* Log Source Requirements: Sysmon Event ID 1 (Process Creation), Sysmon Event ID 10 (ProcessAccess), EDR telemetry (API hooking for `NtUnmapViewOfSection`).
* Coverage Check:
    * HIDS (Sysmon + Wazuh): YES (Partial). Sysmon tracks the creation of the suspended process and the subsequent access, but cannot natively log the `NtUnmapViewOfSection` API call.
    * NIDS (Suricata + Zeek): NO. Completely in-memory local execution.

---

### 2. Custom Sigma Rule

This rule utilizes Sysmon Event ID 10 (ProcessAccess) and correlates it with suspicious access masks that are indicative of a process hollowing sequence, particularly when a parent opens a handle to its own child to manipulate its memory.

```yaml
title: Suspicious Process Access indicating Process Hollowing (AN1076)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detects adversary use of suspended process creation followed by memory manipulation, indicative of process hollowing.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: process_access
    product: windows
    # Relies on Sysmon Event ID 10
detection:
    selection_target:
        TargetImage|endswith:
            - '\svchost.exe'
            - '\explorer.exe'
            - '\notepad.exe'
            - '\dllhost.exe'
    selection_access:
        GrantedAccess|contains:
            - '0x1F0FFF' # All Access
            - '0x143A'
    filter_known:
        SourceImage|endswith:
            - '\MsMpEng.exe'
            - '\services.exe'
    condition: selection_target and selection_access and not filter_known
fields:
    - SourceImage
    - TargetImage
    - GrantedAccess
falsepositives:
    - Antivirus solutions sandboxing or inspecting processes.
    - Specialized enterprise application wrappers.
level: high
tags:
    - attack.privilege-escalation
    - attack.defense-evasion
    - attack.t1055.012
    - analytic.an1076
    - detection.det0382
```

### OCSF Normalized
```xml
title: Suspicious Process Access indicating Process Hollowing (AN1076) (OCSF-Normalized)
id: fbbcc3f9-676b-4e6c-a8eb-8b0ab3606f1d
status: experimental
description: Detects adversary use of suspended process creation followed by memory manipulation, indicative of process hollowing.
author: Krishna Gupta
date: 2026-06-09
logsource:
    category: system
    product: ocsf
    class: process_activity
detection:
    selection_target:
        TargetImage|endswith:
            - '\svchost.exe'
            - '\explorer.exe'
            - '\notepad.exe'
            - '\dllhost.exe'
    selection_access:
        GrantedAccess|contains:
            - '0x1F0FFF' # All Access
            - '0x143A'
    filter_known:
        SourceImage|endswith:
            - '\MsMpEng.exe'
            - '\services.exe'
    condition: selection_target and selection_access and not filter_known
fields:
falsepositives:
    - Standard administrative tasks or baselined activity.
level: high
tags:
    - attack.privilege-escalation
    - attack.t1055.012
    - analytic.an1076
    - detection.det0382
```

---

### 3. Blind Spots & Tuning (The "Problems")

* Problem 1: Administrative Action False Positives
  * The Issue: IT personnel performing legitimate maintenance operations can invoke techniques that mirror adversarial persistence or evasion.
  * The Fix: Audit and baseline corporate administration accounts, whitelisting specific approved service accounts and administrative subnets.

* Problem 2: Command and Execution Obfuscation
  * The Issue: Attackers employ encoding, environment variables, or caret insertions to bypass keyword-based detection filters.
  * The Fix: Enable deep logging (such as Script Block Logging) and apply normalization or regex decoding inside the SIEM parsing pipeline.

* Problem 3: Log Deletion and Modification
  * The Issue: Adversaries can clear security logs or stop monitoring agents (like Wazuh or Sysmon) to blind defensive operations.
  * The Fix: Forward all endpoint logs immediately to a centralized write-once SIEM and alert on service stoppage or log clearing events.
